# Focus Groups — Per-Session Allowed-Apps Groups + Drag Duration

> Generated by the `generate-prompt` skill. Target: the Architect AI (produces a ZIP patch)
> which has full context of the App Tracker (RHEO) repo. This prompt is self-contained:
> it embeds the real source it depends on (see CONTEXT_BUNDLE.md) and the exact UI/skill
> rules to follow. **Do not invent APIs/SQL/tables that conflict with the live codebase.**

## Raw Request (verbatim, from the user)

> I have this idea where on the focus session things make the feature where you are able to focus and see on drag and set a time on how long you are going to be focused on. The more where you challenge yourself to be as focused minimum as possible, I would like to add a feature where we can add a group of things. If we want to focus on certain stuff for example, for now we want to focus on for example development, the applications and the website allowed. For instance, for example, Visual Studio Code, PyCharm and so on and so forth. And then in the next one, for example, I would like to focus on the video creation content creating field. Then the application that is on that list would be, for example, CapCut, the website would be kidney, there are other websites and so on and so forth, OBS and other things. So we can group those into categories of things of focus on the task that we want to focus on so that we can actually have it integrated with, for example, the daily goals of having a specific way to actually track whether we are actually doing this stuff by having this feature is really amazing in the way that you are actually showing that you have actually done a session where you focus on that thing that you want to do. I would like you to design the UI, design the backend, everything related to this skill. I would like you to use the generic prompt skill to configure all of this and to make sure that everything is working properly to make sure that it is a good functional feature. It is a human centric, it follows the human centric skill and uses all the empty paper design, all the front end skills and I would like you to use the generic prompt for the logics and the back ends and the features and make sure to include context of order, the existing features in UI on the real page right on the goals on, I mean, the focus page, right, the focus page. What we currently have and how we're going to be able to add this feature into it and what are the UI's and so on and so forth. So the prompt should focus on the UI but you are the ones that are supposed to design the actual looks and visual aesthetics of the UI but the AI with the generic promise of one that shows just decides on what are the UI to include and you're the one who's supposed to style them right raising your front end skills and MCP.

## Context

Read `CONTEXT_BUNDLE.md` in this folder first — it contains every source file, IPC contract,
schema, type, and design token this feature touches. The target AI must use ONLY the existing
infrastructure described there. Do not invent DB tables/IPC that conflict with `focusManager.ts`
or `GoalStore.ts`. The feature extends the **live** Deep Focus system, not `FocusPage.tsx`
(which is a dead import — see Context Bundle §9).

## Target AI profile

- The receiving AI is the **Architect** (Notion AI) operating in the `ai-collaboration-bridge`
  pipeline: it produces a ZIP patch (`agent/backups/`, `scripts/zip-src.mjs`, `node scripts/build.mjs`)
  of replacement/additional source files. It must emit, alongside the code, a short
  **implementation notes** block explaining the data flow and one **QA checklist** block.
- The Architect HAS repo context (it is not fileless); the fileless constraint in the general
  instructions applies to *earlier* receiving-AI turns — here it means "embed real code, don't
  describe." The zip it ships must compile with `node scripts/build.mjs`.

## Mandatory skills to activate & follow (in order)

1. `agent/skills/generate-prompt/SKILL.md` (this file's generator)
2. `agent/skills/frontend-external-infra` (source routing + re-skin rules)
3. `agent/skills/humancentred-UIUX` (the 6 pillars; EVERY new UI must cover empty/loading/error/populated + feedback + a11y + transitions)
4. `agent/skills/graphify` (only if a knowledge graph of categories→apps is helpful for grouping suggestions)
5. `agent/skills/agent-reflect` (read `agent/skills/agent-reflect/problem.md` + logs before finalising)

## Frontend component source routing (follow exactly — do NOT invent from zero)

| Need | Use |
|---|---|
| Standard button/row/select | shadcn `button`, `select`, `input`, `badge`, `card`, `scroll-area`, `separator` |
| Animated list / crossfade / tap | framer-motion `motion`/`AnimatePresence` (already a dep) |
| Spotlight-on-hover card | Magic UI `magic-card` (hardcoded dark — already vendored) |
| Edge glow for active focus | Magic UI `border-beam` |
| Circular timer progress | `AnimatedCircularProgressBar` (already installed) |
| Numeric ticker / countdown | Magic UI `number-ticker` |
| Particles background for active session | Magic UI `particles` |
| Icons | Lucide — resolve via `google-design-mcp_search_icons` first; candidates: `Timer`,`Clock`,`Target`,`Flag`,`Layers`,`Grid3x3`,`GripVertical`,`Edit`,`Trash2`,`Check`,`Plus`,`Pause`,`RefreshCw`,`Shield`,`ShieldCheck`,`Bookmark`,`AlertCircle` |
| Icon cloud for app picker | Magic UI `icon-cloud` |
| 3D/hero flair only | ReactBits `galaxy`/`particles` (heavy — use sparingly) |

## Anti-Slop Checklist (the output must pass EVERY line — block the patch if any fails)

1. **Type**: every new component is fully typed (props interface, no `any` beyond existing `goal: any` IPC marshalling).
2. **Color**: re-skin all sourced components to `bg-[rgba(24,24,27,0.60)] backdrop-blur-xl border border-zinc-800/60 rounded-xl`; Focus-domain accent is `#ec4899` (pink), goal-progress accent is `--dk-accent` (cyan). No default MagicUI pink/purple gradients unless the feature is Deep Focus.
3. **Geometry**: radius `rounded-xl` for cards, `rounded-lg` for buttons, `rounded-full` for chips/dots. `gap-2`/`p-3` increments.
4. **Hero pattern**: Focus Groups uses a *compact* group-list + a `DragDurationBar` + group chips — NOT a giant centred hero with a title + subtitle + CTA button.
5. **Section labels**: every new section has a `SectionHeader`-style label (`src/components/SectionHeader.tsx`) — never unlabelled blocks.
6. **Motion**: only `motion` tap-scale + `AnimatePresence` crossfades; never a blanket `animate-spin` wall of loaders.
7. **Imagery**: no AI stock photos; icons only. App icons fetched from `chrome://<icons>` via a 16px fallback colour chip if unavailable (no blank tiles).
8. **Empty states**: group list empty → "No focus groups yet — create your first group" CTA. Goal list empty → reuse existing `dk-empty-state`.
9. **Icons**: every action button has an `aria-label` + `title`; icon choice verified against the Lucide list above.
10. **Accessibility**: contrast ≥ 4.5:1 (check with `probe_assert_contrast`), ESC closes editor/modal, Tab order logical.

## Design System (re-skin rules — apply to ALL sourced/new components)

- Background surface: `bg-[rgba(24,24,27,0.60)]` + `backdrop-blur-xl` + `border border-zinc-800/60`.
- Cards: `rounded-xl p-5`, header bar with `border-b border-zinc-800/40`.
- Text: primary `#fafafa`, secondary `#e4e4e7`, faint `#71717a`. Numbers `font-mono tabular-nums`.
- Inputs: `bg-zinc-900/60 border border-zinc-700/50 focus:border-cyan-400/50 rounded-lg`.
- Buttons: primary `bg-emerald-500` (Go/start) / outline glass for secondary; tap `scale-95` via motion.
- Radius max `rounded-xl` (never `rounded-3xl` walls). Padding `p-5` for card bodies (`p-3` for inline).
- Focus-domain color: pink `#ec4899` (`#ec4899→#a78bfa` gradient for active ring). Goal-progress: cyan `#22d3ee`.

## Engineering / Data Layer

The **core insight** the receiving AI must exploit: `focus:start` ALREADY accepts
`cfg.allowed: { apps, domains, tiers }` and persists it to `deep_focus_sessions.allowed_json`.
Focus Groups therefore needs NO change to the enforcement engine — only:
(a) a persistent store of named groups, (b) a UI to manage them, (c) plumbing to pass a
group's `allowed` into `focus:start`, (d) linking the session to progress toward daily goals.

### Existing fact table for goal progress attribution
`useGoalProgress.ts` aggregates `logs` rows by lowercased `category` → seconds, matched against
`goal.target.matchCategory`. **Therefore a focus session "counts toward a goal" only if the
apps/websites the user actually used during the session are logged to `logs` with a `category`
equal to the goal's `matchCategory`.**

### Required additions

#### A. Storage: `src/domains/focus/focusGroupManager.ts` + `focusGroupSchema.ts`
- Add table `focus_groups`:
  `id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT UNIQUE, allowed_json TEXT NOT NULL,
   category TEXT, created_at TEXT DEFAULT (datetime('now','localtime'))`
  where `allowed_json` is `JSON.stringify({ apps:[...], domains:[...], tiers:[...] })`.
  (Mirrors the exact `allowed` shape already used by `focus:start`.)
- Add table `focus_group_usage`:
  `id INTEGER PRIMARY KEY AUTOINCREMENT, focus_group_id INTEGER REFERENCES focus_groups(id)
   ON DELETE CASCADE, session_id INTEGER REFERENCES deep_focus_sessions(id) ON DELETE CASCADE,
   goal_id TEXT, used_at TEXT DEFAULT (datetime('now','localtime'))` — links a focus session
  (and the goals its category matched) to a group, for the "show that you did a session for
  that thing" requirement.
- Service class `FocusGroupManager` (mirror `GoalStore` localStorage + DB dual pattern):
  `list(): FocusGroup[]`, `get(id): FocusGroup|null`, `save(group): FocusGroup`,
  `remove(id): void`, `link(sessionId, groupId, goalIds: string[]): void`.
  Storage: persist to DB via a new IPC `focus-group:*` (below) AND cache in
  `localStorage` under `df_focus_groups` for offline.
- `FocusGroup` type:
  `{ id: string; name: string; allowed: { apps: string[]; domains: string[]; tiers: Tier[] }; category: GoalCategory|null; createdAt: string }`

#### B. Preload + main IPC (do NOT break existing calls)
Add to `deskflowAPI` in `src/preload.ts` and `ipcMain.handle` in `main.ts`:
```typescript
focusGroup: {
  list: () => ipcRenderer.invoke('focus-group:list'),
  save: (group: { name: string; allowed: object; category?: string }) =>
    ipcRenderer.invoke('focus-group:save', group),
  remove: (id: string | number) => ipcRenderer.invoke('focus-group:remove', id),
  startWith: (groupId: string | number, durationSec: number, strictness: Strictness) =>
    ipcRenderer.invoke('focus-group:start-with', groupId, durationSec, strictness),
},
```
`focus-group:start-with` reads the group's `allowed_json`, then calls
`focusManager.start({ durationSec, strictness, allowed })` and records the linkage into
`focus_group_usage` (so we can later report "you spent X minutes on Development").
Return the `FocusPublicState` (and ideally also the `allowed`/category so the renderer can
credit goals — see data-flow note below).

#### C. Progress-attribution data flow (FIX the broken `useFocusGoals`)
`useFocusGoals.ts` is currently broken (reads fields not present in `getPublicState`). The
receiving AI must fix attribution. Preferred minimal approach that stays within existing
infra:
- When `focus:start` is called FOR a group, also stash `{ groupId, categories:
  [g.category].filter(Boolean) }` in a renderer-side singleton (`useFocusActiveGroup`).
- `useFocusGoals` reads that singleton (NOT `state.allowed_json` — which doesn't exist) to know
  which goals match the current session, and ticks `GoalStore.accumulateProgress` per second.
- On `focus:ended` / state→inactive, call `GoalStore.applyAccumulated` + `saveGoal` to persist.
- Keep `useGoalProgress.ts` (DB/category based) as the source of truth for the DailyPlannerCard
  list; the live accumulated seconds are a transient overlay.

This deliberately AVOIDS modifying `focusManager.ts`'s `getPublicState()` shape (which would
risk breaking QuickFocusCard/useDeepFocus); attribution is handled on the renderer side from
the group the user selected.

### D. Hook layer (new)
- `src/hooks/useFocusGroups.ts`: load/save/remove groups via `deskflowAPI.focusGroup.*`;
  exposes `{groups, loading, saveGroup, removeGroup, selectedId, setSelectedId}`.
  Cache in `localStorage` + optimistic update.
- `src/hooks/useDragDuration.ts`: drag-handle on a 0–180 min range; returns
  `{minutes, setMinutes, dragState}` and a `DragDurationBar` presenter. Mirrors the existing
  preset-chip UX but lets the user drag; minimum `1` min, snap to 5-min, live label.

### E. UI components (new, in `src/components/focus/`)
1. `FocusGroupSelector.tsx` — a compact pill-list / dropdown of saved groups + "New group"
   FAB. Selected group chip shows name + category dot; clicking opens `FocusGroupEditor`.
   On select, calls `focusGroup.startWith(id, duration, strictness)` then `start()`-style flow.
   Empty state: "Create a focus group (e.g. Development) to pre-set allowed apps."
2. `FocusGroupEditor.tsx` — modal drawer (shadcn `sheet` + `input`/`select`/`scroll-area`),
   two panes: **Apps** (multiselect from detected running apps) + **Websites** (chips input).
   Presets: "Development" (VS Code, PyCharm), "Video Creation" (CapCut, OBS, …). Tiers
   multi-select (`productive`/`neutral`/`distracting`). Save → `focusGroup.save`.
3. `DragDurationBar.tsx` — horizontal slider (shadcn `slider`) 5…180 min, thumb label follows
   the drag, snap 5-min. Renders the chosen minutes. Used INSIDE `FocusTimer` to replace/augment
   the static `PRESETS` chip row for the custom case (keep presets as quick-chip shortcuts).

### F. Wire-ins (minimal, surgical)
- `FocusSection.tsx`: add `<FocusGroupSelector>` above/beside the preset row; pass the
  selected group's `allowed` into `start()` (i.e. `handleStart = () => start(mins*60, strict, selectedGroup?.allowed)`).
  `useFocusSession.start` signature must accept an optional `allowed` (extend it; default
  `[]` keeps existing callers working).
- `FocusTimer.tsx`: render `<DragDurationBar>` next to/below the preset chips; when user drags,
  `onMinsChange` updates the same `mins` state (so timer + challenge both honour it). No
  behavioural conflict with existing `mode` logic.
- `DailyPlannerCard.tsx` `startFocus`: pass the currently-selected group (read from
  `useFocusGroups`) into `focus.start({…, allowed})` so a goal's `matchCategory` goal gets
  credited when the session's allowed apps produce logs in that category.
- `QuickFocusCard.tsx` (optional): add a tiny group selector + drag duration for the dashboard
  quick-start path (nice-to-have; mark if skipped).

## UX task (human-centred — the receiving AI designs the looks)

- **Group list** in FocusSection: horizontal scrollable chip row. Each chip: left-border
  accent = the group's category color (work=cyan `#22d3ee`, learning=purple `#a78bfa`,
  health=red `#f87171`, personal=green `#4ade80`), group name, a small count of
  apps/domains. Selected = filled `--dk-accent` background; hover = `border-white/20`.
  Empty → ghost "＋ New group" chip that opens the editor.
- **Group editor** (drawer, right-side, shadcn `sheet`): header "Edit group" / "New group",
  input field for name, section "Allowed apps" (multiselect list with running-app detection
  + colour chip), "Allowed websites" (input chips), "Protection tier" (multiselect tags
  `productive/neutral/non_allowed`-ish). CTA "Save group" disabled until name+≥1 rule.
- **Drag duration**: a 5…180 min slider, thumb is a small circular number ticker, track is
  `bg-zinc-800/80` with a cyan fill to the left, current value shown live at the thumb.
  Below: preset chips [15·25·45·90] that snap the slider.
- **Challenge mode** (stopwatch): when engaged, the duration slider hides and a "Stop
  challenge" button appears; the group restriction stays active so the challenge is bounded
  to the group's allowed set — directly satisfying the user's "challenge yourself" request.
- **Live goal credit**: while a grouped session is active, the matching goal's `GoalProgressBar`
  (existing) ticks up second-by-second (via `useFocusGoals` accumulator). Use
  `border-beam` on that goal's card to draw attention while active.
- **End-of-session toast**: on `focus:ended`, a transient snackbar shows:
  "Development · 28 min · 75% of 'Code Project' goal" — linking session→group→goal.
  Use `sonner` (installed shadcn) for the toast.
- **Empty / loading / error**:
  - Loading groups → skeleton chips (shimmer via `dk-skeleton`).
  - No groups → "No focus groups yet — create a group to pre-select apps for your next session."
  - Save failed → inline red banner on the drawer + retry.
- **Accessibility**: `aria-label` on every icon button, `role="group"` for the chip list,
  ESC closes the editor sheet, slider keyboard-accessible (←/→, Home/End).

## Verification tasks (the receiving AI must list these; the Hands&Eyes agent validates with Probe)

1. Focus groups list renders; "New group" opens editor; save persists; reload keeps it.
2. `focus-group:start-with` calls `focus:start` with the group's `allowed` (assert via IPC log).
3. Drag-duration slider updates `mins` used by `focus:start` (`durationSec`).
4. With a "Development" group active (allowed: VS Code in category `programming`) and a daily
   goal `match_category='programming'`, the goal's progress ticks up while the session is
   active (Probe assert text/percentage change; also `getAccumulated` > 0).
5. On session end, `progress_seconds` on the goal increases (or localStorage `df_goal_accum_*`
   increments) — persisted on `focus:ended`.
6. Black-screen regression: rebuild, attach via Probe, confirm `/activity?tab=focus` shows real
   focus UI (FocusSection) and the new selector — NOT a blank screen.

## Constraints

- Use ONLY installed deps (shadcn, MagicUI, ReactBits, lucide, framer-motion, sonner).
- All SQL parameterized; new tables scoped to `ensureFocusSchema` run at app boot.
- Goal shape stays **nested camelCase** (GoalStore `Goal`); do NOT introduce the flat
  snake_case shape from the legacy `daily-goals-planner/RESULT.md`.
- `useFocusGoals.ts` bug fix is REQUIRED (it reads non-existent state fields); fix in-scope.
- Preserve CRLF line endings on edited files.
- `node scripts/build.mjs` must exit 0 after the patch. Add `src/domains/focus/focusGroupSchema.ts`
  + `focusGroupManager.ts`, register tables in the same boot path as `ensureFocusSchema`.

## Anti-slop guardrail (human centric)

The receiving AI must NOT ship a generic "list + modal" with placeholder colors and a giant
hero. The design must feel like part of RHEO: the same glassmorphism `bg-[rgba(24,24,27,0.60)]`,
the cyan/emerald progress vocabulary, the pink Focus accent, and the same compact, dense,
`tabular-nums` aesthetic as `FocusTimer`/`QuickFocusCard`. If two sourced components would
look identical, pick the lighter one and re-skin it to the tokens above.
