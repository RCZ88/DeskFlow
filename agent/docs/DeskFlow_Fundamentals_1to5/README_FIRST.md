# READ ME FIRST — DeskFlow Fundamentals #1–#5 (cumulative)

This single zip contains **all five fundamentals features** in three files. It **replaces** every earlier fundamentals zip you downloaded:

- ~~DeskFlow_Stats_Revamp.zip~~ (Feature #1 only)
- ~~DeskFlow_ModelSwitcher.zip~~ (Feature #2 only)
- ~~DeskFlow_Fundamentals_1and2.zip~~ (Features #1+#2 only)

**Use only this one.** Ignore the older three — they are subsets.

## What to hand to opencode
Give opencode these 3 files to drop into the repo (full files, overwrite in place):
- `main.ts`
- `preload.ts`
- `pages/TerminalPage.tsx`

Then read `APPLY_FUNDAMENTALS_1-5.md` for exactly what changed and how to test each feature by hand.

## Verified before packaging
- All 5 feature markers present and coexisting in the 3 files.
- Isolated `tsc` (TS1xxx syntax) clean on all 3 files (only the pre-existing `main.ts` TS1345 at line ~8151 remains, untouched by this work).

## Repo path (for reference)
`C:\Users\cleme\Documents\COMPUTAH_SAYENCE\App Tracker`
