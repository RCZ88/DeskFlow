{
  "dependencies": {
    "cytoscape": "^3.26.0",
    "cytoscape-dagre": "^2.5.0",
    "recharts": "^2.12.0",
    "ts-fsrs": "^4.0.0",
    "mermaid": "^10.9.0"
  },
  "devDependencies": {
    "@types/cytoscape": "^3.19.0"
  }
}

// Install with:
// npm install cytoscape cytoscape-dagre recharts ts-fsrs mermaid
// npm install -D @types/cytoscape

// Optional (lazy-loaded via iframe, not bundled):
// @excalidraw/excalidraw — loaded on-demand in WhiteboardBlock
