# Collaboration Request: Generalizable "AI Agent System Framework" — File Extraction Discussion

## Your Role
You are the **Architect (Specialist) AI**. I am the **Project Owner AI** (opencode) running inside DeskFlow, a desktop productivity tracker whose embedded "Terminal Workspace" doubles as a full AI-agent infrastructure (multi-agent state, skills, context maintenance, terminal CLI). I have full access to the repository and can paste any file verbatim. You have NO repository access — everything I send you is your only source of truth.

We will collaborate through a structured back-and-forth. **This first round is a DISCUSSION, not a build order.** The goal: discuss what files and infrastructure exist here that can be **extracted and generalized** into a standalone repository that stores all skills + configurations for an AI-agent/analytics system, usable by ANYONE on ANY project — not tied to this app.

## The Idea (user's intent, preserved)
> "I want to make a separate project — not a project but rather like a repository that stores all of my skills and all of my configurations for an AI analytics system where it can be used by anyone. Everything related to the workspace, the agent, the workspace feature of this application — there's a lot of things related to how it manages the state. The AGENTS.md, the scripts for context management, to maintain the context management, and every single configuration on the workspace feature of the application, the terminal CLI, where we have a lot of features and configurations on all of those. The AGENTS.md that is generalistic for every single project, not specific. And how you're going to set up everything with the system prompt, which allows the AI agent to customize it for a particular project."

Repo name / location: **NOT IMPORTANT YET.** This round is only about the FILE discussion.

## Current Context (What I Have) — the inventory at a glance

The infrastructure lives in 8 layers. **The full context bundle is `CONTEXT_BUNDLE.md` with the core files EMBEDDED VERBATIM (Part A)**: AGENTS.md (operating contract), DEFAULT_SYSTEM_PROMPT.md (8-layer assembly), the Hub `state.md` + spoke `_template.md`, MEMORY.md (durable lessons), the Skill DSL guide, maintain-context, the back-and-forth skill itself, the skill router, the dictionary protocol, and the problems/requests JSON structures. The rest of the infrastructure is inventoried in Part B — any file can be pasted verbatim on `REQUEST:`.

1. **The Operating Contract** — `agent/AGENTS.md` (binding rules: zero-destruction, memory discipline, cycle reports, hard invariants) + `agent/DEFAULT_SYSTEM_PROMPT.md` (the 8-layer system-prompt assembly template) + `agent/GENERAL_ADDITIONS.md` (app-wide prompt additions) + `agent/RULES_COMPACT.md`.
2. **Multi-Agent State Management** — the "Hub + Spokes" contract: `agent/state.md` (auto-generated READ-ONLY hub), `agent/state/_template.md` (spoke template), `agent/state/{SESSION_ID}.md` (per-session spokes), `agent/state-archive.md` (append-only history), regenerated by `src/main/stateCoordinator.ts` (main process).
3. **Context Maintenance** — `agent/skills/maintain-context/SKILL.md` + `graphify_maintain.py` (severity 1–5 assessment, per-scale action matrix, graphify sync), plus the memory discipline rules in AGENTS.md.
4. **Skills Library** — 35 skills in `agent/skills/` with a YAML-frontmatter DSL (`agent/skills/SKILL_DSL_GUIDE.md`) and a `skill-router/SKILL.md` dispatcher (decision tree → mandatory/recommended skill mapping → load order). Includes the `backandfourth-skill` (AI Collaboration Bridge — this very conversation format).
5. **Terminal Workspace feature** — `/terminal` 5-group sidebar (Setup/Work/Insights/Studio/Context) + subtabs; `workspace_state` DB table + `workspace:save/list/load/delete` IPC (`src/main.ts`, `src/preload.ts`); terminal sessions in `terminal_sessions` (resume_id, status, cost, tokens); workspace components (WorkspaceCommandBar, WorkspaceGroupRail, SubTabBar, WorkspaceCard); design presets (`src/lib/designPresets.ts`, 8 presets); Conductor/swarm multi-agent orchestration (Work → Swarm subtab).
6. **Terminal CLI** — node-pty PTY lifecycle with the sacred event order (mark-spawned → spawn → created → initialize), split panes/tabs, agent interaction (agent phases launching→ready→busy→attention→error, agent:send, session-id capture), context delta messages (terminalWriteRaw), Fortress protocol (electron:execute-command).
7. **Knowledge Systems (6)** — Graphify (knowledge graph), LLM Wiki (all `agent/*.md`), Obsidian Skills (`agent/skills/`), PARA (`CZVault/`), QMD (`agent/templates/session.qmd`, `problem.qmd`), Automations (`agent/automations/automations.json`).
8. **Data & Glossary docs** — `agent/dictionary.md` (terminology-resolution protocol — extremely generalizable), `agent/data.md` (IPC/DB map), `agent/patterns.md`, `agent/context.md`, `agent/glossary.md`.

## The Discussion Focus (what I want from you)

Talk through, in order:

1. **What is genuinely extractable vs. app-specific.** Go through the inventory and mark each item: 🟢 extract as-is / 🟡 extract with generalization / 🔴 app-specific (leave behind). E.g. the black-screen checklist and vite build steps are DeskFlow-specific; the state contract, memory discipline, and cycle-report format are not.
2. **The AGENTS.md generalization problem.** The current AGENTS.md mixes generic operating contract (who you are, zero-destruction, backup route, memory discipline, cycle reports, hub+spokes) with app-specific build verification (black screen prevention). How do we split it so the framework ships a **project-agnostic AGENTS.md** that any project drops in, plus a project-specific overlay?
3. **The system-prompt layering pattern.** The app assembles prompts in 8 layers (default → general → agent-specific → project-specific → init content → thought process → auto-context → config directives) so an agent can be customized per project. How does this become the framework's "prompt customization" mechanism?
4. **The state contract.** Hub + Spokes with auto-regeneration, per-session spoke ownership, cycle-based updates. Can this be lifted out of Electron (it's currently regenerated by the main process) into a CLI/script that ANY agent workspace can use?
5. **The skills system.** The DSL format, skill-router, and the authoring discipline (humancentred-UIUX, frontend-external-infra, back-and-forth bridge). Which skills are universal assets for the framework's skill library?
6. **What the framework repository's file tree should look like** — proposed structure, given everything above. (Discussion only — no build order yet.)

## Context Gaps (What I Don't Have Yet)

- Full verbatim text of any file — I can paste on request. Ask with `REQUEST:` and the exact path.
- The 6 knowledge systems are DeskFlow-wired; whether the framework should include them as optional modules is your call to propose.
- CZVault PARA layout and automations.json are minimally verified — treat as ⚠️ until requested.
- Licensing / distribution of the framework — not decided yet.

## Conversation Protocol

1. **You ask specific questions.** Format: `REQUEST: <file path> — why you need it`
2. **I fetch and respond.** Format: `CONTEXT: <file path>` + verbatim content
3. **You refine.** Ask follow-ups, propose the extraction plan.
4. **When converged, produce RESULT.md** — the extraction plan: what to extract, the generalization mapping, and the proposed framework file tree.

**Rules:**
- Do NOT assume context you don't have. Ask for the exact file path.
- Do NOT produce a monolithic answer in Round 1. This is a discussion.
- Flag anything you think is NOT worth extracting — the goal is a lean framework, not a mirror of the app.

## Scope
- **IN:** discussion of extractable files, generalization mapping, framework structure proposal.
- **OUT:** no implementation, no repo creation, no file copying yet.

## Expected Output
When we converge: **RESULT.md** — (1) extraction verdict table, (2) generalization design for AGENTS.md + system-prompt layering + state contract + skills system, (3) proposed framework repository file tree, (4) build order for a future implementation round.

## First Question
Here is the full inventory (`CONTEXT_BUNDLE.md`). Start the discussion: **which files do you want to see verbatim first, and what is your initial take on the extract vs. generalize vs. leave split?**
