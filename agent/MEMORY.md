- [2026-08-23] SIGNATURE MOTION SYSTEM — THE CURRENT (RHEO): The ONE visual idea is "The Current" — a persistent directional pulse moving through a continuous visual field. NOT a background effect. It is a persistent visual entity that exists independently of the current route. Each page INTERPRETS the same underlying Current according to the structure of that page's information. Two-layer model: LivingSubstrate = "RHEO is alive" (ambient, organic), The Current = "RHEO is moving" (semantic, structural). The pulse phase persists across navigation — geometry changes, phase doesn't. The 10 mechanics become semantic renderers: `<RheoCurrent mode="network" />` not `<AdjacentBackground />`. Primitives: Pulse, Stream, Node, Edge, Branch, Orbit, Field, Contour, Signal, Mask — composable in workspace too. The Current must NEVER become a different animation on each page — its physics remain recognizable, its topology changes. Backandforth package at `agent/docs/backandfourth-docs/tugo-signature-motion/`. This project is called RHEO — never TURGO.

- [2026-08-23] DATABASE PAGE OVERHAUL (App Tracker): 3 major fixes + 2 new features. (1) ALLOWED_TABLES expanded from 22 to 130+ tables — old list had wrong names (problems→workspace_problems, wallets→finance_wallets, etc.) and was missing 150+ newer tables (finance, learn, content, conductor, compositions, context brain, etc.). Tables were listed in sidebar but returned "table not allowed" when clicked. (2) Pagination fixed: get-table-data IPC added OFFSET param (was ignored — every page showed same first 50 rows); preload bridge updated to pass offset; new get-table-data-count IPC for accurate total row count. (3) New get-table-changes IPC: queries by updated_at/created_at/rowid to return recent modifications per table. (4) DatabasePage redesigned with 3 tabs: Browse (category-grouped sidebar with row counts, PK indicators, pagination, CSV export, "Changes" button per table), Architecture (interactive SVG ER diagram with 12 domain groups, 34 FK relationships, zoom/pan, hover highlights, click-to-navigate), Recent Changes (table picker + expandable timestamped rows showing all columns). (5) deskflow-api.d.ts types updated. Build: vite OK (1m18s), preload.cjs 111KB, main.cjs 1418KB. ~175 total tables across 15 domain categories in the DB.

- [2026-08-18] AI CHAT ORGANIZER & CONTEXT LOOP FIXES (App Tracker): three critical fixes applied after user reported UI broken + features not working: (1) AiContextPanel v3 root div changed from `h-full overflow-hidden` to `max-h-[70vh] overflow-y-auto mt-2 shrink-0 rounded-xl border` — old `h-full` expanded to fill dk-root's 100% height, pushing topbar/sidebar off-screen via dk-root's overflow:hidden. (2) focusOverlay.js now handles DESKFLOW_GRAB_CHAT directly (scrape DOM messages, detectProviderFromUrl, send AI_CONTEXT_CAPTURED to background) instead of relaying through MAIN world — the old relay only worked on provider pages where ai-context-content.js loaded. (3) focusOverlay.js now handles DESKFLOW_INSERT_CONTEXT directly (find textarea/contenteditable, inject text, dispatch input/change events) instead of relaying through MAIN world — same fix pattern. Two-way loop verified: viewer → extensionQueueCommand → /extension/poll → background.js → focusOverlay.js → chat input. Build: vite OK (1m44s, index.CQXBppVZ.js 14.2MB), preload.cjs 113KB, main.cjs 1411KB. (App Tracker): extension manual capture (popup "Save this chat" → DESKFLOW_GRAB_CHAT → content script DOM grab → bufferCapture; "Add link" → POST /ai-context with source='manual'); backend: ai_context_groups table + 6 new columns (nickname/note/tags/group_id/pinned/is_manual) + 7 new IPC handlers (ai-context:update/groups/group-create/group-rename/group-delete + extension:queue-command + updated ai-context:list with group_id/pinned/tag/search); /extension/poll endpoint + pendingExtensionCommands queue for two-way loop; background.js polls every 2s for INSERT_INTO_CHAT commands; focusOverlay.js relays DESKFLOW_GRAB_CHAT and DESKFLOW_INSERT_CONTEXT between MAIN world and isolated world; content script injects text into chat inputs via textarea/contenteditable detection + input/change events; viewer v3 (AiContextPanel.tsx 281 lines): group filter chips with per-group colors, tag pills, inline edit form (nickname/note/tags/group/pin), copy transcript, open link, manual badge, pinned section, "Send to AI" dropdown (copy markdown + insert into chat), glass card styling, provider filter, search by nickname/note/content; preload 6 new bridges + deskflow-api.d.ts 6 new types. Build: vite OK (1m 35s, index.-BxKn2p5.js 14.2MB), preload.cjs 110.6KB, main.cjs 1378KB, AiContextPanel v3.0 stamp verified in bundle. NOT LAUNCHED — user must restart RHEO + reload extension.
- [2026-08-18] AI CHAT ORGANIZER & CONTEXT LOOP PROMPT PACKAGE (App Tracker): prompt package at `agent/docs/generate-prompt-docs/ai-chat-organizer-context-loop-18082026/` — PROMPT.md (9.8KB, Lead Designer AND Engineer → RESULT.md; Engineering A = extension manual capture: popup "Save this chat" + "Add link" → POST /ai-context source='manual'; Engineering B = org schema: nickname/note/tags/group_id/pinned/is_manual + ai_context_groups table + IPC ai-context:update/groups/capture-manual + preload + types; Engineering C = Viewer v3: group chips, tag filter, search by nickname/note, pinned section, edit dialog, copy transcript, open link, manual badge, per-group colors; Engineering D = two-way loop: viewer "Send to AI" → copy/open/insert via chrome.runtime 'DESKFLOW_INSERT_CONTEXT' message → content script injects into chat input; Design task = popup org section + viewer v3 glass cards all 4 states; MCP: shadcn + lucide + magic UI; no new deps, MV3, MAIN-world capture intact). CONTEXT_BUNDLE.md (29KB, self-contained verbatim: manifest.json + popup.html + popup.js + ai-context-content.js + focusOverlay.js + background.js relay + DB schema + main.ts handlers:7642-7708 + /ai-context server handler:20219-20282 + preload bridges:131-138 + deskflow-api.d.ts types:220-227 + AiContextPanel.tsx 485 lines + design tokens + 8 known gaps). FEATURE_TRACKER.md entry added at top. Extension lives at `browser-extension/` (NOT `extension/`).
- [2026-08-17] CONTENT ENGINE v2.0.0 FULLY IMPLEMENTED (App Tracker): backend + all 5 UI phases shipped. Backend: rubric.ts v2.0.0 (14 criteria, 10 non-negotiable, auto-reject rule), scoringSchemes.ts (3 schemes A/B/C, weights summing to 1.00), prompts.ts (contentEngineSystem + 17 prompts), index.ts (5 new tables, 13+ new IPC handlers, logEvent helper, buildScriptInput composing framework+lessons+reflection, lessonConfirm auto-promotion ≥0.8, process:gallery). UI: ScriptProofCard (per-bullet retention proof with criteria chips, mechanism, evidence quotes, score bars, accept/reject/regenerate), EpisodeScoreSummary (weighted breakdown per criterion), ReflectionPanel (human intuition layer with AI analysis: characteristics, intuitions, contradictions, format_fit), ProcessTimelineView (chronological event timeline with 14 color-coded event types), ProcessGalleryView (growth gallery grid with trend summary), AnalyticsImportModal (3-step paste→AI parse→confirm), CalibrationView (accuracy ring + per-criterion bars + recommendations), ProcessSummaryCard (AI narrative), AnalyticsView updated with dual-layer (objective + human reflection) + import button + calibration section. ContentEngineWorkspace now has 8 tabs including Process. All build gates pass: vite 1m21s, preload 108KB, main 1374KB, tsc zero new errors.
- [2026-08-17] LUCIDE ALIAS IMPORT CRASH RECURRED (App Tracker): `BarChart3` (canonical export `ChartColumn`) was used in src/features/content-engine/components/EpisodesView.tsx:14 TABS without being imported → runtime ReferenceError "BarChart3 is not defined" when Episodes view renders (vite build passes — rollup treats the bare identifier as a global; tsc -p tsconfig.app.json would flag it but was reported clean). Same bug class as the 07-30 TerminalPage BarChart3 crash. After any lucide-import edit, grep the built bundle for the usage site and confirm the canonical name (e.g. `Icon: ChartColumn`) appears — a missing import only crashes at RUNTIME, never at build.
- [2026-08-17] AI CONTEXT VIEWER v2.0 + YOU/GEMINI ADAPTERS (App Tracker): AiContextPanel.tsx (27.4KB, v2.0 console stamp) now has: collapsible 5-stage "How it works" pipeline strip (Globe→Radio→Server→MonitorSmartphone→Brain) with live values; 30-day cyan bar timeline fed by `ai-context:stats`.capturesByDay (backend main.ts:7661 groups by UTC `date()` — renderer must pad missing days to 30 locally); topics chips from `ai-context:topics` (context_entities ≤50) with EXACT empty copy "Topics will appear after Context Brain extraction runs."; Brain tab now shows FACTS (predicate→object_literal + confidence %) between Entities and Signals; live pulse via `onAiContextCaptured` (preload returns VOID — no unsubscribe; guard duplicate ipcRenderer.on subscriptions with a ref); exact spec empty state; skeleton shimmer; armed row-delete (2.5s); Escape closes expanded capture. Extension: `extractMessagesFromYou` (ai-context-content.js:128, paths data.messages[]→data.thread.messages[]→data.response.messages[]; role order role→author→sender) + `extractMessagesFromGemini` (:154, messages/thread + deep-scan, try/catch→[]) + dispatcher cases :217-222, default Claude fallback unchanged. Spec-layout refactor into src/features/ai-context/ NOT done — Subagent B returned state=completed with EMPTY task_result and did nothing; treat empty subagent results as red flags and verify filesystem/build yourself, then re-dispatch.
- [2026-08-17] CONTENT ENGINE SYSTEM PROMPT PACKAGE (App Tracker): the "construct the WHOLE system" prompt lives at `agent/docs/generate-prompt-docs/content-engine-system-17082026/` — CONTEXT_BUNDLE.md (self-contained: raw request verbatim, full verbatim backend rubric.ts/prompts.ts/responseParser.ts/index.ts, renderer types d.ts:571-705, preload bridge 258-292, UI layer + design tokens §8, 6 known gaps §9, 33-channel IPC inventory §10) + PROMPT.md (Lead Designer AND Engineer mandate → RESULT.md; Engineering = 4 pipelines: per-bullet evidence enforcement, dedicated system prompt, prompt/AI-driven analytics ingestion, learning loop auto-promotion; rubric-version-agnostic mandated since content-retention-research-17082026 RESULT.md will bump RETENTION_RUBRIC; MCP inventory w/ real component names + 7 design skills + anti-slop checklist; no new deps; threshold 0.6 binding). This is the answer to the user's "WHERE'S THE PROMPT FOR CONSTRUCTING THE SYSTEM AND EVERYTHING" — point here, never re-search. When Architect RESULT.md lands → generate-prompt post-result workflow (Phase 1 recursive coverage check, then implement).
- [2026-08-17] CONTENT ENGINE BACKEND + UI SHIPPED (App Tracker): backend = `src/services/contentEngine/` (rubric.ts RETENTION_RUBRIC v1.0.0 — 7 criteria, threshold 0.6, nicheRule; prompts.ts 10 JSON-only templates; responseParser.ts extractJson/parseJsonLoose/validateShape/parseAiJson retry≤2; index.ts `registerContentEngineHandlers(db, aiCall)` creating content_ideas/content_episodes/themes/content_frameworks/content_videos/content_lessons + 33 IPC channels). Registered from main.ts ~L3891 AFTER the Learn block inside initializeStorage (same provider-closure pattern: buildChain(pState,'contentEngine') + runWithFallback); 'contentEngine' added to router.ts:34 feature union. UI = `src/features/content-engine/` 7 views mounted via mode toggle in OverlayStudioPage.tsx header ('studio'|'engine'); FeatureStudioPage export must stay. Every script frame carries `retention:{criteria,mechanism,evidence,score}` (<0.6 REJECTED; validate-script-evidence rewrites evidence via AI); gates = scroll_stop/hard_cut/asset_ready; learning loop videos→lessons→frameworks→future prompts. Preload bridge object `contentEngine` (33 methods) + deskflow-api.d.ts types (ContentEngineApi + entity interfaces). Full build = `node scripts/build.mjs` (5min, compiles services/contentEngine per-file — rebuild-main.mjs does NOT).
- [2026-08-17] AI CONTEXT CAPTURE — BROWSER EXTENSION → DESKTOP APP PIPELINE (App Tracker): content scripts (`ai-context-content.js`) run in MAIN world on ChatGPT/Claude/Perplexity/You/Gemini pages, intercept `window.fetch` to capture conversation data from API responses, plus DOM observer fallback. relay via `focusOverlay.js` → background.js → `POST http://localhost:54321/ai-context` → `ai_context_captures` table (provider, messages JSON, url, title, source, timestamp, dedup_key). IPC: `ai-context:list/stats/delete/clear/get-brain-links/topics` + preload bridges `aiContextList/Stats/Delete/Clear/GetBrainLinks/Topics`. Episode writer `writeAiContextEpisode` in episodeWriters.ts feeds Context Brain. Manifest v1.2.0 added `scripting` permission + content script entries for 6 AI domains. `world: "MAIN"` required for fetch interception (page context access). AI Context Viewer panel in AiPage with provider filters, conversation cards, message bubbles, brain integration display (episodes/entities/signals), stats dashboard. Deduplication via captureKey in content script + background + server-side dedup_key column.
- [2026-08-17] SKILL ROUTER SYNC (user-mandated, do not regress): the Router (agent/skills/skill-router/SKILL.md) is a LIVING document — every new skill added to agent/skills/ MUST be added to the Router IN THE SAME CYCLE (Decision Tree category, scenario table, load order, version bump); if the Router misses a skill you know exists on disk, update it IMMEDIATELY before loading it. A stale Router = "why do you never use the skills" rage. Also: root AGENTS.md §1c is the binding Skill Router mandate (edit THAT file for router rules); agent/AGENTS.md is a separate AUTO-GENERATED Tracker Mind file.
- [2026-08-17] TOP NAV TOTAL TIME = APPS ONLY (App Tracker, USER-MANDATED): the tracking browser (e.g. Comet) is already logged as an app row (is_browser_tracking=0) with substantial duration; website rows (is_browser_tracking=1) are a SUBSET of browser time. Adding both = double-counting. Fix: `getTotalTime` and `focusAndTotalTime.total` now filter `!l.is_browser_tracking`. Button tooltips updated to "Apps only". Focus time still includes websites mapped via WEBSITE_CATEGORY_MAP (unchanged — user only complained about total). Future: if focus mode also double-counts, apply same filter there.
- [2026-08-17] MANUALASSIGNMODAL 'ALREADY FILLED' SECTION (App Tracker): added explicit "Already filled today" chips below the 24h strip showing tracked spans (time + app name) and manual assignments (time + app + duration). Backend `manual-assign:day-context` now returns `app` field for tracked entries (SQL: `SELECT timestamp, duration_ms, app FROM logs`). `TimeInterval` interface in `manualTime.ts` gained optional `app?: string | null`. This fixes the user complaint "WHY IS THE FILL IN MANUAL UNABLE TO HAVE THE FEATURE WHERE IT SHOWS THE TIME THAT IS ALREADY FILLED".
- [2026-08-17] MODAL BACKDROP CLICK = ACCIDENTAL CLOSE (App Tracker, USER-MANDATED): NEVER put `onClick={close}` on a modal backdrop div. User rage: "ONLY LIKE THE X BUTTON TO CLOSE IT AND NOT RELY ON CLICKING OTHER THINGS TO CLOSE BECAUSE A LOT OF THE TIMES I ACCIDENTLY CLOSE ON ACCIDENT". All modals must ONLY close via explicit X button or explicit action button — backdrop click, Escape key, and auto-close after action are ALL forbidden unless the user explicitly asks for them. SleepDetectionModal fixed: removed `onClick={handleDone}` from both backdrop divs (sleep step + gaps step) and removed `onClick={e => e.stopPropagation()}` from inner divs (no longer needed without backdrop handler).
- [2026-08-16] RD jasonwebb repo is REFERENCE-ONLY: the user explicitly said the agent does NOT have to rely on https://github.com/jasonwebb/reaction-diffusion-playground — the existing LivingSubstrate implementation (src/components/life-river/LivingSubstrate.tsx + src/shaders/rd-*.glsl) is OURS and is evolved (full-bleed river background), never replaced wholesale from the repo.
- [2026-08-16] SLEEP BELONGING DATE RULE (App Tracker, USER-MANDATED — SUPERSEDES the 08-11 "+1 day for 12AM-6AM" rule): a sleep belongs to the PREVIOUS calendar day's evening when local bedtime hour < 12 (past midnight), e.g. bedtime Aug 12 3:03 AM → "Aug 11's sleep". The next-day date applies ONLY to the bedtime/wake-up LABELS (raw timestamps, e.g. 🛏️ Aug 12 3:03 AM → 🌅 Aug 12 8:39 AM), never to the belonging date, and NEVER to the stored started_at/ended_at. Enforced at: getSleepGroupDate (main.ts ~22417, hour<12 → −1 day), was_shifted (~22437, <12), get-sleep-for-date window ([date 12:00, date+1 12:00) on started_at OR raw started_at within the calendar day — so searching the raw bedtime date also finds it), ExternalPage load-effect resync (search box snaps to the belonging date), buildTimestampForSleepDay (hour<12 → raw next day), Past Sleep modal banner ("Sleep of <belonging date>" heading + raw label dates). Editing an existing sleep with unchanged times MUST reproduce identical timestamps — any future sleep-date change must keep this invariant or the user will rage again.
- [2026-08-16] DETECT-USAGE-GAPS RENDERER TYPING (App Tracker): `detectUsageGaps` is NOT declared in `src/types/deskflow-api.d.ts` DeskflowAPI — every renderer call site casts `(window as any).deskflowAPI?.detectUsageGaps?.({ period, minGapMinutes })` (App.tsx:891, GapsListModal, GapFillDrawer, ExternalPage, DashboardPage). IPC: main.ts ~20972, periods today/week/month/else-all, minGapMinutes default 5, coverage = logs + external_sessions + manual_time_assignments. Any new gaps UI must use this IPC (never renderer detectGaps) and cast the API.
- [2026-08-16] MANUAL TIME SCATTER PRECISION (App Tracker): real tracking log timestamps are SECOND-aligned (e.g. 10:23:17), never whole-minute. Any time math that computes whole-minute boundaries from real timestamps (scatterChunks maxStart, freeSpans deltas) MUST floor, never `Math.round` — round() overshoots the slot by up to ~1 min and the backend overlap check rejects the chunk (intermittent failures that vanish when occupied intervals are hand-built whole-minute, as the first harness did). Same trap class as the sleep "ORDER BY DESC LIMIT 1" lesson: the harness must use REAL-shaped data (second-aligned, overlapping) or the bug hides.
- [2026-08-16] SLEEP ADJACENT GAP FILL (App Tracker): `detectAdjacentSleepGaps` (main.ts ~21327) merges logs + external_sessions over a 48h lookback, finds the merged gap overlapping the sleep window, returns pre/post portions (≥5min) as `adjacentGaps` in the `check-sleep-detection` IPC response (contract: `{start,end,durationSeconds,relation:'before'|'after'}`). The sleep window itself is excluded by design (the sleep session covers it; past sleep sessions merge as covered intervals). SleepDetectionModal has a 2-step flow (early-return `step==='gaps'` view, props adjacentGaps/step/onOpenGapFill/onDone); GapFillModal gained an optional `zClass` prop (default z-50) — App.tsx renders a second instance at z-[10000] and fills via fillGapWithSegments + addExternalTime. Any future sleep-detection work must keep `adjacentGaps` in the response shape or the gaps step silently renders empty.
- [2026-08-15] DASHBOARD CARD GLITCH/BRIGHTNESS ROOT CAUSES (App Tracker): (1) "cards too bright / text unreadable" on Reminders(DeadlinesCard), Focus Sessions(LongestFocusCard), Deep Focus(QuickFocusCard), Goals(GoalsCard) = BorderBeam `absolute inset-0` renders as a translucent COLORED WASH over the WHOLE card because `mask-composite: exclude` fails in this Electron/Chromium build — remove BorderBeam from any content card; use the thin top h-px gradient instead. (2) "cards glitch when adding an item" = `AnimatePresence mode="popLayout"` + per-item `layout` prop + `staggerChildren` containerVariants all fight on list mutation → remove popLayout (plain AnimatePresence), remove `layout` props, drop containerVariants/stagger and use a plain div. (3) Non-memoized `calculateInsights(goals, deadlines, suggestions)` in useDashboardData.ts:288 creates a new object every render → every GoalsCard child re-animates on ANY parent render → wrap in useMemo. If a dashboard "fix" appears reverted, the other agent session may have restored the file — re-apply, don't assume it persisted.
- [2026-08-14] ALWAYS USE EXISTING RESULT.md + code.tsx FIRST (CRITICAL): when a feature has `agent/docs/generate-prompt-docs/<name>/RESULT.md` and `code.tsx`, those ARE the implementation. Read them FIRST. Do NOT implement from scratch. The code.tsx IS the spec — copy it, adapt exports/imports, then add missing pieces. Writing from scratch when a complete implementation exists is a critical failure that wastes massive time and frustrates the user. The user provided the code.tsx for CodeArchitectureMap and I kept rewriting from scratch for 5+ cycles instead of using it.
- [2026-08-14] POLLING TIMER DEATH DIAGNOSIS (CRITICAL LESSON): when user reports "tracking stopped working", the FIRST thing to check is the DB directly — count entries by day, split app vs website (is_browser_tracking). If website entries exist but desktop app entries are ZERO, the pollForeground setInterval died. Do NOT waste time searching for code bugs — the timer is a runtime issue (Windows sleep/hibernation can kill Node.js setInterval). Fix: add watchdog (check every 30s if lastPollTime is stale), Dashboard restarts tracking on mount, crash recovery. NEVER chase code bugs when the DB tells you the answer in 2 seconds.
- [2026-08-14] TRACKING SYSTEM RESILIENCE (BUILD MARKER v5): added 4 crash-recovery mechanisms: (1) process.on('uncaughtException'/'unhandledRejection') logs instead of dying, (2) render-process-gone auto-reloads renderer, (3) requestSingleInstanceLock prevents duplicate processes, (4) tracking watchdog checks every 30s if pollForeground interval is alive, restarts if stale >30s. Also added restart-tracking IPC + Dashboard page calls restartTracking() on mount as belt-and-suspenders. Root cause: setInterval(pollForeground) dies silently after Windows sleep/hibernation — old code had zero recovery.
- [2026-08-11] LIFE RIVER OVERHAUL IMPLEMENTED: back-and-forth collaboration completed with Specialist AI (2 rounds). RESULT.md produced. Implemented: (1) DB migration `status TEXT DEFAULT 'complete'` on life_phases, (2) lens state lifted from CoreSample local → LifePage and propagated to PhaseCard (conditional opacity on memories/gold sections), (3) VoiceInputWrapper added to 5 textareas in PhaseFormDialog (story, feelings, lessons, impact, reflection), (4) draft status wired via saveNow('draft') + saveNow('complete'), (5) ring clicks open PhaseFormDialog via editingPhase state, (6) PhaseCard gets dashed border + opacity-60 for draft status. Deferred: organic ring SVG (Step 4) and backend context aggregation (Step 8). VoiceInputWrapper pattern confirmed: cloneElement wraps child, adds ref + pr-10, dispatches native input event — works seamlessly with useReducer dispatch. LensId type is exported from RingCanvas.tsx. Build verified: vite OK, preload OK, main OK.
- [2026-08-10] SUBSCRIPTION DESCRIPTION MISMATCH (CRITICAL): all 4 subscription payment writers (create, generate-due, retry, record) INSERT `description = sub.name` + `note = subDesc` (e.g. description='SPOTIFY', note='Subscription: SPOTIFY (monthly)'), but ALL 4 dedup/existence queries searched `WHERE description = subDesc` → NEVER matched → duplicates on every page load + failed_dates never cleared → status stuck 'failed' → Retry buttons forever. Fixed: all 4 queries now use `WHERE (description = ? OR note = ?)` with subDesc. Pattern to watch: any new subscription query that checks existence MUST use `(description = ? OR note = ?)` to match both formats.
- [2026-08-10] FT REPAYMENT TAGS MISMATCH: main writes `metadata.repayment_for` on repayment income txn + `metadata.ft_repaid = true` on original, but renderer's `getRepaymentStatus` (receivables.ts) matches on `tags` column (`ft_repaid:{id}` comma-separated) → repayments never detected. Fixed: added `UPDATE finance_transactions SET tags = 'ft_repaid:{originalTxId}'` after repayment INSERT. Any future FT repayment handler MUST also write to the `tags` column.
- [2026-08-10] FT PERSON BALANCE: repayment handler (`finance:ft-person-record-repayment`) did NOT update `finance_ft_persons.balance` (TopUp/Deduct did). Fixed: added `UPDATE finance_ft_persons SET balance = balance + amount` after repayment INSERT. Also fixed sync-balances formula: was `topups - repayments - deductions` (wrong), corrected to `topups + repayments - deductions`. Simplified topup query to `on_behalf_of = 0` only (removed description LIKE conditions that double-counted with repayments). User can click Sync button in People tab to fix stale balances.
- [2026-08-10] TUI prompt-insertion (RESULT.md tui-prompt-insertion-10082026, implemented): agent readiness is now the IDLE-SETTLE heuristic `isTuiSettled(st)` — ≥150B buffer + last line not shell + `Date.now()-st.lastOutputAt >= 500ms` (lastOutputAt reset on every PTY chunk) OR `detectAgentPrompt` regex; the handshake-based isAgentReady path and the 5s blind force-ready timer are GONE (startAgentTimeout now 15s hard-only). `agent:send` sets a 2.5s verifyTimeout; any new PTY data while busy = write confirmed (`[AGENT-VERIFY]` + `agent:write-verified` broadcast, phase→ready); timeout = retry `\r`+payload once, then `[AGENT-VERIFY] Retry failed` → phase=error + `agent:write-failed`. Renderer no longer sends dummy `\r` at launch (300ms settle only). PASS markers: `[AGENT-SETTLE]`, `[AGENT-VERIFY] Write confirmed` in main console. `agentSend` return type lives in src/types/deskflow-api.d.ts (was previously undeclared — renderer used it via any).
- [2026-08-10] NewSessionDialog.tsx MUST stay a NAMED export (`export function NewSessionDialog` + `export interface SessionConfig`) — TerminalPage.tsx:10 imports `{ NewSessionDialog, type SessionConfig }`; converting to a default export fails vite build with "NewSessionDialog is not exported by". VoiceInputWrapper has NO render-prop/onTranscript API — it cloneElements a single child (injects ref + pr-10). Large file writes: the write tool truncates around ~2.7KB — write big components in chunks (write + marker edit) and verify final size with Get-Item before building.
- [2026-08-10] Multi-gap fill (External gaps list → GapFillModal): GapsListModal multi-select (checkbox per row + footer bar) opens the fill editor with `multiGaps`; the editor composes against reference = `multiGaps[0]` and `scaleSegmentsToGap` (src/lib/external/gaps.ts) scales each segment proportionally per gap — drops null-activity segments, floors ≥1 min, remainder to the LAST segment; submit runs sequentially ("Filling X/N…") and on partial failure keeps the modal open with `failedGaps` so RETRY targets only the failed gaps (never double-fills saved ones). Single-gap flow (`gap` prop) unchanged.
- [2026-08-09] opencode session-capture (main.ts capture-opencode-session-id): opencode.db `session.time_created` is INTEGER epoch-millis (must compare NUMERICALLY vs `sinceTimestamp`, never `String(time) >= ISO-string` which is ALWAYS false); session rows are created LAZILY (after first message, not at spawn) so a capture seconds after spawn legitimately finds nothing — a bare-directory fallback must NOT then grab the most-recent session (wrong-session bug). Untimestamped captures should default to a ~15min recency window; cross-dir fallback only with an explicit bound. Call sites must pass `cwd, Date.now()-10000`.
- [2026-08-09] MERMAID SANKEY GRAMMAR (CORRECTS the 08-08 rule below): mermaid 11.16 sankey-beta is CSV (RFC 4180) — header `sankey-beta`, then one `source,target,value` row per link. `A --> B : 10` NEVER parses ("Expecting 'COMMA', got 'NEWLINE'" — reproduced for every quoting variant in node). Fields containing `,` or `"` are double-quoted (`""` escape). Headless verify signature: `DOMPurify.addHook is not a function` = grammar ACCEPTED; `Parse error on line 2` = rejected.
- [2026-08-09] Renderer CSP is injected by Electron at main.ts:19414 (onHeadersReceived) — index.html has NO CSP tag. script-src was `'self' 'unsafe-inline'` and lacked `'unsafe-eval'`, which vega-lite needs (compiles expressions via new Function) → chart blocks threw CSP violations at runtime. `'unsafe-eval'` is now included (local-only app).
- [2026-08-09] lightweight-charts in this repo is v5.2.0: `addLineSeries/addAreaSeries/addHistogramSeries/addCandlestickSeries` are REMOVED — series must be created via `chart.addSeries(lwc.LineSeries/AreaSeries/HistogramSeries/CandlestickSeries, opts)` (named exports verified). Module is ESM-only (system node needs dynamic import).
- [2026-08-09] learn_nodes re-import: `repo.insertNode` was a plain INSERT → importing a CHANGED lesson threw `UNIQUE constraint failed` and rolled back the whole import transaction. Now `ON CONFLICT(id) DO UPDATE`. NEVER use INSERT OR REPLACE — learn_progress has FK ON DELETE CASCADE and would be wiped.
- [2026-08-09] DB INSPECTION while the app runs: byte-scan %APPDATA%\RHEO\deskflow-data.db (122 MB) read-only via `[System.IO.File]::Open($p, 'Open', 'Read', 'ReadWrite')` + StreamReader works (app holds the file, no sqlite3 CLI exists, better-sqlite3 won't load in system node). Recent writes may live in the `-wal` file — scan it too when data looks missing.
- [2026-08-09] LOCALSTORAGE TRAP (App Tracker): the production HTTP server used server.listen(0) = RANDOM port per launch; Chromium scopes localStorage by origin = scheme+host+PORT, so every relaunch got a fresh empty localStorage and ALL localStorage-backed settings (lyceum.shortcutsEnabled, df-sidebar-order, resume-builder-storage, gap banner) silently reset. Fixed: fixed port 38123 with EADDRINUSE fallback (main.ts startProdServer ~L4750). If a setting 'doesn't save across restarts', first check the app is on a STABLE origin (localhost:38123, not localhost:5xxxx).
- [2026-08-09] The App Tracker app's REAL userData dir is %APPDATA%\RHEO (deskflow-data.db there, appUserModelId com.rheo.app) - %APPDATA%\DeskFlow holds a STALE copy (8/5). ALWAYS query the RHEO one. Life phase data gotcha: any year-scale viz (RiverMap) needs a min-span anchor - a single ongoing phase started this year gives minYear==maxYear==2026, span=1, and everything collapses to the left edge (x=5) unless the math pads +/-2yr and uses fractional months.
- [2026-08-09] App-sidebar config persistence map (App.tsx): page ORDER = localStorage `df-sidebar-order` (JSON array of paths; effect-driven persist on `sidebarOrder` change, skip-first-render ref; reset clears key); collapse "orientation" = `df-sidebar-collapsed` ('true' string, try/catch init at L423 + toggle L427). Terminal WORKSPACE sidebar is a SEPARATE gap: `handleSaveWorkspace` never sends `sidebarWidth` (main.ts writes `|| 400` default) and `sidebarOpen` (collapsed) isn't persisted at all — if user reports "sidebar config not saved" for the terminal page, that's the fix location.
- [2026-08-09] Life Phases PeriodContext split (RESULT (10).md): main's `lifePhase:getPeriodContext` aggregates ONLY SQLite (goals/focus/external/logs; sleep + is_browser_tracking rows excluded); `memories[]` and `covenantCompletionRate` come back empty/null from main and are filled RENDERER-side (ConnectionDataStrip/CoreSample merge useMemories + useCovenant) because memories are IndexedDB blob URLs and covenant is localStorage-only. `memoryUrl()` is therefore a list-lookup (memoryUrl(memories, id)), not a sync id-to-url function.
- [2026-08-09] Port 9222 on this machine is Lenovo Vantage's msedgewebview2, NOT the app - never probe it expecting DeskFlow. Probe LAUNCH of this Electron app fails ("Debug endpoint never came up") and leaves an orphan electron.exe (kill only the PID you spawned, by StartTime). Runtime verification requires the app ALREADY running with --remote-debugging-port (attach:true), else mark NOT LAUNCHED.
- [2026-08-08] Lyceum hierarchy VISUALIZATION is `src/components/learn/HierarchyGuide.tsx` (tree lines + per-level colored cards, `showHeader` prop) wired into LearnHome (replaces the text-badge strip) and OnboardingPanel step 1 — if user asks for "the hierarchy visualization", that component is the answer; never regress it back to a text list.
- [2026-08-08] Lyceum lesson-block runtime rules (blocks in `src/components/learn/blocks/`): (1) mermaid 11.16 hangs if `initialize()` runs before EVERY render — initialize EXACTLY ONCE per session (module singleton `blocks/mermaidLoader.ts`) + 15s render timeout; (2) sankey-beta edges are CSV `source,target,value` rows — see the 08-09 top entry (this `A --> B : 10` claim was WRONG, superseded); (3) Tabulator v6 themes are CSS-only — the `theme: 'dark'` option is silently ignored; without importing `tabulator.min.css` + `tabulator_site_dark.min.css` tables render as unstyled stacked text; (4) learn parser stores FULL vega-lite specs, so finchart/chart data lives at `spec.data.values` (not top-level); (5) all heavy dynamic imports must go through `isDynamicImportFailure → autoHealDynamicImport()` (ErrorBoundary.tsx) or stale chunks spin forever — a `.catch` that swallows the error also kills the global self-heal.
- [2026-08-08] Lyceum markdown directive syntax: `:::` open lines may OMIT the space (`:::illustration` == `::: illustration`) — open regex `^:{3,}(?:\s+)?(\w+)`, bare `:::` closes, 4+ colons always close. dist-electron/services/learn/*.js are compiled PER-FILE (rebuild-main.mjs does NOT rebuild them) — recompile index.ts + validator/validate.ts with esbuild after touching src/services/learn/*.ts or fixes "appear fixed in source but fail at runtime".
- [2026-08-08] dist-electron/services/learn/*.js are compiled PER-FILE, not into main.cjs: `scripts/rebuild-main.mjs` only rebuilds main.cjs/preload. After editing src/services/learn/*.ts, recompile per-file with esbuild EXACTLY like build.mjs Step 3 — NO --bundle: `npx esbuild "<src>.ts" --outfile="<dist>.js" --format=cjs --platform=node --target=node22`. NEVER use --bundle for these files: bundle inlines db/repo.js's `runMigration`, whose `path.join(__dirname, 'migrations')` then resolves against the bundle's dir (dist-electron/services/learn) instead of .../learn/db → readdirSync ENOENT → registerLearnHandlers throws → EVERY learn:* IPC reports "No handler registered" at runtime while source looks fine. validate.ts has the same __dirname dependency (../../../schemas/ldoc-1.0.json). A stale service file in dist-electron makes "fixed" source still fail at runtime (the L5 prereq bug appeared fixed in source but the app ran old compiled validation for a whole round).
- [2026-08-08] Lyceum Learn naming cleanup (PENDING — user requested): "Part" should be renamed to "Topic" in UI display (DB field stays `part`). "Chapter" (user-created lesson grouping) should be renamed to "Group" to avoid confusion with lesson nodes (which are the actual "chapters" inside a lesson). L0-L5 mastery levels now have readable labels via `MASTERY_LABELS` and `MASTERY_SHORT` in `src/shared/learn/types.ts` — wire these into all UI spots showing raw L-codes. The onboarding panel title "Mastery Levels (L0–L5)" should be renamed. The hierarchy confusion: Part=Topic (predefined area), Chapter=Group (user-created), Node=sub-chapter (inside lesson), L-level=mastery axis (separate from hierarchy). Also: `learn:validate` IPC handler was NOT passing `publishedIds` to `validateFull()` — fixed to query `SELECT id FROM learn_nodes` and pass the set. `checkVisual` was missing `code`, `table`, `layer`, `illustration` etc. from the visual types list — fixed to match parser's `VISUAL_TYPES` exactly. Prereq error messages now include sample valid IDs for debugging.
- [2026-08-07] IDE analytics loader: there is NO function named `loadWorkspaceAnalytics` in IDEProjectsPage.tsx — the real loader is `fetchAnalytics` (useCallback, IDEProjectsPage.tsx:612–671) feeding `setWorkspaceAnalytics` (state line 363; `analyticsCacheRef` line 366; `analyticsReqIdRef` exists — reuse, don't re-declare). Local capture server: `startBrowserTrackingServer()` at main.ts:17968 (browserServerPort = 54321, listen at 18185, bound 127.0.0.1) — a new `/code-activity` route belongs as a sibling `else if` AFTER the `/browser-data` branch, NOT gated on `isBrowserTrackingEnabled`; main.ts:4695 is an OLD separate createServer, don't confuse them. projects/ides/tools/ai_usage CREATE TABLEs at main.ts:2000–2108; `projects.path` is TEXT UNIQUE (join key for extension fsPath); `project_line_stats` already exists (per-file lines, AUTOINCREMENT id). ai_usage id is TEXT PK.
- [2026-08-05] Canvas group display invariant (user requirement): grouping cards MUST preserve the actual card content/rendering — it only combines cards into a resized group container. NEVER replace card bodies with previews. GroupCard.tsx previously rendered `extractCardPreview` mini-chips (≤120-char text) while real cards were filtered off the canvas — the user treated this as a display bug. Fix: GroupCard gets a `renderChild={(c) => renderCardContent(c, ctx)}` prop from CanvasCard's group case (reuses the exact standalone renderers) and absolutely positions each child at `(child.pos - group.pos)` minus the 10/30px creation margins, at real sizes (w/h × 40), with a per-card X overlay instead of drag-out. Children render from the `data.childCards` snapshot (positions fixed at creation; arrangeGroup moves live cards but NOT the snapshot — pre-existing); function props (connectors/automation handlers) don't survive the JSON snapshot → render inert inside groups.
- [2026-08-05] Canvas scatter root cause (AiPage): dedup refs (processedMsgIds/msgCardIds/lastCardId) are component refs that reset on remount, while chat.messages is a module-level zustand store that survives navigation → the bridge effect re-spawns EVERY historical message as new cards at heuristic positions (getCardPosition bases) and the save-on-change effect then persists the scattered layout. FIX INVARIANT: any card spawned from a chat message MUST carry `data.msgId`, and effects that spawn cards MUST match existing cards by `data.msgId` (or by type like the connectors/planner effects) before adding — "check canvas for existing card instead of relying on ref (ref resets on mode switch)". Never key spawn-dedup purely on refs.
- [2026-08-04] Resume Builder persistence lives in TWO layers that must stay in sync: (1) zustand persist localStorage key `resume-builder-storage` — `partialize` MUST include `builderProgress` (it only persisted previewMode/previewZoom, so ALL answers vanished on restart); (2) backend file `%APPDATA%/DeskFlow/resume-data.json` via `resume:saveProgress`/`resume:loadProgress` IPC (handlers + preload existed forever but NOTHING called them; main.ts `resume:submitAnswer` does NOT persist). Fix = expand partialize + debounced 400ms auto-save subscription on builderProgress change + restore-on-mount in ResumeBuilderPage (`initProgress`: resolves next question via `resume:nextQuestion` from the last answered question in the current phase) + Save button onClick. GOTCHA: JSON round-trips (localStorage AND disk) convert `phaseStatus`'s numeric keys to STRING keys → normalize with `Number(k)` in the store `merge` AND the page restore path, else `phaseStatus[1]` lookups silently return 'locked'.
- [2026-08-04] trackerAppMode ('track'|'show-other'|'pause', SettingsPage.tsx:73) semantics in main.ts `pollForeground()`: for the tracker app (DeskFlow/Electron/RHEO) in show-other/pause mode you must NOT set `currentApp = null` — that made the dashboard flip to "Waiting for app" / jump to the last non-browser app (VS Code) while the user was in the tracker app. Correct: keep `currentApp`, reset `sessionStart = now` (so tracker-app time isn't attributed to the prior app's log), send `isTrackerApp: true` in `foreground-changed`; the renderer (DashboardPage.tsx foreground listener + initial fetch) must check `isTrackerApp && tam !== 'track'` BEFORE clearing isInBrowser/currentWebsite and just return (pause sets the paused flag). `lnb` (lastNonBrowserApp) is NOT a valid "what to show" fallback in this branch — it's the previous real app that just got buried.
- [2026-08-04] The browser-extension manifest had NO `action` key, so clicking the Comet toolbar icon showed nothing — MV3 popups require `action.default_popup`. Added popup.html/popup.js reading chrome.storage.local `deskflow_*` keys + /health poll. Manifest 1.1.1 → 1.1.2.
- [2026-08-04] RHEO has NO `requestSingleInstanceLock` (grep main.ts = none), but `probe_open` LAUNCH mode still never brings up the debug endpoint on a second instance (tried 2x) and silently leaves an orphan `electron.exe` behind — kill only the PID you spawned (match by StartTime > launch). To runtime-verify RHEO with Probe, the user's app must be started WITH `--remote-debugging-port` (then `attach:true`), else mark NOT LAUNCHED. Never try to launch manually.
- [2026-08-04] BorderBeam (`src/components/ui/border-beam.tsx`) relies on `mask-composite: exclude`, which does NOT work in RHEO's Electron/Chromium build — an `absolute inset-0` BorderBeam renders as a translucent COLORED WASH over the whole card interior instead of a border ring. User perceives it as a bug ("weird yellow shade obstructing content"). Do NOT place large BorderBeam overlays on content cards; if warmth is needed, use WarmCard `ambient` aurora. (ReflectionCard fix in GoldPage.tsx removed the amber BorderBeam; GoldHeader streak badge size-120 + BellBoard size-150 still use BorderBeam in small containers — watch for washes there.)
- [2026-08-04] The RHEO repo has NO test runner configured: `vitest` is NOT in package.json, so `npx vitest run` fails with ERR_MODULE_NOT_FOUND. `node scripts/build.mjs` performs NO test step (steps: renderer vite build → preload esbuild → service shims → main Vite lib). Verification = build output + `tsc -p tsconfig.app.json` (only pre-existing syntax errors in aiAgentService.test.ts, untouched) + eslint on changed files. Don't waste time trying to run tests.
- [2026-08-04] "Failed to fetch dynamically imported module" (stale-hash lazy chunk 404) fix: (1) prod server in main.ts now returns a CLEAN 404 (not SPA-fallback index.html) for missing files under /assets — serving text/html to a module import() caused the error; (2) added `Cache-Control: no-cache, no-store, must-revalidate` to ALL prod-server responses so a reload can never re-serve a stale index.html/chunk; (3) renderer self-heals: `ErrorBoundary.componentDidCatch` + `window.onerror`/`unhandledrejection` detect `/Failed to fetch dynamically imported module|Importing a module script failed/i` and `autoHealDynamicImport()` does ONE `location.reload()` (8s throttle guard) to pick up the freshly built index.html + new chunk names. Since the app uses HashRouter, extensioned missing paths should NEVER get SPA fallback.
- [2026-08-02] Finance Subscriptions is NOT a standalone route anymore — it is a tab inside FinancePage (SubscriptionsTab compact + SubscriptionsPageView full via showSubscriptionsPage). It was view-merged under the Budget & Expenses tab as a sub-TabBar (Bills & Budget / Subscriptions, state budgetSubView in FinancePage.tsx) with NO data migration: finance_subscriptions vs finance_fixed_expenses remain separate tables, and all 23 subscription handlers stay in FinancePage. Any future 'subscriptions page' work targets FinancePage.tsx:1344+ (budget tab sub-view).
- [2026-08-02] Gold/Goals tab: goals feature is now a tab inside LifePage (?tab=gold) not a standalone /goals route. Removed Goals from App.tsx sidebar + route. GoldPage.tsx at src/features/warmth/gold/ with 7 sub-components (GoldHeader, DayRing, WeekBoard, DeadlineRadar, TheVault, BellBoard, DayJournal). Species routing: `isWeeklyish = (g) => !!g.isHabit || g.cadence === 'weekly' || g.period === 'weekly'` — dailies go to Day Ledger, weeklyish go to Week Board. MCP components: NumberTicker, BorderBeam, AnimatedCircularProgressBar. Dynamic import chunks change on every rebuild — user must fully close+relaunch Electron app after rebuild to avoid "Failed to fetch dynamically imported module" errors.
- [2026-08-12] SLEEP DETECTION AFTER HIBERNATION (three bugs fixed): (1) `checkSleepGap` 16-hour hard cap silently skipped detection for hibernation >16h — raised to 24h. (2) `deskflow-sleep-detection.json` with `checked: false` from a dismissed popup blocked ALL future detections forever — now auto-clears stale files older than 2 hours. (3) No logging on `powerMonitor.on('resume')` — added console.log with gap duration for debugging. The `checkResumeSleepGap` handler (line 19777) fires on both `powerMonitor 'resume'` and `'unlock-screen'` events. If the user reports "popup still doesn't appear after hibernation", check console for `[DeskFlow] 💤 powerMonitor resume/unlock` — if absent, the event isn't firing (Electron/Windows issue); if present but popup missing, check for the other guards (sleep hours, existing sleep record).
- [2026-08-11] SLEEP DATE LOGIC (CORRECTED AGAIN): The previous "grouped evening date" logic (bedtime <6AM → subtract 1 day) was WRONG per user requirement. Correct rule: bedtime between 12:00 AM and 6:00 AM → assign to NEXT calendar day (add +1). Example: bedtime Aug 10 2:30 AM → Aug 11 2:30 AM (same day as wake-up Aug 11 1:50 PM). `getSleepGroupDate` now uses `d.setDate(d.getDate() + 1)`. `get-sleep-for-date` query window changed from `[date+06:00, date+1+06:00]` to `[date+00:00, date+1+00:00]` — old window missed bedtime 02:30 because it started at 06:00. Also: sleep date selection persisted to localStorage (`external-sleep-date` key) so it survives modal close/reopen.
- [2026-08-06] Smart Gap Fill Drawer (src/components/GapFillDrawer.tsx, app-wide in App.tsx, mixed app+website+external fill via predict-gap-fill/confirm-gap-fill/get-known-apps IPC) had ONLY ONE entry point: the amber gap banner, whose sole dismiss is "don't show again" (sets showGapBannerSetting=false forever) — clicking it made the drawer unreachable. Entry point restored: ExternalPage.tsx header now has a "Smart Fill" button dispatching `new Event('open-gap-drawer')` (App.tsx:1495 listens). The External-page GapFillModal (src/components/external/) is a SEPARATE external-only fill UI — two gap-fill systems exist, keep them distinct.
- [2026-08-06] BUILD GOTCHA: scripts/build.mjs pre-compiled only src/{services,domains,main} + gameDetection.ts — NEVER src/lib. Any main-process file importing `../lib/*` (ProblemsService/RequestsService import lib/mojibake, customSlashCommands imports lib/uuid, main.ts:3510 requires ./lib/mojibake) crashed at runtime with "Cannot find module '../lib/mojibake'" because dist-electron/lib/ was never emitted. Fix: `const libFiles = findAllTs(resolve(SRC,'lib'))` added to allTsFiles in build.mjs Step 3. If a future "module not found" points into dist-electron/lib, the build script is the culprit — check lib is in the compile list.
- [2026-08-09] Sleep day-lookup INVARIANT (App Tracker): any IPC that resolves "the sleep for date X" must match the LOCAL grouped evening date — window on started_at in `[X 12:00 local, X+1d 12:00 local)`, ASC, first match (same rule as getSleepGroupDate: local bedtime hour < 12 → previous evening). NEVER use SQLite UTC `date(started_at) = ?` OR `date(started_at) = date(?, '+1 day')` with `ORDER BY started_at DESC LIMIT 1` — in UTC+7 every after-midnight bedtime matches BOTH days and DESC returns the NEXT night's sleep (the "popup stuck at 7" bug).
- [2026-08-09] Smart Fill / gap-fill drawer is app-wide: dispatch `new Event('open-gap-drawer')` from ANYWHERE (App.tsx ~1495 listens, renders GapFillDrawer ~3274). Global top bar = App.tsx ~2662-2840 (h-16 glass, PageTitle + LIVE badge left; Focus/Total + period + timeline + time + tracking right). ExternalPage header Smart Fill (908) + now top-bar button both use the event.
- [2026-08-09] DEBUG SIGNATURE: "correct only on the LAST item / all earlier ones show the same wrong value" (e.g. sleep popup "stuck at 7") = `ORDER BY <ts> DESC LIMIT 1` (or un-ordered .get()) with an over-wide predicate matching multiple rows (e.g. `date(started_at)=date(?,'+1 day')` OR-clauses) — it returns the NEXT period's record for every lookup. Fix the predicate window (local-grouped, ASC), not the ordering. Detailed lesson: agent/skills/agent-reflect/logs/2026-08-09_sleep_popup_symptom_decoding.md + problem.md rules 19-21.
- [2026-08-09] PROCESS: user requests must be persisted to FEATURE_TRACKER.md/PROBLEMS.md AT REQUEST TIME (not session end) — the beautiful-charts-for-overview ask was never tracked and the user had to ask "where was my request". Also: "gap/missing data" claims in bug reports must be verified against the real DB before writing a fix (the sleep "2 day gap" was real data absence).


- [2026-08-05] Gold page "The Vault" (GoldPage.tsx) now has FULL long-term goal CRUD: header + button, add/edit form (title/category/priority/deadline/description), two-step delete confirm (3s arm), ProgressRing from `ltg.progress` (backend-computed from progress_seconds/target_seconds, capped 100). The `goals` table HAS a `deadline TEXT` column (main.ts:2778, guarded ALTER) and save-goal/save-goals-batch INSERTs include it (16393/16548); get-longterm-goals maps deadline + progress (16419). OLD MEMORY "AI-created long-term goals store deadlines in description text only / no deadline column" is SUPERSEDED — deadline persists end-to-end now. GoldPage does NOT use useLongTermGoals.ts (own local state + loadLongTerm); LongTermGoal type already has deadline/priority.
- [2026-08-05] Reorderable app sidebar: sidebar items array lives at App.tsx `DEFAULT_SIDEBAR_ITEMS` (module level, was inlined in component); persisted order = localStorage `df-sidebar-order` (JSON array of paths, try/catch, validated against known paths on load). dnd-kit sortable wiring: PointerSensor activationConstraint `{ distance: 5 }` so plain clicks still navigate inside reorder mode; drag handle (GripVertical) is the ONLY activator (listeners on handle button, NOT the item button). Reorder toggle = Pencil/Check button next to collapse button, hidden when sidebar collapsed.


- [2026-08-04] Browser-tracking data-loss root cause: the EXTENSION self-gated on its own `isBrowserFocused` (periodicSync + logPreviousSession returned early), and that flag can be stuck false forever because UA-based brand detection (detectBrowserName) sees Comet's UA as "Chrome" → process names ['chrome','chromium'] never match appName 'comet'. Fix: extension always sends with is_browser_focused as a hint only — the DESKTOP app is the authority (freshForegroundIsBrowser / forceBrowserTracking). Also `handleBrowserData` in main.ts had an UNCONDITIONAL `data.is_browser_focused === false` return that ignored forceBrowserTracking — it must be `&& !(userPreferences?.forceBrowserTracking === true)`. MV3 alarm minimum is 0.5 min (30s) — 0.083 gets silently clamped. Reload extension + full RHEO restart both required after a fix.
- [2026-08-04] Browser identity matching: when sending `/browser-identify`, send the UNION of all BROWSER_PROCESS_NAMES aliases, otherwise a UA misdetect (Comet→Chrome) makes the server clobber `browserProcessNames` and drop the comet/edge entries. `checkBrowserFocus` matching must also use the full alias union set, not just the detected brand's process names.
- [2026-08-02] NEVER claim "fully fixed" without a full-spec audit: every deliverable checked end-to-end (mainâ†’preloadâ†’rendererâ†’UI render) AND runtime-verified in the layer the feature lives in. Build-OK + unit-test-OK = "NOT LAUNCHED", never PASS. Also: every feature MUST get an entry in agent/requests.json + agent/problems.json with checks[] same-cycle; resolve CONTEXT_GAPS.md in-doc when code pivots on them; read agent/skills/agent-reflect/logs/ + problem.md before any task (mandatory per AGENTS.md).
- [2026-08-02] TUI agent interaction (committed a6ae60c): opencode session-ID = db-pid PRIMARY (doesn't print to stdout); claude/gemini/codex = output-parser (Ink TUI header). AgentState phases launchingâ†’readyâ†’busyâ†’attentionâ†’error live at main.ts:11457-11479; parseAgentOutput wired at main.ts:10645; agent:status payload {terminalId, phase, sessionId?, error?} at main.ts:10629; anomaly detection CLI_ANOMALY_PATTERNSâ†’terminal:anomaly main.ts:12428 (preload onTerminalAnomaly preload.ts:442, TerminalPage AlertCircle render); auto-rename update-session-summary main.ts:15544; compaction check-session-compaction main.ts:21471. All runtime-UNVERIFIED.

- [2026-08-02] Sleep detection has THREE independent triggers in main.ts: window focus/blur events, tracking-poll gaps (null `active-win`), and system-idle + `powerMonitor` resume/unlock listeners. Window-focus-only detection NEVER fires when RHEO stays focused while the user sleeps â€” that gap needs `powerMonitor.getSystemIdleTime()` â‰¥ `SLEEP_DETECTION_MIN_GAP_MS` (45min) tracked in `pollForeground()` and `powerMonitor.on('resume'/'unlock-screen')`. Idle/resume callers pass `{ skipActiveGuard: true }` to `checkSleepGap`. `check-sleep-detection` IPC must NOT mark `checked` (only confirm/dismiss do). Gaps with neither endpoint inside sleep hours are skipped by `isWithinSleepHours` by design.
- [2026-08-01] Build emits a HASHED entry (`dist/assets/index.<hash>.js`) â€” `dist/assets/index.js` missing is NORMAL, NOT a black-screen signal. Verify by reading `dist/index.html` for the `<script type="module" src="./assets/index.<hash>.js">` ref, then confirm that exact file exists and is > 10 KB. Also: canvas group membership has TWO layers â€” canonical `state.groups[id]` (label/color/cardIds) and the group card's own `data.childCards` snapshot (what GroupCard renders) â€” reducers MUST keep both in sync (ADD/REMOVE_TO_GROUP, DELETE_GROUP, CREATE_GROUP position snapshots) or UNGROUP restores cards at 0,0.
- [2026-08-01] After ANY renderer source change, the running RHEO app holds the OLD bundle in memory until restarted â€” verify by comparing RHEO process StartTime vs `dist/assets/index.js` LastWriteTime. A user reporting "you didn't fix it" on a UI change is often staring at a stale app. Do not re-debug source; check timestamps first, then have the user fully close + relaunch.
- [2026-08-01] Finance Overview canonical layout: exactly ONE Net Flow card (Deep Dive hero w/ sparkline + MoM) and exactly ONE Receivables/FollowThrough-person card. Quick Stats = Income/Expense/SpendingSplit only. FollowThroughCard is dead (imported-but-unrendered). Removed dead imports: FollowThroughCard, IncomeExpenseCard, LiquidityWaterfall, SubscriptionBurdenRadar, CashFlowRunway, WalletHealthScorecards, TransferCostMatrix, CryptoUnifiedPortfolio.
- [2026-07-31] TUI agent interaction is FAKE: `buildAgentInputPayload` (main.ts:10464) just wraps text in `\x1b[200~...\x1b[201~\r` (Bracketed Paste) and `capture-opencode-session-id` (main.ts:11594) path-matches opencode's SQLite DB. CZ demands REAL TUI interaction + session ID from agent OUTPUT. Task files: `agent/docs/tui-agent-interaction/CONTEXT_BUNDLE.md` + `PROMPT.md` â€” Architect builds it. `agent:send` (main.ts:11505) queues when phase is launching/busy; `detectAgentPrompt` (main.ts:10399) uses regex `/^(?:opencode)?\s*>\s*$/i` on ANSI-stripped buffer â€” cannot distinguish TUI prompt from shell prompt.
- [2026-07-31] Terminal spawn has TWO paths: `spawnTerminal` (TerminalPage.tsx:1172) â†’ IPC `spawn-terminal`, and xterm `onTerminalReady` â†’ `handleTerminalReady` (TerminalWindow.tsx:726). The `terminal:mark-spawned` CustomEvent dispatches BEFORE the `await` to dedupe the double-spawn race (fixes applied to TerminalPage.tsx:4069,1504,2216,2246,2528).
- [2026-07-05] Conductor is a WORKSPACE feature, not a standalone page. It lives in the workspace sidebar (Work group â†’ Swarm subtab). Project selection comes first (from the terminal's project dropdown), then the swarm operates on that project's path. Never build a separate repoRoot picker for it.

## Workspace Infrastructure (2026-07-27)

- **System Prompt is 8-layer:** default â†’ general â†’ agent-specific â†’ project-specific â†’ init content â†’ thought process â†’ auto-context â†’ config directives. All assembled in `initializeTerminal()` in TerminalPage.tsx.
- **Context auto-injection:** `assemble-context` IPC is called during session creation with 2000 token budget. Agents automatically receive problems, requests, and sessions.
- **Workspace overlay z-index:** Must be z-[200] to cover app sidebar (z-[100]). The workspace opens as a `fixed inset-0` overlay inside IDEProjectsPage, NOT as a separate route.
- **Compose/Quick buttons:** Tooltip wrapper from shadcn was swallowing click events. Use plain `<button>` elements for action buttons, not wrapped in Tooltip.
- **Fortress Protocol:** Uses `electron:execute-command` IPC (not spawn-terminal). Returns real stdout/stderr now (was faking with 2s wait).
- **`/sync` table name:** Must use `workspace_problems` not `problems`. The old table name caused runtime errors.
- **File locks are in-memory only:** Lost on app restart despite `file_locks` DB table existing. 60-second TTL.
- **Config toggles are localStorage-only:** Not persisted to backend. Reset on app restart unless saved via workspace state.
- **Full system map:** `agent/docs/workspace-system-map.md` â€” single source of truth for all feature connections.

## UI Revamp Components (2026-07-27)

- **WorkspaceCommandBar:** Clean command bar with exit, project, terminal tabs, compose/quick/save. Replaces hand-rolled header.
- **WorkspaceGroupRail:** Vertical icon rail (44px) with accent-colored indicators. Replaces 6 chunky text buttons.
- **PresetsTab:** Revamped with WorkspaceCard, WorkspaceSection, animated list, proper empty state.
- **ConfigsTab:** Full component with shadcn Switch, Input, Select. Wired into TerminalPage. Live context viewer and saved workspaces sections retained below.
- **Switch component:** Created `src/components/ui/switch.tsx` (was missing). Simple accessible toggle with emerald accent.
- **WorkspaceCard:** Updated glass aesthetic: `bg-[rgba(24,24,27,0.60)]`, top-edge gradient highlight.
- **SubTabBar:** Rewritten with group-aware accent pills.
- **WorkspaceShell:** Cleaner structure with accent trunk line.

## MCP Component Inventory (2026-07-27)

- **shadcn (installed):** accordion, alert, badge, button, card, collapsible, dialog, input, select, separator, skeleton, switch, tabs, toggle, tooltip
- **Magic UI (available):** animated-beam, border-beam, magic-card, number-ticker, particles, shimmer-button, terminal
- **Lucide (installed):** 1500+ icons via lucide-react
- **Source routing:** Standard UI â†’ shadcn, Animated effects â†’ Magic UI, Icons â†’ Lucide, Specific component â†’ 21st.dev

## Design Preset System (2026-07-27)

- **Preset definitions exist:** `src/lib/designPresets.ts` â€” 8 presets (Cyberpunk, Minimal, Glass, Brutalist, Warm, Terminal, Ocean, Neon) with full color/geometry/typography/motion/glass/MCP mappings
- **Prompt generated:** `agent/docs/generate-prompt-docs/workspace-design-preset-system-27072026/PROMPT.md` â€” tasks AI with building DesignPresetManager, presetAdapter, presetPromptInjector
- **Context bundle:** `agent/docs/generate-prompt-docs/workspace-design-preset-system-27072026/CONTEXT_BUNDLE.md` â€” full code context for target AI
- **Design system map:** `agent/docs/workspace-system-map.md` â€” complete featureâ†’IPCâ†’DB connectivity map
- **Gap:** Preset gallery UI, adjustment knobs, presetâ†’prompt injection, presetâ†’component adaptation not yet built
- **Goal:** Users select a preset â†’ tweak knobs â†’ AI follows it consistently in prompts and UI generation

## Dashboard MCP Card Improvements (2026-07-30)

- **MagicCard for interactive cards:** Mouse-following gradient gives visual personality without "AI slop". Use on hero/stat cards (MomentumHero, QuickFocusCard) where hover interaction makes sense.
- **BorderBeam for conditional urgency:** Render BorderBeam only when state is active/urgent (e.g., `activeGoals.length > 0`). Creates visual hierarchy â€” glowing cards draw attention to what matters.
- **BorderBeam color mapping:** Goals=emerald (#10b981â†’#34d399), Deadlines=roseâ†’amber (#f87171â†’#fbbf24), AI=violet (#8b5cf6â†’#a78bfa), Focus=violet (#8b5cf6â†’#6366f1), Momentum=pink (#ec4899â†’#f472b6).
- **Particles behind data charts:** Particles as absolute-positioned background behind Chart.js bars adds depth without interfering with data rendering. Use `pointer-events-none` and low quantity (30).
- **Marquee conflicts with interactivity:** Marquee auto-scroll breaks click targets and edit mode. Skip when card has interactive children (buttons, toggles, edit mode). Only use on purely display elements.
- **MagicCard adapted for Electron:** The installed magic-card.tsx already removed `next-themes` dependency and hardcoded dark mode colors (#18181b background). No additional adaptation needed.
- **NeonGradientCard available but heavy:** NeonGradientCard works but uses CSS pseudo-elements with complex calculations. Prefer MagicCard for mouse-following effects, BorderBeam for edge glow.

## StopwatchTimer Glow Fix (2026-07-30)

- **BorderBeam alone is too subtle:** A thin animated line on the border isn't enough for a "glow" effect. Combine with CSS box-shadow for visible radiance.
- **Box-shadow glow approach:** Use `box-shadow: 0 0 20px 2px rgba(R,G,B,alpha), inset 0 0 30px 1px rgba(R,G,B,alpha)` for always-visible glow. Outer shadow = card radiance, inner shadow = internal glow.
- **Always-on vs conditional:** The glow should ALWAYS be visible (idle state = subtle, active state = brighter). Conditional rendering makes the glow disappear when idle, which looks broken.
- **Tier color mapping for glow:** Convert hex to RGB for box-shadow: productive=#10b981â†’16,185,129; distracting=#ef4444â†’239,68,68; neutral=#3b82f6â†’59,130,246; external=#8b5cf6â†’139,92,246.
- **BorderBeam thickness:** borderWidth=2 is visible; borderWidth=1.5 is too thin. Duration: 6s when active (faster animation), 12s when idle (slower, calmer).

## MagicCard Component Fix (2026-07-30)

- **Original MagicCard hardcoded colors:** The installed magic-card.tsx had hardcoded pink (#ec4899) and purple (#a855f7) in the gradient template, ignoring any props passed.
- **Fix:** Add `gradientFrom` and `gradientTo` props to MagicCard interface and use them in the template literal.
- **Inner overlay:** Changed from `bg-zinc-900/95` to `bg-[rgba(24,24,27,0.80)] backdrop-blur-xl` to match the project's glass pattern.
- **Dashboard layout swap:** QuickFocusCard moved to center position (between GoalsCard and DeadlinesCard) per user request.

## TerminalPage BarChart3 Crash (2026-07-30)

- **BarChart3 missing from import:** TerminalPage.tsx uses `BarChart3` icon at line 3769 in SetupPresets content but the icon was not imported from `lucide-react`. This caused the entire TerminalPage to crash on mount â€” React rendered `undefined` and the ErrorBoundary caught it, making ALL terminal/session/workspace features completely non-functional. No dialog, no text box, no sessions. Fix: add `BarChart3` to the `lucide-react` import block.
- **Session init race condition:** `handleTerminalReady` and `onCreate` both call `spawnTerminal` for the same terminalId. First spawn gets killed when second spawns. This creates a double-spawn scenario where the first PTY's data handler chain can be corrupted. initializeTerminal writes to the (live) second PTY so the agent should still launch.

## Lock Screen Tracking Fix (2026-07-30)

- **Problem:** After hibernation, `active-win` detects `lockapp.exe` (Windows Lock Screen) as the foreground window. Since it wasn't in `TRANSIENT_APPS`, it got tracked as a real app â€” making the timer show "lockapp" instead of the actual app.
- **Fix:** Added `lockapp`, `winlogon`, `credentialui`, `logonui` to `TRANSIENT_APPS` list in main.ts (line 3866).
- **Root cause:** The transient apps filter (`userPreferences.filterTransientApps`) is enabled by default, but the list was incomplete â€” it didn't include Windows lock/login screen processes.
- **Other Windows system processes to watch:** `searchapp`, `searchhost`, `startmenuexperiencehost`, `taskmgr`, `settings`, `systemsettings`. These are already partially covered by existing entries.
- [2026-08-01] Activity sub-tab pages (StatsPage/BrowserActivityPage/ProductivityPage) return bare fragments in embedded mode â€” their own wrapper div MUST carry the spacing (`space-y-6`), since PageShell's classes never apply. With the shared AppBackground behind translucent glass cards, also add a dim backdrop (`absolute inset-0 bg-[#121212]/70` + `relative z-10` content) inside the tab scroll container so particles don't bleed through card gaps. Uniform tab order: hero chart on top â†’ stat cards â†’ profile/sections â†’ detail lists â†’ collapsed debug tools (Live Detection) at bottom.
- [2026-08-01] Finance privacy-mask canonical pattern: `useNumberMask()` + `maskNumber(x)` from `src/lib/useMaskedCurrency`. Mask DISPLAY STRINGS only â€” never the underlying numeric state (BudgetModal keeps numeric `amount`; only the rendered `$` preview is masked). Fully-masked siblings/screens can skip unmasked-sibling masking (BudgetModal line 104). AnimatedAmount already masks internally (SelectionAggregatePanel guard is redundant-but-safe).
- [2026-08-01] AnimatedGradientText (vendored Magic UI) is INERT unless Tailwind v4 has `--animate-gradient` in `@theme` â€” `@keyframes gradient` alone is not enough. Fix lives in index.css: `@theme { --animate-gradient: gradient 8s linear infinite; }`. After edits, confirm `animate-gradient` appears in dist/assets/index.css.
- [2026-08-01] dot-pattern.tsx hardcoded `id="dot-pattern"` â†’ multiple instances on one page collide on the same SVG pattern ID. Now uses `useId()`. Use `text-[var(--page-accent)]` + `opacity={0.05}` for page-accent dot texture inside hero GlassCards.
- [2026-08-01] Page-accent gradient language: Stats cyan #22d3eeâ†’indigo #6366f1, Browser sky #38bdf8â†’violet #8b5cf6, Productivity emerald #10b981â†’cyan #06b6d4. SectionHeader `title` accepts ReactNode (not just string) so animated gradient JSX can be passed.

- [2026-08-04] Old multi-segment gap-assignment UX (timeline bar with draggable dividers, per-segment -/+/number minute steppers, inline searchable activity picker, Split time, Auto-fill) is now the canonical GapFillModal in src/components/external/GapFillModal.tsx; the pre-redesign src/components/GapFillModal.tsx + GapFillDrawer.tsx still exist in the working tree (view-only via git show — NEVER restore/delete). Fill path = fillGapWithSegments(gap, segments, addExternalTime) in src/lib/external/gaps.ts:133.
- [2026-08-04] grid.ts squarifyTreemap INVARIANT: a treemap row item's width must be rea / rowThickness (rowThickness = rowSum/w horizontal, rowSum/h vertical) and the row-merge heuristic must use that EXACT thickness — using the raw area scalar as width or approximating thickness with the short side makes every row single-item, all rects x:0/w:full, xCoords collapse to [0,1200] and every mosaic card spans the full row (the 'cards take the whole row' bug). Verify layout changes with the esbuild-bundle + node harness (TEMP/opencode/grid-run.js) before building.
- [2026-08-04] Gap semantics SOURCE OF TRUTH = main.ts IPC 'detect-usage-gaps' (main.ts:18870): merges logs (app/browser tracking) + external_sessions, gaps = holes where NOTHING was tracked, min 5m, periods today/week/month/else-all, returns {start,end,durationSeconds}. The renderer detectGaps(sessions,…) (lib/external/gaps.ts) is EXTERNAL-ONLY — on days with sparse external sessions it fabricates huge multi-hour gaps. Any gaps UI (list, timeline blocks) must use the IPC, not detectGaps; timelines.ts usageGapsForDay() clips IPC gaps per day.
- [2026-08-05] Canvas drag/interaction invariant (CanvasCard/CanvasGrid): (1) EVERY pointer interaction must have an onPointerCancel + window pointerup/pointercancel/blur + unmount fallback that clears refs/classes/attributes — a lost pointerup leaves dragging class + zIndex 1000 + grid draggingCardId/data-card-dragging stuck (card clicks dead, resize armed forever, drop targets lit). (2) Enforce a ~2px movement threshold before committing drag-end, else any click on the drag surface fires onDragEnd/onDragStop and the grid GROUPS cards under the cursor (accidental merging). (3) Drop-target rects must be compared in GRID coordinates — the dragged card's own computed transform is grid coords (it lives inside the scaled layer); multiplying targets by zoom/pan breaks grouping whenever zoom != 1. (4) Drag surface = the WHOLE card (CSS already had cursor:grab/touch-action:none on .dk-canvas-card) — header-only wiring made the grab cursor lie and was reported as 'dragging doesn't work'.
- [2026-08-06] Back-and-forth collaboration deliverables (INITIAL_PROMPT/CONTEXT_BUNDLE/CONTEXT_GAPS/CONVERSATION_PROTOCOL/round-XX) belong in `agent/docs/backandfourth-docs/<idea-slug>/` with rounds under `conversation/` — per the ai-collaboration-bridge skill's own artifact spec (corrected by user twice: NOT the docs root, NOT `agent/skills/backandfourth-skill/<idea>/`). CONTEXT_BUNDLE must embed core files VERBATIM (the external AI has no repo access); remaining files go in a fetch-on-REQUEST Part B inventory.
- [2026-08-06] Focus Groups backend gap CLOSED: focus_groups now persists daily_goal_sec/goal_category end-to-end (focusSchema.ts CREATE + guarded ALTER; focusGroupManager.ts save/SELECT/INSERT/UPDATE; main.ts focusGroup:save pass-through ~4728). New IPC focusGroup:getUsage (~4781) returns {group_id, session_id} rows from focus_group_usage (manager getUsage ~154); renderer FocusGroupProgress computes today's sec/streak via computeGroupDailyProgress/computeGroupStreak in focusHelpers.ts. The get-known-apps IPC + preload getKnownApps (preload.ts:1285) DO exist and are safe to call from FocusGroupEditor. Old FocusGroupSelector.tsx deleted (backup agent/backups/20260806-181340-focus-group-selector-pre/).
- [2026-08-06] Life Phases "River of Years" feature shipped (Gold warmth page, LifeRiver section): DB `life_phases` (id/version/title/description/category/start_month/start_year/end_month/end_year/magnitude/color/reflection/era_trends/impact_notes/milestones+connections as JSON strings, created_at/updated_at) + `life_timeline_meta` (key/value/updated_at for journey summary) with guarded migration in main.ts; 7 IPC `lifePhase:*` handlers (get/getSummary/save/delete/saveAll/aiReflect/aiEraTrends/aiSummarize) returning {ok,data?,error?}; preload `lifePhase*` (8 methods) in preload.ts + deskflow-api.d.ts. Renderer: `src/lib/riverMath.ts` (math/types), `src/hooks/useLifePhases.ts` (optimistic CRUD), `src/components/life-river/` (river-canvas, phase-drawer, phase-form-dialog, reflection-flow, era-trends-card, empty-river, river). NOTE: `src/domains/life/` does NOT exist — DB is the source of truth. Backend seeds starter phase `1_start_university`.
- [2026-08-06] lucide-react in App Tracker does NOT export `Loader2` or `Globe2` — names are `LoaderCircle` and `Globe` (verified against lucide-react.d.ts). Also: no toast/sonner package exists (LifeRiver uses hand-rolled fixed div z-[120]); Google Fonts link in index.html now includes Source Serif 4 (it had Inter/DM Serif Display/JetBrains Mono/Space Grotesk already).
- [2026-08-06] base-ui patterns for App Tracker ui primitives: base-ui Drawer used for Sheet (Sheet/SheetTrigger/SheetPortal/SheetClose/SheetOverlay/SheetContent side top/right/bottom/left, close button via `render={<Button variant="ghost" className="absolute top-2 right-2" size="icon-sm"/>}` + XIcon); Slider via SliderControl/SliderTrack/SliderIndicator/SliderThumb/SliderLabel/SliderValue (indicator amber-400/80, thumb amber ring); Dialog via base-ui Dialog.*. Do NOT reach for radix primitives — they aren't installed.
- [2026-08-06] User demand (App Tracker): ALL DB access strictly READ-ONLY — never delete/modify data in %APPDATA%/DeskFlow/deskflow-data.db; use `sqlite3 -readonly` or `readonly: true` connections only. Probe via sqlite3 CLI: repo better-sqlite3 is compiled for Electron's Node (NODE_MODULE_VERSION 145), system Node (137) throws ERR_DLOPEN_FAILED.
- [2026-08-06] detect-usage-gaps (main.ts ~19175) returns ONE merged gap object per contiguous untracked span, even across day boundaries (Aug 2 08:42Z→Aug 5 10:22Z = one 73.7h gap). GapsListModal.tsx now splits per LOCAL calendar day at fetch-mapping stage (splitGapByDay, flatMap); any other gap UI must clip per day like usageGapsForDay (timelines.ts:162). fillGapWithSegments only uses gap.start/end.
- [2026-08-06] External sleep sessions are single DB rows that can span multiple calendar days (e.g. id 64: Jul 25 18:30Z→Jul 27 09:36Z, 39h, incl. ~24h device_off_to_sleep_seconds). Dashboard heatmap buckets sessions hour-by-hour (DashboardPage.tsx:1576) → one session paints a solid multi-day block; that's correct rendering, not duplicate data. "Sleep for 3-5 straight days" = one over-long session, not multiple nights.
- [2026-08-07] Activity Mosaic size caps INVARIANT (user requirement): the biggest card's share of the mosaic is HARD-CAPPED per mode — subtle 0.18 / balanced 0.30 / dramatic 0.48 (MAX_SHARE_MAP + applyMaxShare water-filling in src/lib/external/grid.ts buildTargetWeights). Gamma flattening alone is NOT enough (dominant could still eat 1/3+). ⚠ SUPERSEDED 08-09: the old applyMaxShare renormalized to sum=1 after clamping, so with ≤4 cards the cap NEVER held (subtle n=2 → [0.5,0.5], n=3 → [0.33×3]) — exactly the user's "subtle still shows a card huge" report. FIX (cycle 6): clamp ALL over-cap cards, redistribute only up to the cap, DISCARD leftover (never renormalize), then `fill=sum(weights)` scales treemap h AND `aspectRatio=aspect/fill` so each card's ON-SCREEN share = its weight ≤ cap. Verify grid math via esbuild bundle + node harness (TEMP/opencode/grid-cap-test.js), never eyeball.

- [2026-08-07] Vendored Magic UI components have UNPREDICTABLE prop forwarding: MagicCard (src/components/ui/magic-card.tsx) does NOT spread ...props — style/border-color overrides are silently dropped; put dynamic glow/border on a WRAPPER div around it. ShinyButton used string-concat className (no tailwind-merge) + hardcoded indigo — class overrides like py-2.5 conflict with base py-3; it now accepts ccent (rgb triplet) + orderClass and merges via cn(). Before re-skinning any vendored component, read it: check (1) does it forward ...props, (2) does it use cn/tailwind-merge.
- [2026-08-07] Focus card character set (all already vendored, zero new deps): MagicCard (mouse-follow spotlight border) for group cards, GlareHover (diagonal glare) for stat tiles, DotPattern (useId-safe, currentColor) for card texture at 0.04-0.05 opacity, ShinyButton for primary CTAs, AnimatedGradientText (needs --animate-gradient in index.css @theme — confirmed present line 134), confetti({particleCount,spread,startVelocity,colors}) for goal-hit bursts. Per-group accent = groupAccent(name) hash into 8-color palette (focusHelpers.ts) — deterministic color identity per group.
- [2026-08-07] REFINEMENT of the Loader2/Globe2 lesson: lucide-react in App Tracker exposes `Loader2` and `Globe2` as RUNTIME aliases (`exports.Loader2 = LoaderCircle`, `exports.Globe2 = Earth` in dist/cjs) — code that imports them BUILDS and RUNS fine (vite/esbuild does no type-checking). They are simply MISSING from lucide-react.d.ts, so `tsc` errors on them. Rule: existing code using Loader2/Globe2 is not broken; new code should prefer LoaderCircle/Globe/Earth for type safety.
- [2026-08-07] IDE analytics back-and-forth package (agent/docs/backandfourth-docs/ide-analytics-vscode-extension/): commits table has NO per-file rows — "top files / per-file lines" charts are IMPOSSIBLE with current DB data and MUST come from the new DeskFlow VS Code extension's live capture (new endpoint on port 54321 + new table like code_activity/file_changes, to be specified in RESULT.md). Extension must mirror browser-extension pattern (MV3, chrome.alarms, health check, silent retry). Fetch-on-REQUEST items: IDEProjectsPage.tsx:1337-1520 (AI & Projects Row), ai_usage/projects/ides/tools CREATE TABLEs, loadWorkspaceAnalytics, local server handler ~17949, git/AI handlers.

- [2026-08-07] External-page sleep-exclusion architecture (user rule: sleep ONLY in Sleep Patterns card + Add/Edit flows): enforce at BOTH layers — backend get-external-sessions/get-external-stats filter ea.type != 'sleep' (sleep-deficit recomputed via separate ea.type = 'sleep' query), renderer filters orderedActivities + TransferSessionModal + GapFillDrawer picker. detect-usage-gaps KEEPS sleep as tracked coverage on purpose (nights must not become gaps). Sleep Patterns card reads getSleepTrends/getSleepForDate — separate IPC, never filter those.
- [2026-08-07] BUILD GOTCHA: zip-src.mjs writes dist\src.zip INSIDE dist/ — if any process holds it open (Explorer preview, etc.), 
px vite build dies with EPERM on emptyDir(src.zip) even though compilation succeeded. Free the lock (close app/Explorer) and re-run; vite can't skip a locked outDir file.

- [2026-08-07] Finance Monthly Recap (App Tracker) architecture INVARIANTS: (1) there is NO getLastUsedProviderId() in main.ts — capture the provider from runWithFallback()'s returned { result, usedProviderId } and return { summary, providerId }; OpenRouter fallback model = userPreferences.ai_briefModel || 'google/gemini-2.0-flash-001'. (2) stats_json is stored with CAMELCASE keys (income.total, expense.total, net, activeDays, topCategories, walletBalanceDelta, followThrough, biggestExpense, biggestIncome, momDelta, displayCurrency) — list query json_extract's '$.income.total'/'$.expense.total'/'$.net'; hydrateRecap JSON.parses it. (3) recap stores a generation-time displayCurrency snapshot in stats_json; the UI converts live via convertAmount(amount, snapshotCur, currentDisplayCur) — never re-store amounts. (4) auto-gen (checkMonthlyRecaps) ONLY targets the immediate previous calendar month, is idempotent (skip if row exists), runs at whenReady + every 6h; manual generate supports force to overwrite.
- [2026-08-07] buildChain (src/services/providers/router.ts) takes a CLOSED feature union ('researchDigest'|'goalAssistant'|'resumeBuilder'|'category'|'colors'); ANY new AI feature that wants provider routing must extend that union in router.ts or TS errors will block the renderer build — new AI IPC features go through buildChain(pState, '<feature>') + runWithFallback for the provider chain, then OpenRouter direct fallback.- [2026-08-07] IDE analytics + VS Code extension SHIPPED: `code_activity` table + always-on `POST /code-activity` on the port-54321 capture server (accepts `{workspace_path, activities[]}`, `{events[]}`, or bare arrays; per-entry `id` from the extension's crypto.randomUUID makes retried batches idempotent via INSERT OR IGNORE); `set-browser-tracking` no longer stops the server — only the browser session-flush timer follows the pref. `get-code-activity-stats` IPC + `codeActivity` on `get-ide-projects-overview`; renderer Live Pulse grid + Coding Activity chart live in IDEProjectsPage.tsx / AnalyticsDashboard.tsx; extension in vscode-extension/ (npm install + npm run compile + F5, or vsce package). `getCodeActivityStats` was added ONLY to preload.ts (preload-old.ts / preload2.ts are legacy, untouched).
- [2026-08-07] AIToolsTab.tsx ALREADY has an all-time "Model Usage Timeline" (timeLock → `effectiveAiPeriod === 'all'`, minDate-anchored window, stacked per-model across all tools) fed by `overview.aiUsage.byTool[agentId].daily` + `.modelDaily[model][dayStr]` — both have IDENTICAL field shape (tokens/tokens_in/tokens_out/messageCount/sessions/cost). Before adding any AI-usage timeline/chart, check AIToolsTab first: the data + model timeline already exist renderer-side, NO new IPC needed. The 2026-08-08 round added a per-tool counterpart ("Tool Usage Timeline", byTool[toolId].daily) + dominance "phases" strips (contiguous winner-per-day runs as chips) to both charts.
- [2026-08-08] Monthly Recap AI-output cleaning (supersedes any "rendered verbatim" notes): raw provider output is cleaned by the shared heuristic cleaner `src/shared/recap.ts` at BOTH layers — generate-time in main.ts `finance:recap-generate` (cleanRecapSummary; empty result → fallback message + status 'failed') AND render-time defensively in RecapPanel (narrative memo, so legacy verbatim rows are fixed without regen). `computeApexInsight(stats)` derives the APEX card from REAL stats (dominant category ≥30% share → net swing vs prev → wallet delta ≥5000 → biggest expense) and is stored inside stats_json — NO schema change. Progress stages (reading→analyzing→writing→saving→done) flow over `finance:recap-progress` IPC only; DB rows stay frozen history. GOTCHA: main-process `toLocaleString()` with no args uses the SYSTEM locale (dots as thousands sep) — pass 'en-US'. GOTCHA: type-only destructuring from require() in main.ts needs `import type` (esbuild strips types; tsc doesn't). build.mjs takes ~5min — use shell timeout ≥600000ms or run steps separately (vite ~1m46s + esbuild shared/preload/main).
- [2026-08-08] ZIP GOTCHA: `node scripts/zip-src.mjs` HANGS — it Compress-Archive's `agent\*` which now contains agent/backups (740MB, 21.5k files) + agent/docs (375MB) → >20min or lock contention, and bsdtar `--exclude` silently FAILS on this machine. Working method: build an explicit relative file list (`Get-ChildItem src,scripts,agent -Recurse -File | Where notmatch backups/docs`, `-replace '\\','/'`) → `Set-Content` list → `tar -a -c -f dist/src.zip -T <list>` (10.6MB, exit 0, verified via `tar -tf`). Verify the zip's integrity AFTER creating it (a killed Compress-Archive leaves a corrupt dist/src.zip).
- [2026-08-09] Finance IPC contract drift (the "undefined label / NaN chart" failure class): the renderer MUST consume the exact backend field names — `finance:get-spending-by-category` (main.ts:27764) returns `categoryName/categoryColor/categoryIcon/txn_count/amount` (LEFT JOIN with 'Uncategorized'/'#888888' fallbacks, NO percentage field — compute pct renderer-side); `finance:get-subscription-intelligence` (main.ts:30020) returns `totalMonthlyCost/burdenPercentage/monthlyIncome/subscriptionCount/growthTrend/upcomingRenewals/urgentRenewals/radarData{axes,values,colors}/subscriptions[{name,price,currency,billingCycle,monthlyEquivalent,nextRenewalDate,daysUntilRenewal,isUrgent,isWarning}]`; `finance:get-wallet-health` returns per-wallet `transactionFrequency` (per-day = txnCount/30), `avgDailySpend` (expenseTotal/30, excludes adjustments), `lastActivity` + top-level `overallScore` (mean of healthScores)/`totalBalance`. Also: watch for `fmtMoney()` being applied to a PERCENTAGE (feeBurden rendered as "Rp" — display bug #3 found in the same round). `computeRecapStats` (main.ts:25810) returns `spendingByCategory` (ALL categories); RecapPanel falls back to `topCategories` for legacy stats_json rows.
- [2026-08-10] COMPONENTS-IN-COMPONENTS FLICKER BUG: defining a component inside another component body (e.g. `const Row: React.FC = ...` inside PromptHistoryTab) gives it a NEW type identity on EVERY parent render → React unmounts+remounts the subtree each time → any framer-motion `initial="hidden"` fade-in replays forever (user sees constant appear/disappear flicker; DOM shows opacity:0 stuck mid-animation). Fix: hoist the component to module level and pass closures as explicit props. Top suspect for ANY flickering UI in this repo (PromptHistoryTab.tsx had it for StatTile + Row).
- [2026-08-10] FLATTENED-FILE SIGNATURE (root-caused learn:* "No handler registered"): a .ts source rewritten as ONE line (0 newlines) parses as only its import statements — the first `//` comment swallows the entire file, so esbuild emits exactly "use strict"; (14 bytes, exit 0, NO error) and every consumer gets undefined at runtime (registerLearnHandlers threw at index.ts:122 loadPromptLibrary). Detect: node statement-parse (statements.length === 4) or dist file === 14b. Restore from git HEAD blob via node execSync Buffer (the PowerShell `git show | Set-Content` pipeline ALSO strips all newlines — never extract git blobs through the PS pipeline), apply renames manually, recompile, then LF-scan src/ for other flattened files. Cause here: parallel-process save of the curriculum rename on promptLibrary.ts.
- [2026-08-12] FOCUS STRICTNESS SEMANTICS (final, do not regress): Focus groups are PURE allowed-set containers (apps/sites/categories) � strictness/duration/daily-goal are NOT group fields anymore. Lenient ('distracting') = blocks distracting tier only; Strict ('non_allowed') = hard whitelist: explicit apps/domains + category matches, else productive tier only (focusManager isAllowed). Per-mode daily goals live in DB singleton ocus_goal_config (id=1, lenient_goal_sec/strict_goal_sec) via IPC focusGoal:get/save (FocusManager.registerIpc) � renderer trackers: src/features/focus/FocusGoals.tsx (progress rings + GoalsDialog), NOT per-group. deep_focus_sessions.strictness column drives per-mode progress (completed outcomes only). Never put strictness/goals back into the FocusGroupEditor or FocusGroupsPanel.
- [2026-08-13] FOCUS BUFFER MODEL (final semantics): categories in a focus group are a TOLERANCE BUFFER, not a whitelist. focusManager.isAllowed: explicit apps/domains always allowed; STRICT ('non_allowed') group session = explicit list ONLY (categories BLOCKED � hasExplicit check returns false for everything else); LENIENT group session = explicit list + apps whose real category ? allowed_categories; no-explicit-list fallbacks keep plain sessions working (strict=productive tier only, lenient=blocks distracting). Goals are per-mode (strict_goal_sec/lenient_goal_sec in focus_goal_config). UI copy: group editor labels 'Allowed apps/sites � always' + 'Tolerance buffer � lenient only'; FocusTimer pills 'Exact apps & sites only' / 'Allowed apps + buffer'.
- [2026-08-13] dist-electron/domains/*.js are compiled PER-FILE by scripts/build.mjs Step 3 (esbuild, NO --bundle, target=node22) � scripts/rebuild-main.mjs does NOT rebuild them (main.cjs only bundles main.ts; focusManager is loaded via runtime require('./domains/focus/focusManager')). After editing src/domains/**/*.ts, recompile each file exactly like build.mjs Step 3 or the running app silently uses STALE domain logic (focus fixes 'verified' but inert for 5 days: focusManager.js was dated 8/8 while src was 8/12). Same class of trap as the services/learn lesson.

- [2026-08-16] LIFE PAGE SELF TAB MERGE (App Tracker, user-mandated): the Life page pages-mode has FIVE tabs now: covenant/memories/gold/notes/self. The old separate profile/graph/brain tabs are GONE � they live stacked inside ONE 'self' tab (ProfileTab = 'Identity & Profile', ContextGraphView = 'Knowledge Graph', BrainManagementView = 'Memory & Brain', max-w-5xl space-y-10 with uppercase section headers). User was angry they were separate ("they're the same shit"). Backward-compat: handleOpenPage/redirectToPage tabMaps in LifePage.tsx map 'profile'/'graph'/'brain' ? 'self' so CoreSample onOpenPage still works. NEVER re-split these into separate tabs.
- [2026-08-16] REACTION-DIFFUSION LIVING SUBSTRATE (App Tracker): lives in src/components/life-river/LivingSubstrate.tsx (R3F orthographic Canvas, ping-pong WebGLRenderTargets FloatType/NearestFilter 256x256, 384 when devicePixelRatio > 1.5, 2 sim passes per frame, coral preset f=0.0545/k=0.062/dA=1.0/dB=0.5, 14 random circular B-gradient seeds) with shaders in src/shaders/rd-simulation.glsl + rd-display.glsl imported via ?raw and typed by src/shaders/glsl.d.ts. GLSL1 style (texture2D/varying/gl_FragColor) is CORRECT for THREE.ShaderMaterial defaults — never convert to GLSL3. Renders at z-0 under RingCanvas z-10 inside CoreSample.tsx data-core-sample-stage. Display ramp is premultiplied alpha (vec4(color*alpha, alpha)): B=0 -> #09090b/a0, B=0.5 -> #b45309/a0.15, B=1 -> #fbbf24/a0.40. Shaders end up in the LAZY LifePage chunk, NOT the main index bundle — when verifying, grep dist/assets/LifePage*.js for previousIterationTexture/textureToDisplay.
- [2026-08-17] RETENTION ENGINE (App Tracker, user-mandated): Pattern Interrupt / Curiosity Gap / Hook-at-3rd-4th-sec / Attention Anchor were NOT in overlayPrompts.ts (only 'hook = first segment, covers first 5s') or any agent/*.md — retention system is Phase 7 (NEW, beyond v3.0 spec). Master checklist of EVERY user-mentioned feature: agent/CONTENT_ENGINE_CHECKLIST.md (Parts A–G, updated every cycle). Research prompt package: agent/docs/generate-prompt-docs/content-retention-research-17082026/{PROMPT.md,CONTEXT_BUNDLE.md} (per generate-prompt skill v2.0.0). Every script bullet must carry machine-checkable evidence of which retention criteria it satisfies; script_frames prompt must emit per-frame etention: {criteria, mechanism, evidence, score}. Hook timing: 0–3s interrupt, 3–4s hook payoff. Do NOT implement v3.0 Phase 0+1 until retention RESULT.md lands (research informs script_frames + Content Equation merge). Provider chain feature union (router.ts:34) needs new id for content engine/retention calls.

- [2026-08-17] CONTENT ENGINE UI SHELL v1 (App Tracker): 7-view workspace lives in src/features/content-engine/ (Brainstorm, Ideas 4-col funnel, Episodes with Script/SEO/Analytics/Assets/Metrics sub-tabs, Themes, Analytics w/ AnalyticsBody({episodeId}) export, Lessons, Frameworks versioned) + shared primitives in components/ui.tsx. Toggled from OverlayStudioPage.tsx header via mode state ('studio'|'engine'); FeatureStudioPage export + StudioProvider stay mounted. ALL IPC goes through (window as any).deskflowAPI?.contentEngine (ideasList/ideaSave/scriptGenerate/scriptRegenerateLine/validateScriptEvidence/gateOverride/injectSeo/synthesizeIdeas/brainstormClassify/brainstormSummary/themes*/analytics*/lessons*/frameworks*) � BACKEND NOT YET IMPLEMENTED (research phase; UI is shell). SvgRetentionChart exports RetentionCurveChart({data:[{t,pct}],height}) � converts csv?points at call site (AnalyticsView/EpisodesView). Chunked-write strategy (<=2.2KB + // ~MORE~ marker) worked; noUnusedLocals bites hard on lucide imports � verify usage before build.
- [2026-08-17] CONTEXT-RESTORE RESULT.md SYMBOL DRIFT (App Tracker): the Architect's context-retrieval-memory-restore RESULT.md references APIs that DON'T exist � contextFormatter.ts (created it: formatBrainContext/formatMemoryContext/truncateToBudget with hard final cap so output NEVER exceeds maxChars), memoryRetrieval.search() (real: getRelevantMemories(db, threadDate, query?, limit) � ai_chat_memories only), memoryStore.saveAgentMemory() (real: insertMemory(MemoryEntry)), and ContextService.ts:149 (that's a renderer prompt assembler, NOT the assemble-context IPC pass-through � the IPC has NO active renderer caller, preload-only). The completion-capture hook lives in save-terminal-message (meta.status==='completed'), not a dedicated handler. Always verify RESULT.md symbols against src before implementing.
- [2026-08-18] ASSEMBLE-CONTEXT HAD NO RENDERER CALLER (App Tracker): the ssemble-context IPC (main.ts:15073) existed with preload bridges (preload.ts:876, preload2.ts:691) but NO renderer code ever invoked it � the pipeline was dormant. Now wired in TWO places: (1) NewSessionDialog handler (TerminalPage.tsx ~4132) � after initContent resolution, before systemPrompt, appends assembled context to initContent; (2) Quick instruction handler (~1455) � after DB save, before agentSend, writes assembled context to terminal via terminalWriteRaw. Both pass topic + sessionId + tokenBudget:2000. ContextService.ts:149 is NOT the IPC caller (it's a renderer prompt assembler � different service entirely).
