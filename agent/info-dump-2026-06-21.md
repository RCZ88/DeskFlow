# DeskFlow Info-Dump for Architect

**Generated:** 2026-06-21
**Source:** `src/main.ts` (19,156 lines), `src/preload.ts`, `src/pages/SettingsPage.tsx`, `src/services/`, `agent/`

---

## 1. IPC Registry

### Core / App Lifecycle
| Channel | Args | Return |
|---------|------|--------|
| `get-logs` | none | `LogEntry[]` |
| `quit-app` | none | void |
| `show-window` | none | void |
| `get-auto-start-status` | none | `{ openAtLogin: boolean }` |
| `set-auto-start` | `(enabled: boolean)` | void |
| `get-window-state` | none | `{ width, height, maximized }` |
| `reset-window-state` | none | `{ success, error? }` |

### Preferences / Settings
| Channel | Args | Return |
|---------|------|--------|
| `get-preferences` | none | `UserPreferences` object |
| `set-preference` | `(key, value)` | void |
| `get-tracking-settings` | none | `{ sleep_gap_ms, max_session_ms }` |
| `set-tracking-setting` | `(key, value)` | boolean |
| `get-external-settings` | `(key)` | value or null |
| `set-external-settings` | `(key, value)` | boolean |

### Database / Tables
| Channel | Args | Return |
|---------|------|--------|
| `get-db-path` | none | string |
| `get-storage-status` | none | `{ useJson, dbPath, tableCount }` |
| `get-table-schema` | `(tableName)` | `{ columns[] }` |
| `get-database-tables` | none | `{ tables[], type }` |
| `get-table-data` | `(tableName, limit=50)` | `{ rows[], schema }` |
| `migrate-to-aggregates` | none | void |
| `get-daily-aggregates` | none | `DailyAggregate[]` |
| `backfill-aggregations` | none | void |

### Tracking / Logs
| Channel | Args | Return |
|---------|------|--------|
| `get-stats` | none | `AppStats` |
| `toggle-tracking` | none | boolean |
| `set-tracking` | `(enabled)` | void |
| `clear-data` | none | void |
| `clear-today` | none | void |
| `get-current-foreground` | none | `{ app, category, title, timestamp }` or null |

### Dashboard / Aggregates
| Channel | Args | Return |
|---------|------|--------|
| `get-dashboard-data` | `({ period, dateOffset })` | `{ overview, weeklyHeatmap, appStats, websiteStats }` |
| `get-page-stats` | `({ page, period, dateOffset })` | page-specific stats |
| `get-logs-by-period` | `(period)` | `LogEntry[]` |
| `get-daily-stats` | `(period)` | daily stats |
| `get-app-stats` | `(period?)` | `AppStats` |
| `get-daily-productivity` | `(date)` | productivity data |
| `get-productivity-range` | `(startDate, endDate)` | range data |

### Browser Tracking
| Channel | Args | Return |
|---------|------|--------|
| `get-browser-logs` | `(period, dateOffset)` | `BrowserLog[]` |
| `get-browser-domain-stats` | `(period, dateOffset)` | domain stats |
| `get-browser-category-stats` | `(period, dateOffset)` | category stats |
| `get-browser-tracking` | none | boolean |
| `set-browser-tracking` | `(enabled)` | void |
| `get-browser-tracking-status` | none | boolean |
| `set-browser-excluded-domains` | `(domains[])` | void |
| `set-browser-with-extension` | `(browser)` | void |
| `get-available-browsers` | none | browser list |
| `get-tracked-browsers` | none | tracked browser list |

### Categories / Tiers
| Channel | Args | Return |
|---------|------|--------|
| `get-category-config` | none | category config |
| `set-app-category` | `(appName, category)` | void |
| `set-domain-category` | `(domain, category)` | void |
| `set-app-tier` | `(appName, tier)` | void |
| `set-domain-tier` | `(domain, tier)` | void |
| `set-tier-assignments` | `(assignments)` | void |
| `get-tier-assignments` | none | tier assignments |
| `apply-category-to-historical` | `(tierAssignments)` | void |
| `get-default-categories` | none | category list |
| `add-category` | `(name)` | void |
| `remove-category` | `(name)` | void |
| `set-domain-keyword-rules` | `(domain, keywordSets)` | void |
| `get-domain-keyword-rules` | `(domain)` | keyword rules |
| `set-domain-default-category` | `(domain, category)` | void |
| `get-domain-default-category` | `(domain)` | category |
| `get-keyword-enabled-domains` | none | domain list |
| `add-keyword-domain` | `(domain, keywordSets)` | void |
| `remove-keyword-domain` | `(domain)` | void |
| `update-categories-from-overrides` | `(appOverrides, domainOverrides)` | void |

### External Activities / Sessions
| Channel | Args | Return |
|---------|------|--------|
| `get-external-activities` | none | `ExternalActivity[]` |
| `add-external-activity` | `(activity)` | `{ success, id }` |
| `update-external-activity` | `(id, updates)` | boolean |
| `reorder-external-activities` | `(ordered[])` | boolean |
| `delete-external-activity` | `(id)` | boolean |
| `start-external-session` | `(activityId)` | `{ success, sessionId }` |
| `stop-external-session` | `(sessionId, endTime?, offToSleep?, wakeToApp?)` | `{ success, duration }` |
| `update-external-session` | `(sessionId, updates)` | `{ success }` |
| `delete-external-session` | `(sessionId)` | boolean |
| `get-external-sessions` | `(period)` | `ExternalSession[]` |
| `get-active-external-session` | none | session or null |
| `get-external-stats` | `(period)` | `{ byActivity, total_seconds, sleep_deficit, average_sleep }` |
| `get-comparison-stats` | `(period)` | `{ external_seconds, internal_seconds }` |
| `get-activity-stats` | `(activityId)` | `{ today, week, month, session_count }` |
| `add-manual-sleep` | `(sleepData)` | `{ success, sessionId }` |
| `update-manual-sleep` | `(sessionId, sleepData)` | `{ success }` |
| `get-sleep-for-date` | `(dateStr)` | sleep session or null |
| `get-consistency-score` | `(period)` | `{ score, weekly_comparison, trend, streak }` |
| `add-external-time` | `({ activityId, durationMinutes, started_at, ended_at })` | `{ success, sessionId }` |

### Sleep Detection
| Channel | Args | Return |
|---------|------|--------|
| `check-sleep-detection` | none | `{ detected, gapStart, gapEnd, gapMinutes, suggestedBedtime, suggestedWakeTime }` |
| `confirm-sleep` | `(sleepData)` | `{ success, sessionId }` |
| `dismiss-sleep-detection` | none | boolean |
| `get-sleep-trends` | `(period, dateOffset)` | `{ daily[], average_bedtime, average_wake_time, average_sleep_duration, average_latency, average_wake_latency }` |
| `get-sleep-debug` | `(period, dateOffset)` | `{ sessions[], trendsRaw[], queryRange }` |
| `fix-sleep-dates` | none | `{ fixed, message }` |
| `get-morning-prompt` | none | morning prompt data or null |
| `dismiss-morning-prompt` | none | boolean |
| `get-typical-activity-at-time` | `(timestamp)` | `{ id, name, color }` or null |
| `detect-usage-gaps` | `({ period, minGapMinutes })` | gap array |

### Heatmap / Insights
| Channel | Args | Return |
|---------|------|--------|
| `get-typical-day` | `(days, dateOffset)` | `{ grid, legend, stats, generatedAt, daysCovered }` |
| `get-hourly-heatmap` | `(days)` | `number[][]` (7×24 grid) |
| `get-best-days` | none | `{ bestDay, worstDay, averages }` |
| `get-day-detail` | `(dateStr)` | `{ logs[], externalSessions[] }` |

### Productivity Sessions
| Channel | Args | Return |
|---------|------|--------|
| `save-productivity-session` | `(session)` | `{ id }` |
| `get-productivity-sessions` | `(opts)` | `{ sessions[], stats }` |
| `clear-productivity-sessions` | none | void |

### Projects / IDEs
| Channel | Args | Return |
|---------|------|--------|
| `detect-ides` | none | `IDE[]` |
| `get-ides` | none | `IDE[]` |
| `get-extensions` | `(ideId)` | `Extension[]` |
| `scan-tools` | none | `Tool[]` |
| `get-tools` | `(category?)` | `Tool[]` |
| `get-tool-categories` | none | categories |
| `add-project` | `(projectData)` | `{ success, id, name }` |
| `update-project` | `(projectId, updates)` | `{ success }` |
| `detect-project-language` | `(projectPath)` | `{ success, language, fileCount, totalFiles }` |
| `detect-projects-languages` | `(projectPaths[])` | per-project language results |
| `delete-project` | `(projectId)` | `{ success }` |
| `restore-project` | `(projectId)` | `{ success }` |
| `open-project` | `(projectId, ideId?)` | `{ success, ide }` |
| `get-projects` | none | `Project[]` |
| `get-all-projects` | none | `Project[]` |
| `get-project-tools` | `(projectId)` | `Tool[]` |
| `calculate-project-health` | `(projectId)` | `{ healthScore, activityLevel, aiSessions, commits }` |
| `get-project-details` | `(projectId)` | `{ project, tools, sessions, presets, aiUsage, health }` |
| `remove-project` | `(projectId)` | `{ success }` |
| `scan-ide-default-projects` | none | `{ ide, projects[] }[]` |
| `scan-custom-directory` | `(rootDir)` | `{ success, projects[] }` |
| `sync-commits` | `(projectId, repoPath)` | `{ success, commits, errors[] }` |
| `sync-github-commits` | `(projectId, owner, repo, token?)` | `{ success, commits, errors[] }` |
| `get-dora-metrics` | `(projectId, period)` | DORA metrics or null |
| `get-commit-history` | `(projectId, limit)` | `Commit[]` |
| `get-contributor-stats` | `(projectId)` | contributor stats |
| `get-git-diff` | `(projectId, diffType)` | `{ success, diff }` |

### AI Usage / Routing
| Channel | Args | Return |
|---------|------|--------|
| `get-ai-usage-summary` | `(period, dateOffset, projectId?)` | `{ totalTokens, totalCost, byTool }` |
| `get-commit-stats` | `(projectId, period)` | commit stats |
| `sync-ai-usage` | none | sync results |
| `get-ide-projects-overview` | `(period?, dateOffset)` | `{ ides, tools, projects, aiUsage, commits }` |
| `generate-ai-colors` | `(apps[])` | color map |
| `generate-ai-categorization` | `(items[])` | categorized items |
| `test-openrouter-key` | none | `{ success, error?, model? }` |
| `summarize-with-llm` | `(prompt, options)` | `{ success, summary?, error? }` |
| `get-ai-config` | none | AI config object |
| `save-ai-config` | `(config)` | `{ success }` |
| `get-ai-providers` | none | provider state |
| `save-ai-providers` | `(state)` | `{ success }` |
| `test-ai-provider` | `(providerId)` | `{ success, content?, error? }` |

### Terminal / PTY
| Channel | Args | Return |
|---------|------|--------|
| `terminal:create` | `(id, cwd, cols, rows, agentType?)` | `{ success, error? }` |
| `spawn-terminal` | `(id, cwd?, agentType?)` | `{ success, error? }` |
| `terminal:write-raw` | `(terminalId, data)` | `{ success }` |
| `write-terminal` | `(terminalId, data)` | `{ success, error? }` |
| `terminal:write` | `(terminalId, data)` | `{ success }` |
| `terminal:write-old-format` | `(terminalId, data)` | `{ success, queued?, error? }` |
| `resize-terminal` | `(terminalId, cols, rows)` | `{ success }` |
| `terminal:resize-old-format` | `(terminalId, cols, rows)` | `{ success }` |
| `kill-terminal` | `(terminalId)` | `{ success }` |
| `terminal:destroy` | `(terminalId)` | `{ success }` |
| `terminal:destroy-old-format` | `(terminalId)` | `{ success }` |
| `retry-agent-init` | `(terminalId, agentType)` | `{ success, error? }` |
| `agent:verify` | `(agentType)` | `AgentVerifyResult` |
| `agent:arm-handshake` | `(terminalId)` | `{ success, token, bracketedPaste }` |
| `agent:send` | `(terminalId, data, agentType?)` | `{ success, queued }` |
| `agent:get-phase` | `(terminalId)` | phase string |
| `agent:start-timeout` | `(terminalId, agentType)` | `{ success }` |
| `agent:retry-launch` | `(terminalId, agentType)` | `{ success }` |
| `get-terminal-layouts` | `(projectId?)` | layout array |
| `save-terminal-layout` | `(data)` | `{ success, id }` |
| `delete-terminal-layout` | `(layoutId)` | `{ success }` |
| `set-active-terminal-layout` | `(layoutId)` | `{ success }` |
| `get-terminal-presets` | `(projectId?)` | preset array |
| `add-terminal-preset` | `(preset)` | `{ success, id }` |
| `remove-terminal-preset` | `(presetId)` | `{ success }` |
| `execute-terminal-preset` | `(presetId, terminalId?)` | `{ success, sessionId, command }` |
| `save-terminal-preset` | `(data)` | `{ success, id }` |
| `get-terminal-sessions` | `(projectId?, limit?)` | session array |
| `save-terminal-session` | `(session)` | `{ success, id, resumeId }` |
| `delete-terminal-session` | `(sessionId)` | `{ success }` |
| `get-terminal-session-resume-id` | `(sessionId)` | resumeId or null |
| `get-terminal-session-by-id` | `(sessionId)` | session or null |
| `check-session-exists` | `(sessionId)` | `{ exists, error? }` |
| `save-terminal-message` | `({ sessionId, role, content })` | `{ success, id }` |
| `get-session-messages` | `(sessionId, agentType?)` | `{ success, data[] }` |
| `delete-terminal-message` | `({ id })` | `{ success }` |
| `get-prompt-history` | `({ projectId, limit })` | `{ success, data[] }` |
| `summarize-session` | `(sessionId, projectPath?)` | `{ success, data }` |
| `create-terminal-window` | `(options?)` | `{ success, terminalId, windowId }` |
| `debug-ai-agents` | none | `{ agents, database }` |
| `send-instructions-to-terminal` | `({ terminalId, instructions, linkedProblemId?, linkedRequestId? })` | `{ success }` |
| `save-terminal-binding` | `({ terminalId, problemId?, sessionContext?, status? })` | `{ success }` |
| `get-terminal-binding` | `(terminalId)` | `{ success, data }` |
| `get-terminal-bindings` | none | `{ success, data[] }` |
| `assign-problem-to-terminal` | `({ problemId, terminalId?, skillId?, systemPrompt?, projectId?, projectPath? })` | `{ success, data: { terminalId, isNewTerminal, prompt } }` |

### Workspace State
| Channel | Args | Return |
|---------|------|--------|
| `workspace:save` | `(data: full payload)` | `{ success, name }` |
| `workspace:load` | `({ projectId, name? })` | `{ success, data }` |
| `workspace:list` | `({ projectId })` | `{ success, data[] }` |
| `workspace:delete` | `({ projectId, name })` | `{ success }` |
| `workspace:list-all` | none | `{ success, data[] }` |

### Cross-Session Sync
| Channel | Args | Return |
|---------|------|--------|
| `get-cross-session-sync-config` | none | config object |
| `set-cross-session-sync-config` | `(config)` | `{ success }` |

### Session Categorization
| Channel | Args | Return |
|---------|------|--------|
| `update-session-category` | `(data)` | `{ success }` |
| `get-parsed-session-items` | `(sessionId)` | `{ success, data[] }` |
| `analyze-session-category` | `(sessionId)` | `{ success, category, confidence, tags, productArea }` |
| `save-session-config` | `({ sessionId, config, projectPath })` | `{ success }` |
| `load-session-config` | `({ sessionId, projectPath })` | `{ success, data }` |

### @mention Routing
| Channel | Args | Return |
|---------|------|--------|
| `resolve-at-mention` | `({ input, terminalTabs })` | `{ terminalId, message, resolved }` |

### Auto-Assign Routing
| Channel | Args | Return |
|---------|------|--------|
| `route-prompt` | `({ prompt, projectPath? })` | routing result |
| `update-session-summary` | `({ sessionId, force? })` | `{ success, summary?, topic?, autoNamed? }` |
| `get-routing-costs` | none | `{ today, week, month, total, byType }` |
| `reset-routing-costs` | none | `{ success }` |
| `get-auto-assign-config` | none | config |
| `save-auto-assign-config` | `(config)` | `{ success }` |

### Goals / Planning
| Channel | Args | Return |
|---------|------|--------|
| `get-goals` | `(date)` | `{ date, reviewSummary, goals[] }` |
| `get-goals-batch` | `(startDate, endDate)` | `{ success, days[] }` |
| `save-goal` | `(date, goal)` | `{ success }` |
| `get-longterm-goals` | none | `{ success, goals[] }` |
| `delete-goal` | `(goalId)` | `{ success }` |
| `save-goal-review` | `(date, reviewSummary)` | `{ success }` |
| `suggest-goals` | `(date, ctx?)` | `{ success, suggestions[], error? }` |
| `review-goals` | `(date, ctx?)` | `{ success, review, suggestions[] }` |
| `parse-goal-feedback` | `({ message, goals })` | `{ completed[], added[], note }` |
| `read-planning-md` | none | `{ content, error? }` |
| `write-planning-md` | `(content)` | `{ success, error? }` |
| `get-goal-context` | `(date)` | `{ success, last7dByCategory, yesterday }` |

### Problems / Requests (Tracker Mind)
| Channel | Args | Return |
|---------|------|--------|
| `get-problems` | `({ projectId?, projectPath? })` | `{ success, data[], fromFile }` |
| `create-problem` | `(data)` | `{ success, data }` |
| `update-problem-status` | `({ problemId, status, projectId?, projectPath?, actor? })` | `{ success }` |
| `update-problem` | `(data)` | `{ success }` |
| `delete-problem` | `({ problemId, projectId?, projectPath?, actor? })` | `{ success }` |
| `get-requests` | `({ projectId? })` | `{ success, data }` |
| `create-request` | `(data)` | `{ success, data }` |
| `update-request-status` | `({ requestId, status, projectId?, actor? })` | `{ success }` |
| `delete-request` | `({ requestId, projectId?, actor? })` | `{ success }` |
| `link-problem-to-request` | `({ requestId, problemId, projectId?, actor? })` | `{ success }` |
| `unlink-problem-from-request` | `({ requestId, problemId, projectId?, actor? })` | `{ success }` |
| `sync-problems-md` | `({ projectId?, projectPath? })` | `{ success, count }` |

### Skills
| Channel | Args | Return |
|---------|------|--------|
| `get-skills` | `({ projectPath? })` | `{ success, data[] }` |
| `get-workspace-skills` | `(args)` | `{ success, data[] }` |
| `create-skill` | `({ name, category, description, content, projectPath? })` | `{ success, data }` |
| `update-skill` | `({ id, name, category, description, content, projectPath? })` | `{ success, data }` |
| `delete-skill` | `({ id, projectPath? })` | `{ success, data }` |
| `get-app-skills` | none | `{ success, data[] }` |
| `add-skill-to-project` | `({ skillId, projectPath })` | `{ success, data }` |
| `get-saved-skills` | none | `{ success, data[] }` |
| `save-workspace-skill` | `({ skillId })` | `{ success, data }` |
| `unsave-workspace-skill` | `({ skillId })` | `{ success, data }` |
| `seed-workspace-skills` | `({ sourceDir })` | `{ success, data: { copied, skipped } }` |
| `watch-agent-files` | none | `{ success, watching, files[] }` |

### Checklists / Feedback
| Channel | Args | Return |
|---------|------|--------|
| `add-check-feedback` | `({ parentId, checkId, parentType, feedback })` | `{ success, check }` |
| `send-check-feedback-to-terminal` | `({ terminalId, checkId, checkDescription, feedback, sessionId? })` | `{ success, message }` |

### Session Compaction
| Channel | Args | Return |
|---------|------|--------|
| `check-session-compaction` | `({ sessionId, messageThreshold? })` | `{ needsCompaction, messageCount? }` |
| `compact-session` | `({ sessionId, summaryPrompt? })` | `{ success, newSessionId, archivedSessionId }` |

### Context Maintenance
| Channel | Args | Return |
|---------|------|--------|
| `get-context-systems` | `(projectPath?)` | `{ success, data[] }` |
| `get-session-summaries` | `(opts?)` | `{ success, data[] }` |
| `get-deep-memory` | none | `{ success, data }` |
| `get-rag-stats` | `(projectPath?)` | `{ success, data }` |

### Activity Log
| Channel | Args | Return |
|---------|------|--------|
| `log-activity` | `(data)` | `{ success }` |
| `get-activity-log` | `(opts?)` | `{ success, data[] }` |
| `get-ai-context` | `(opts?)` | `{ success, context }` |

### Prompt Templates
| Channel | Args | Return |
|---------|------|--------|
| `get-prompt-templates` | `(projectId)` | `{ success, data[] }` |
| `save-prompt-template` | `(data)` | `{ success, data }` |
| `delete-prompt-template` | `(templateId)` | `{ success, data }` |

### Misc / Stubs
| Channel | Args | Return |
|---------|------|--------|
| `update-activity-chart-preference` | `(activityId, chartType)` | `{ success }` |
| `get-hour-detail` | none | `{ success: false, error: 'Not implemented' }` |
| `get-workspace-todos` | none | `{ success: false, error: 'Not implemented' }` |
| `add-workspace-todo` | none | `{ success: false, error: 'Not implemented' }` |
| `toggle-workspace-todo` | none | `{ success: false, error: 'Not implemented' }` |
| `delete-workspace-todo` | none | `{ success: false, error: 'Not implemented' }` |
| `write-agent-actions` | `({ projectPath, terminalId, actions })` | `{ success }` |
| `setup-actions-file-watcher` | `({ projectPath, terminalId })` | `{ success }` |
| `execute-actions-from-file` | `({ projectPath, terminalId })` | `{ success }` |
| `write-feature-spec-file` | `(content)` | `{ success }` |
| `pick-folder` | none | `{ success, path }` |
| `show-open-dialog` | `(options)` | `{ canceled, filePaths }` |
| `save-file` | `(options)` | `{ success, path }` |
| `list-init-files` | `({ projectPath? })` | `{ success, data[] }` |
| `read-init-file` | `({ filename, projectPath })` | `{ success, data }` |
| `clean-corrupted-data` | none | void |
| `deep-clean-and-rebuild` | none | void |
| `get-browser-sessions` | none | sessions |
| `get-sessions` | none | sessions |

### MCP / Design Library
| Channel | Args | Return |
|---------|------|--------|
| `mcp-start-server` | `(serverId)` | `{ success, tools? }` |
| `mcp-server-status` | `(serverId)` | `{ status, toolCount?, uptime? }` |
| `mcp-stop-server` | `(serverId)` | `{ success }` |
| `mcp-list-tools` | `(serverId)` | `{ success, tools }` |
| `mcp-call-tool` | `(serverId, toolName, args)` | `{ success, result }` |
| `aceternity-fetch-registry` | none | `{ success, components, total }` |
| `aceternity-fetch-component` | `({ slug })` | `{ success, component }` |
| `aceternity-install-component` | `(slug, cwd)` | `{ success, filesWritten }` |
| `fetch-refero-catalog` | `({ forceRefresh?, query? })` | `{ success, systems, total }` |
| `fetch-refero-system` | `({ slug })` | `{ success, system }` |
| `search-refero-systems` | `({ query })` | `{ success, systems, total }` |
| `get-design-library-config` | none | config |
| `set-design-library-config` | `(config)` | `{ success }` |
| `get-design-cached-data` | `({ key })` | `{ success, data, timestamp, stale }` |
| `test-design-library-connection` | `({ serverId })` | `{ success, latency, toolCount }` |

### Bug Reports
| Channel | Args | Return |
|---------|------|--------|
| `bug-report:submit` | `({ projectId, title?, errorText })` | `{ success, id }` |
| `bug-report:list` | `({ projectId })` | `{ success, data[] }` |
| `bug-report:get` | `({ id })` | `{ success, data }` |
| `bug-report:auto-consult` | `({ problemId, problemTitle, problemDescription, projectId? })` | `{ success, bugReportId }` |
| `bug-report:investigate` | `({ bugReportId })` | `{ success, data }` |

### Model Improvement
| Channel | Args | Return |
|---------|------|--------|
| `get-model-improvement-stats` | `({ terminalId? })` | stats object |
| `set-reinject-threshold` | `({ threshold })` | `{ success }` |
| `set-model-debug` | `({ enabled })` | `{ success }` |
| `read-actions-error-log` | none | `{ entries[], exists }` |

### AI Briefs / Topics
| Channel | Args | Return |
|---------|------|--------|
| `get-topic-digest` | none | `{ success, topics[], error? }` |
| `get-interest-topics` | none | topic string array |
| `add-interest-topic` | `(topic)` | `{ success }` |
| `remove-interest-topic` | `(topic)` | `{ success }` |

---

## 2. SQLite Schema

All tables created in `src/main.ts` inside `initializeStorage()` (~lines 1900-2100).

```sql
CREATE TABLE IF NOT EXISTS logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  timestamp TEXT, app TEXT, category TEXT, duration_ms INTEGER,
  title TEXT, project TEXT, url TEXT, domain TEXT, tab_id TEXT,
  is_browser_tracking INTEGER DEFAULT 0
);

CREATE TABLE IF NOT EXISTS external_activities (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT, type TEXT, color TEXT, icon TEXT,
  default_duration INTEGER, is_default INTEGER DEFAULT 0,
  is_visible INTEGER DEFAULT 1, sort_order INTEGER
);

CREATE TABLE IF NOT EXISTS external_sessions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  activity_id INTEGER, started_at TEXT, ended_at TEXT,
  duration_seconds INTEGER,
  device_off_to_sleep_seconds INTEGER DEFAULT 0,
  wake_up_to_app_seconds INTEGER DEFAULT 0
);

CREATE TABLE IF NOT EXISTS external_settings (
  key TEXT PRIMARY KEY, value TEXT
);

CREATE TABLE IF NOT EXISTS projects (
  id TEXT PRIMARY KEY, name TEXT, path TEXT, repository_url TEXT,
  vcs_type TEXT, primary_language TEXT, default_ide TEXT,
  last_activity_at TEXT, deleted_at TEXT
);

CREATE TABLE IF NOT EXISTS ides (
  id TEXT PRIMARY KEY, name TEXT, version TEXT, install_path TEXT,
  detected_at TEXT, detection_method TEXT
);

CREATE TABLE IF NOT EXISTS tools (
  id TEXT PRIMARY KEY, name TEXT, category TEXT, version TEXT,
  install_path TEXT, detected_at TEXT, detection_method TEXT
);

CREATE TABLE IF NOT EXISTS project_tools (
  project_id TEXT, tool_id TEXT,
  PRIMARY KEY (project_id, tool_id)
);

CREATE TABLE IF NOT EXISTS commits (
  id TEXT PRIMARY KEY, project_id TEXT, sha TEXT, author TEXT,
  author_email TEXT, date TEXT, message TEXT,
  additions INTEGER, deletions INTEGER, files_changed INTEGER
);

CREATE TABLE IF NOT EXISTS ai_usage (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT, tool TEXT, project_id TEXT, project_path TEXT,
  model TEXT, input_tokens INTEGER, output_tokens INTEGER,
  cost_usd REAL, message_count INTEGER
);

CREATE TABLE IF NOT EXISTS terminal_layouts (
  id TEXT PRIMARY KEY, name TEXT, layout_data TEXT,
  project_id TEXT, is_active INTEGER, created_at TEXT, updated_at TEXT
);

CREATE TABLE IF NOT EXISTS terminal_presets (
  id TEXT PRIMARY KEY, name TEXT, command TEXT,
  project_id TEXT, working_directory TEXT, category TEXT,
  created_at TEXT, updated_at TEXT
);

CREATE TABLE IF NOT EXISTS terminal_sessions (
  id TEXT PRIMARY KEY, project_id TEXT, agent TEXT, resume_id TEXT,
  topic TEXT, working_directory TEXT, terminal_id TEXT,
  total_tokens INTEGER, total_cost REAL,
  category TEXT, status TEXT, product_area TEXT,
  description TEXT, auto_tags TEXT, category_confirmed INTEGER DEFAULT 0,
  subpage TEXT DEFAULT 'work/sessions',
  created_at TEXT, updated_at TEXT
);

CREATE TABLE IF NOT EXISTS terminal_messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT, role TEXT, content TEXT, status TEXT,
  created_at TEXT
);

CREATE TABLE IF NOT EXISTS terminal_bindings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  terminal_id TEXT, problem_id TEXT, session_context TEXT,
  status TEXT, created_at TEXT, last_activity_at TEXT
);

CREATE TABLE IF NOT EXISTS session_parsed_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT, item_type TEXT, content TEXT,
  source_message_id INTEGER, created_at TEXT
);

CREATE TABLE IF NOT EXISTS workspace_state (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id TEXT, name TEXT DEFAULT 'default',
  scope TEXT DEFAULT 'project',
  sidebar_width INTEGER DEFAULT 400,
  active_tab TEXT DEFAULT 'presets',
  terminal_tabs TEXT,
  state_json TEXT,
  is_active INTEGER DEFAULT 0,
  created_at TEXT, updated_at TEXT,
  UNIQUE(project_id, name)
);

CREATE TABLE IF NOT EXISTS productivity_sessions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  started_at TEXT, ended_at TEXT, duration_seconds INTEGER,
  app_name TEXT, category TEXT, is_streak INTEGER,
  day INTEGER, week_number INTEGER, month TEXT
);

CREATE TABLE IF NOT EXISTS ai_briefs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  type TEXT, date TEXT, content TEXT,
  model_used TEXT, tokens_used INTEGER, created_at TEXT
);

CREATE TABLE IF NOT EXISTS ai_interests (
  topic TEXT PRIMARY KEY, enabled INTEGER DEFAULT 1, created_at TEXT
);

CREATE TABLE IF NOT EXISTS ai_feature_usage (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT, feature TEXT, model TEXT,
  input_tokens INTEGER, output_tokens INTEGER,
  cost_usd REAL, success INTEGER, created_at TEXT
);

CREATE TABLE IF NOT EXISTS goals (
  id TEXT PRIMARY KEY, date TEXT, title TEXT, description TEXT,
  category TEXT, target_type TEXT, target_seconds INTEGER,
  match_category TEXT, status TEXT, period TEXT, source TEXT,
  links TEXT, progress_seconds INTEGER, completed_at TEXT,
  created_at TEXT
);

CREATE TABLE IF NOT EXISTS goal_reviews (
  date TEXT PRIMARY KEY, review_summary TEXT,
  suggestions TEXT, created_at TEXT
);

CREATE TABLE IF NOT EXISTS routing_costs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  call_type TEXT, model TEXT, input_tokens INTEGER,
  output_tokens INTEGER, cost_usd REAL,
  session_id TEXT, prompt_preview TEXT,
  timestamp TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS dora_metrics (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id TEXT, period TEXT,
  deployment_frequency REAL, lead_time_hours REAL,
  change_failure_rate REAL, mean_time_to_recovery_hours REAL,
  level TEXT, commit_count INTEGER, total_lines INTEGER,
  active_days INTEGER, date TEXT
);

CREATE TABLE IF NOT EXISTS activity_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  entity_type TEXT, entity_id TEXT, entity_title TEXT,
  action TEXT, actor TEXT, summary TEXT, details TEXT,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS bug_reports (
  id TEXT PRIMARY KEY, project_id TEXT, title TEXT,
  error_text TEXT, status TEXT DEFAULT 'pending',
  agent_responses TEXT, linked_problem_id TEXT,
  flow_type TEXT DEFAULT 'manual', root_cause_report TEXT,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS touched_files (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  terminal_id TEXT, session_id TEXT, file_path TEXT,
  action TEXT, created_at TEXT DEFAULT CURRENT_TIMESTAMP
);
```

### Problems/Sessions/Features/Settings Tables

| Table | Purpose |
|-------|---------|
| `terminal_sessions` | AI agent sessions — topic, agent, cost, tokens, category, status, description, auto_tags |
| `terminal_messages` | Chat messages per session — role, content, status (in_progress/completed/failed) |
| `terminal_bindings` | Terminal-to-problem linking — problem_id, session_context, status |
| `session_parsed_items` | Parsed decisions/action_items/status_changes from AI output |
| `workspace_state` | Saved workspace snapshots — layout, tabs, sidebar width, theme, configs |
| `productivity_sessions` | Focus sessions — duration, app_name, category, streak tracking |
| `goals` / `goal_reviews` | Daily goals and evening reviews |
| `ai_briefs` | Cached AI-generated briefs (daily brief, weekly review, topic digest) |
| `ai_feature_usage` | Per-feature AI cost/token tracking |
| `routing_costs` | Auto-assign routing LLM call costs |
| `activity_log` | Audit trail for all CRUD on problems/requests/checklists |
| `bug_reports` | Collaborative debugging reports with agent responses |
| `touched_files` | Cross-session file edit tracking for conflict detection |

**Note:** Problems and Requests are NOT stored in SQLite — they use JSON files as source of truth (`agent/problems.json`, `agent/requests.json`). The SQLite `workspace_problems` and `workspace_requests` tables exist but are secondary/legacy.

---

## 3. JSON File Shapes

### `agent/problems.json`
```json
[
  {
    "id": "96",
    "title": "Terminal data not displaying",
    "priority": "high",
    "category": "bug-fix",
    "status": "Fixed",
    "description": "Terminal shows only 'Starting shell...' — PTY data never reaches xterm",
    "user_notes": "Reproduced on Windows 11",
    "terminal_id": "term-1716400000-abc123",
    "session_id": "session-1716400000",
    "session_name": "Fix PTY data flow",
    "checks": [
      { "id": "96-check-1", "description": "Verify IPC handler fires", "status": "completed", "instruction": "Add console.log in data handler" },
      { "id": "96-check-2", "description": "Check preload bridge", "status": "completed", "instruction": "Verify terminalAPI.onData callback" }
    ],
    "linked_requests": ["req-12"],
    "created_at": "2026-05-12T10:00:00Z",
    "updated_at": "2026-05-12T14:30:00Z"
  }
]
```

### `agent/requests.json`
```json
[
  {
    "id": "req-12",
    "title": "Add terminal split drag resize",
    "priority": "Medium",
    "category": "Feature",
    "status": "In Progress",
    "description": "Users should be able to drag split dividers to resize terminal panes",
    "checks": [
      { "id": "req-12-check-1", "description": "Implement drag handler", "status": "completed" }
    ],
    "linked_problems": ["96"],
    "session_id": "session-1716400000",
    "created_at": "2026-05-12T10:00:00Z"
  }
]
```

### `agent/checklists.json`
**Not present as a standalone file.** Checklists are embedded inside `problems.json` and `requests.json` as the `checks` array on each item.

### `agent/actions.json`
**Not present as a persistent file.** `actions.json` is a transient write target — AI agents write actions to it, the main process reads and executes them, then clears the file back to `{ "actions": [] }`. It lives at `<projectPath>/agent/actions.json`.

Sample (transient):
```json
{
  "terminal_id": "term-123",
  "actions": [
    { "type": "create_problem", "title": "New bug found", "priority": "high", "category": "bug-fix", "description": "Something broke" },
    { "type": "update_problem", "id": "96", "status": "In Progress" }
  ]
}
```

### `agent/ai-tasks.json`
**Not present.** AI task tracking is done via the `terminal_messages` table (status column: `in_progress`, `completed`, `failed`) and the in-memory `pendingCompletions` Set. No JSON file.

### `agent/state.md` (not JSON — markdown)
**File:** `agent/state.md`
```markdown
# 📌 Project State

> **Version:** 4.118
> **Last Updated:** 2026-06-21
> **Agent:** opencode
> **Current Focus:** Terminal Map gate verification (Gates A/B/C)

### 2026-06-21 — Fix TDZ crash + verify Terminal Map/Drag/Fit
...
```

### `agent/saved-skills.json`
```json
["generate-prompt", "frontend-design", "fix-problems"]
```

### `planning.md`
**File:** `%APPDATA%/DeskFlow/planning.md` — user-editable markdown plan. Read/written via `read-planning-md` / `write-planning-md` IPC.

---

## 4. Knowledge Systems

### Graphify
- **Data location:** `<projectPath>/graphify-out/graph.json`
- **Format:** JSON — `{ nodes: [{id, label, type, ...}], edges: [{source, target, label}], communities: [...] }`
- **CLI/Tool:** `graphify_maintain.py` in `agent/skills/maintain-context/` — has `full` and `para` commands. Also has a `graphify` skill at `agent/skills/graphify/SKILL.md`.
- **Status:** ✅ Active. IPC `get-context-systems` reports node/edge count + lastBuilt mtime.

### LLM Wiki
- **Data location:** `<projectPath>/agent/*.md` — all markdown files in agent/ directory (AGENTS.md, context.md, constraints.md, patterns.md, data.md, debugging.md, etc.)
- **Format:** Markdown files with optional YAML frontmatter
- **CLI/Tool:** No dedicated CLI. Files are created/read via `runInitAll()` in main.ts (~line 15941). `get-context-systems` counts `.md` files.
- **Status:** ✅ Active. 30+ markdown files scaffolded on init.

### Obsidian Skills
- **Data location:** `<projectPath>/agent/skills/<skill-name>/SKILL.md` (directory-based) or `<projectPath>/agent/skills/<name>.md` (flat files)
- **Format:** Markdown with YAML frontmatter (`id`, `name`, `description`, `category`, `version`, `sidebar`, `workspace`)
- **CLI/Tool:** `SkillsService` class in `src/services/SkillsService.cjs` — reads/parses skills. IPC: `get-skills`, `create-skill`, `update-skill`, `delete-skill`, `add-skill-to-project`, `seed-workspace-skills`.
- **Status:** ✅ Active. 18 skill directories pre-scaffolded.

### PARA
- **Data location:** `<projectPath>/CZVault/` — directories `01_Areas/`, `02_Resources/`, `03_Archives/`, `00_Projects/`
- **Format:** Markdown files (Obsidian vault format)
- **CLI/Tool:** `graphify_maintain.py` has `sync_to_para()` function and `para` command. `get-context-systems` counts subdirectories + newest `.md` mtime.
- **Status:** ✅ Active. Directories created on init.

### QMD Templates
- **Data location:** `<projectPath>/agent/templates/*.qmd`
- **Format:** `.qmd` files (Quarto Markdown templates for sessions and problems)
- **CLI/Tool:** No dedicated CLI. Files are created by `runInitAll()`. `get-context-systems` counts `.qmd` files.
- **Status:** ✅ Active. `session.qmd` and `problem.qmd` scaffolded.

### Automations
- **Data location:** `<projectPath>/agent/automations/automations.json`
- **Format:** JSON array or object with `automations`/`rules` keys
- **CLI/Tool:** No dedicated CLI. Directory + file scaffolded by `runInitAll()`. `get-context-systems` reads file mtime + item count.
- **Status:** ✅ Active (directory + file exist), but no automation engine implemented yet.

---

## 5. SettingsPage "Prompts" Tab — Storage & Loading

**File:** `src/pages/SettingsPage.tsx`

### Storage
Prompts are stored in `userPreferences` (JSON file at `%APPDATA%/DeskFlow/deskflow-prefs.json`) under keys:
- `prompt-templates-global` — global prompt templates (array)
- `prompt-templates-{projectId}` — per-project prompt templates (array)

Each template object:
```json
{
  "id": "tpl-1716400000-abc123",
  "name": "My Prompt",
  "content": "Do X and Y...",
  "category": "general",
  "isFormattingTemplate": false,
  "projectId": null,
  "createdAt": "2026-06-01T10:00:00Z",
  "updatedAt": "2026-06-01T10:00:00Z"
}
```

### Loading
- `get-prompt-templates` IPC handler (main.ts ~line 15219) reads from `userPreferences[key]`
- `save-prompt-template` IPC handler (main.ts ~line 15229) upserts into `userPreferences[key]` then calls `savePreferences()`
- `delete-prompt-template` IPC handler (main.ts ~line 15249) iterates all `prompt-templates-*` keys and removes matching ID

### SettingsPage UI
- Tab 5 ("Prompts") in SettingsPage renders a list of templates with category filtering
- Uses `getPromptTemplates(projectId)` preload bridge on mount
- Save/Delete calls go through IPC
- Also has a "System Prompts" sub-tab (within the Prompts tab) showing per-agent system prompt editors (claude, opencode, custom) with 4-level merge preview (default + general additions + project additions + session additions)

---

## 6. Markdown Renderer & Theme System

### Markdown Renderer
**File:** `src/components/AiChat/blocks/TextBlock.tsx` — uses `parseBlocks` + `BlockRenderer` pipeline
**File:** `src/services/parseBlocks.ts` — dual parser (V2 `>>type` sigil vs legacy `[type: x]` DSL)
**File:** `src/components/MarkdownPreview.tsx` — regex-based markdown renderer used in AiPage for plan cards
**File:** `src/components/BasicMarkdownViewer.tsx` — basic markdown viewer (headers, bold/italic, code, blocks, lists, blockquotes, links)

The app does NOT use a full markdown library (no `react-markdown`, `marked`, `remark`). All rendering is custom regex-based.

### Theme System
**File:** `src/index.css` — Tailwind v4 `@theme` block with CSS custom properties:
```css
@theme {
  --color-clay-300: ...;
  --color-clay-400: ...;
  --color-clay-500: ...;
  --color-clay-600: ...;
  --color-sage-400: ...;
  --color-amber-400: ...;
  --color-sky-400: ...;
  --font-serif: ...;
  --font-mono: ...;
}
```

**File:** `src/components/GlassCard.tsx` — accepts `accent` prop (`'pink' | 'amber' | 'none'`), renders top accent stripe + glass-morphism container.

**File:** `src/pages/SettingsPage.tsx` — Color customization tab (Tab 2) allows per-app color overrides stored in `userPreferences`.

**File:** `src/components/finance/` — Finance pages have their own glass-card theme with accent rails (emerald for accounts, amber for custodial).

**No formal theme provider/context.** Colors are hardcoded Tailwind classes (`zinc-950`, `zinc-800`, `emerald-500`, etc.) with a few CSS custom properties in `@theme`. The app is dark-mode only — no light mode toggle exists.

---

## File Reference Index

| File | Lines | Purpose |
|------|-------|---------|
| `src/main.ts` | 19,156 | All IPC handlers, DB init, tracking, terminal management |
| `src/preload.ts` | ~350 | All preload bridges (ipcRenderer.invoke wrappers) |
| `src/App.tsx` | ~2500 | Router, sidebar, global state, deskflowAPI type declarations |
| `src/pages/SettingsPage.tsx` | ~1800 | Settings UI (5 tabs: Category/Colors/General/Tracking/Prompts) |
| `src/pages/TerminalPage.tsx` | ~4900 | Terminal workspace with 5-group sidebar |
| `src/pages/DashboardPage.tsx` | ~2400 | Dashboard with orbit, heatmap, charts |
| `src/pages/ExternalPage.tsx` | ~2500 | External activity tracking, sleep, comparison |
| `src/pages/IDEProjectsPage.tsx` | ~3271 | IDE projects, workspace, analytics |
| `src/services/ProblemsService.cjs` | ~300 | JSON-based problem CRUD |
| `src/services/RequestsService.cjs` | ~300 | JSON-based request CRUD |
| `src/services/SkillsService.cjs` | ~200 | Skill file parsing |
| `src/services/AIService.cjs` | ~400 | AI provider calls |
| `src/services/providers/router.cjs` | ~200 | Multi-provider fallback chain |
| `src/services/providers/templates.cjs` | ~100 | Provider templates |
| `src/services/providers/callProvider.cjs` | ~150 | Universal API caller |
| `src/services/parseBlocks.ts` | ~200 | DBN2 wire format parser |
| `src/services/wireFormat.ts` | ~100 | Wire format types |
| `src/services/planningParser.ts` | ~50 | Checklist markdown parser |
| `src/services/GoalStore.ts` | ~150 | localStorage goal persistence |
| `agent/problems.json` | varies | Problem source of truth |
| `agent/requests.json` | varies | Request source of truth |
| `agent/state.md` | varies | Project state changelog |
| `agent/saved-skills.json` | small | Saved skill IDs |
| `planning.md` | varies | User planning document |
