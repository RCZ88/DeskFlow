Here's an idea of concept that I would like you to look on and expand upon so as you know I have a app a tracker app which I have planned to expand into the AI agent world where I found some problems with the AI agents in the CLI especially with using the terminal where if you have multiple incidents and you do if you're seriously doing something there's a lot of things that you need to do and there's a lot of features that it needs to be fixed and stuff like that you're going to have multiple instances of AA agent and multiple terminals now the problem with this is that you get confused on which chat is which you forget which terminal what are you doing that specific terminal so the idea arises work this software of tracker mind which has the ability to write code and stuff like that basically open a terminal on itself and like having the interface sort of represent and do both of the works and basically the idea that I have is that the interface should provide a clear path on which is this terminal discussing about or what is the content of chatters being discussed in this terminal so I should be able to select the terminal easily I should be able to select a chap easily that's the first concept so there's like a instead of just a chat title or it should show the contents a use this some sort of agenda you can we can utilize the open code agent by giving an agent that I'm need and prompting them to update a certain market for a certain JC file something that is possible by the system by the application in which it would show that chat what what is it that we're discussing what is the latest day and stuff like that that's the first thing right It should also be able to like show the list of cats easily the list of messages and put it into that session right and also that's the first feature right 2nd feature is that you see there's a lot of markdown files in the ages dot MD that help manage the agents and so that the agents can work properly and organize and there's a lot of things marked on file so just the problems that mark down there's the state that mark down and there's a lot of things there's like the user checklist which I create there's also the dictionary dot md which is state of London context dot md a lot of files but the focus I have the idea I had was the problems at MD we can mention a lot of our problems in it should go to like the problem if the part if the user input is a problem that to be fixed it should be inserted into the problems that I need and right now the the AI already has the ability and the knowledge from the AI agents that I need that it should update the problems that I need properly by stating it's like AI ordered it but it requires user approval so the system the app should be able to parse the problems that I need maybe we can move it into certain other format that can be parsed properly and basically we re utilizing the I capability to write an updated state alright and be able to use that to capitalize on that on the application itself by the application reading what has been written by the agent so the app should be able to have the ability to list all the problems and the user should be just able to select something and for example the user want to test this problem and the user can put something like there should be like the option of like select whether it's complete it's something problem and the user can strive in the UI interface and by entering that and selecting a problem and entering the result of the problem the system the application should instantly or like be able to allocate those into either a specific terminal or a better idea would be that if not the terminal matches the theme of what the problem is maybe like it's a different page or like it doesn't match with the existing terminal chats it should create a new one for example how it would do that I don't know but if it's under the chart it's fine but make sure that the context is managed properly so basically we're creating a UI so that the insertion of a certain thing a certain problem a certain request a certain additional feature is not longer manual and you don't have to find where you put it it's a live quality of life feature where everything is UI based and you can just everything is organized neatly and everything is managed properly so there's some sort of report system So you know what are the things that the air has done and what are the things that you should check whether there is that everything you should be need everything should display feedback properly today to the user because a lot of times the edges output text that is not clear and what it has done and what to check and stuff like that and there's a lot of skill so that the IT should be that I am able to use the scale properly so for example when constructing something it should be able to have the user the ability to for example use the skill select a skill to use on this problem and it's either that the prop should be as simple as doing this telling the agent to do the problem by selecting your problem in incentives as a problem would already the app we can set up some sort of default system problem or like system problem for agents to do the problem not only that we're giving the problem and what to refer to but also a system problem and it goes for a lot of stuff like the request of MD which is the file that is responsible for managing all the user requests that means an addition of a feature removal of a feature what has been implemented and stuff like that The idea is that the current state is that we have the open workspace this is the workspace part where there's like a terminal and I would like to the the final product the actual terminal itself can be small part it can just be a minimal part that's showing that it actually works but it actually works right but the main thing should be that the application that what is it called the open workspace the workspace should be the main thing right the UI thing should be to make content of the application where to control out everything is control from that UI and everything should be controlled from the UI properties it can be vanished neatly and can make the AI coding experience better for anyone and I think this is a really good idea and something that is new and not yet solved there's no apps to do this so I'm gonna be the 1st one so I would like you to refine the idea and I will give you like some sort of context of like how my work flow is there's like a lot of stayed at the markdown files that are in my setup and there's something like my initial markdown which is to initialize the AI with the graphite skills stuff like that and should be able to manage the token costs and stuff like that and I will give you the state of MD of the current project and I will give you some more details so that you understand the context of the project. heres the list of skills i currently have. and heres the list of markdown files that is related. if you have any questions feel free to ask. again you r goal is to enahnce this idea and make it come to life with realideas that can be help ful, the real concrete way to do my plans, refine the plans. the workspace context markdown file is the context of whats ucrrently in the app, but i do think most of them arent workping properly yet. 