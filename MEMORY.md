# MEMORY.md — DeskFlow Durable Memory

> Auto-loaded into every opencode prompt via opencode.json "instructions".
> This is long-term memory. Append durable lessons (Section rules in AGENTS.md #4).
> Format: `- [YYYY-MM-DD] <one-line durable lesson>`

- [2026-08-06] INDEX.CSS THEME GAP (root cause of "transparent popup/overlay" bugs): `src/index.css` @theme defined NO shadcn semantic color tokens (`--color-popover`, `--color-primary`, `--color-background`, `--color-foreground`, `--color-border`, `--color-muted`, `--color-muted-foreground`, `--color-ring`, `--color-card`, etc.) — Tailwind v4 only emits utilities for DEFINED theme keys, so `bg-popover`, `bg-primary`, `text-popover-foreground`, `border-border`, `bg-background`, `text-foreground` compiled to NOTHING → every Sheet/Dialog/DropdownMenu/Popover/Alert/Accordion/Calendar + Button variant="default" rendered TRANSPARENT app-wide. FIXED 2026-08-06: added a second `@theme` block after the first in index.css line 1 (values: background #09090b, foreground #fafafa, card/popover #18181b, primary #fbbf24 amber + primary-foreground #09090b, secondary/muted/accent #27272a, muted-foreground #a1a1aa, destructive #ef4444, border/input #27272a, ring #fbbf24) + explicit glass re-skins (bg-zinc-900/95 backdrop-blur-xl border-zinc-800/60) on sheet/dialog/dropdown-menu. LESSON: ANY new ui primitive must have its token classes verified in compiled dist CSS (grep `.bg-popover` etc.) — a green vite build proves NOTHING about undefined theme tokens. Also: NO tw-animate plugin — `animate-in`/`fade-in-0`/`zoom-in-95`/`slide-in-from-*` classes are dead no-ops; use base-ui's own data-open states.
- [2026-08-06] Canvas drag "stuck in app but works in harness" root cause chain: (1) the running RHEO prod server (port 58062, owned by RHEO main PID — NOT a legacy YAP-A-TrON instance; 54321 = browser-tracking server, 8788 = websocket/426 — ALL RHEO's own ports) serves dist/ per-request, so a stale PROCESS still serves NEW files; the killer is the stale RENDERER session: after any rebuild (emptyOutDir), old entry/chunk hashes (e.g. index.BlMX7Qcz.js) are DELETED, and the already-running renderer (loaded old index.html) still lazy-loads OLD chunk names → 404s / "Failed to fetch dynamically imported module" → canvas never mounts. Fix = FULL close of ALL RHEO processes + relaunch (RHEO has no single-instance lock, so straggler windows keep the old session alive). Chunk CSS hashes (canvas.CzzlepXn.css, CanvasGrid.BJH0GxYV.css) are CONTENT-hashes and stay stable across builds — a 404 on a CURRENT CSS hash mid-session means the build was mid-flight when the lazy chunk loaded (files not yet written), not a server bug. Verify app freshness by comparing RHEO main StartTime vs dist/index.html LastWriteTime.
- [2026-08-06] OrbitSystem solar-system ENTRY camera invariant (FIXED, supersedes "must use same offset as scene"): SolarSystemScene renders at ORIGIN with no offset — ALL solar-system camera entry paths (handleEnterSystem, handleCategorySelect, planet clicks, focusOnPlanet) must target (0,0,0); x=3250 is ONLY for galaxy view (switchToGalaxy/handleZoomOut/initial camera). Camera → origin while scene at origin; the websites galaxy dust stays at 3250 but is unmounted in solarSystem view.
- [2026-08-06] drei Html + React state = label blur invariant: per-frame `setCameraDist(dist)` in useFrame re-renders every planet every frame → blurry/flickering labels (React style recalc + fractional CSS transform scale). Correct pattern: write `el.style.opacity`/`transform` directly on a div ref inside useFrame (guarded by a `dataset.scale` change check), never per-frame setState. Also `backdrop-filter: blur(8px)` on hundreds of Html labels kills text crispness — use solid/translucent rgba background instead. Galaxy-label scale formula: `220 / cameraDistance` clamped [0.6, 1.8] (default galaxy cam distance ≈ 224 → ≈1.0); key label refs by galaxy scope (`${scope}__${category}`) since apps & websites galaxies render simultaneously with overlapping category names.
- [2026-08-06] `scripts/zip-src.mjs` (Compress-Archive of src+scripts+agent) can leave a CORRUPT partial `dist/src.zip` if killed/locked mid-run — agent/ alone is 1GB/21,938 files, so the full zip ≈ 750MB and takes 5-10 min; a blocking run with a short timeout ALWAYS dies mid-write. A corrupt/locked src.zip in dist/ then BREAKS the next `vite build` (emptyOutDir rmSync EPERM). Recovery: delete dist/src.zip (derived artifact — safe), rebuild, then re-zip DETACHED (`Start-Process powershell ... -PassThru` + poll `$ps.HasExited`, log tail shows 'DONE'), then verify by opening the zip and checking root entries (main.ts/preload.ts/index.css/App.tsx present). src.zip intentionally contains only src/, scripts/, agent/ — project-root files (index.html) are NEVER in it. FASTER ALTERNATIVE (2026-08-06, supersedes): `tar -a -c -f dist/src.zip --exclude="agent/backups" --exclude="agent/docs" --exclude="agent/node_modules" src scripts agent` → 10MB in ~30s instead of 750MB/10min; excludes are exactly backups (710MB) + docs (374MB).
- [2026-08-06] OrbitSystem fix-5 MSAA/antialias: Canvas `antialias` + EffectComposer `multisampling` are only applied on Canvas REMOUNT — the canvas `key` must include `perfMode` AND the existing `textureRefreshKey` (dropping the latter breaks the Refresh textures button). PerformanceMonitor auto-degrade to a lower quality remounts the canvas — acceptable per spec but watch for FPS-drop loops.
- [2026-08-06] problems.json IDs are NOT unique across sessions — pre-existing 171/172/173 existed when adding this round's entries; always compute `max(existing [int]id) + 1` and re-verify duplicates after write (PowerShell 5.1 gotcha: `"175" + 1` = "1751" string concat — cast [int] first).
- [2026-08-05] Chart-card heading font decision (Stats/Browser/External): display font = Space Grotesk via `--font-display` token in index.css @theme + Google Fonts link in index.html (body stays Inter); `.subtle-aurora` utility in index.css = pure-CSS page-accent wash (radial gradients with `color-mix(in srgb, var(--page-accent) 10%/6%, transparent)`, no deps — beats ReactBits Aurora which needs uninstalled `ogl`). `--page-accent` is mapped per page in index.css via `[data-page="stats"] #22d3ee` / `[data-page="browser"] #38bdf8` / `[data-page="external"] #fbbf24` (PageShell sets data-page). SectionHeader accepts `titleClassName` (appended to h2) so only opted-in headers get the display font. Google Fonts design MCP `search_fonts` needs full category path identifiers (e.g. "/Sans/Neo Grotesque"), not short names like "SANS_SERIF".
- [2026-08-06] AI Tools heatmap root cause (AIToolsTab): the "All tools" heatmap filtered `aiAgents` by status/tokens ONLY and read daily data from `workspaceAnalytics.byTool` — which NEVER has `daily` (only the overview's aiUsage.byTool does), so the heatmap silently rendered NOTHING. Fix invariant: heatmap agents MUST come from the overview's `ovByTool` (require non-empty `daily` object), and any no-data case must render a semantic empty state (heatmapEmpty), never `return null`. Also: the heatmap chip filter state is `heatmapToolFilter/setToolFilter` at AIToolsTab.tsx:280 (still live, used at 1973/2100/2102).
- [2026-08-06] Code Changes analytics data flow: `get-code-change-stats` IPC (main.ts:10438) → preload `getCodeChangeStats` (preload.ts:310) → AnalyticsDashboard `codeStats` prop (CodeChanges section after AI Usage Summary). `get-ide-projects-overview` (main.ts:10211) does NOT return code-change stats; daily AI usage in its `byTool` is populated for ALL periods including 'all'. Terminal workspace analytics lives in TerminalPage's insights WorkspaceShell group (~line 3917) — 'code-stats' tab there is CodeStatsTab (LOC scanner), NOT commit stats.
- [2026-08-06] Mojibake repair layering: deep repair (`repairJsonInPlace` with timestamped `.bak-*` backup + write-back) lives in ProblemsService/RequestsService read paths; render-time `sanitizeMojibake` (src/lib/sanitize.ts) is the last line of defense in ProblemsTab/IssuesWorkspace renders. badges.tsx renders enum map labels only — no free text, no sanitize needed there.

- [2026-08-05] Sleep-data handlers in main.ts cap total duration at 24h (NOT 16h — legit full-day spans must never truncate); `get-sleep-for-date` matches `date(started_at)` OR `date(ended_at)` OR `date(started_at)=date(?, '+1 day')` (bedtime / wake / chart-grouped evening date); `fix-sleep-dates` PROBLEM 1 is FORWARD-ONLY repair (pre-6AM start + cross-calendar-day span ≥ 18h → shift started_at +1 day, recompute duration) — it must NEVER back-shift normal post-midnight sleeps to the previous day.
- [2026-08-05] External mosaic hover sparkline invariant (user requirement): EVERY card tier with 7-day session data must show the trend chart on hover — gate is `height >= 80 && sparklineValues.length > 0` with per-tier heights h-12/h-9/h-7/h-5; only no-data cards are exempt. Never reintroduce tier-based or 160px-height exclusions for medium/small cards.
- [2026-08-04] `dist/` is TRANSIENTLY EMPTY during `node scripts/build.mjs` Step 1: `npx vite build` + vite.config `emptyOutDir:true` empties `dist/` before writing the new bundle. Do NOT panic-check `dist/index.html` mid-build — it is stable (288 assets) once build completes. Also: `purpose` is TYPE-ONLY in preload.ts (stripped by esbuild) — its absence from compiled preload.cjs is EXPECTED; it only appears in main.cjs IPC payloads and the renderer bundle. Verify routing features via main.cjs handler + `aiRun`/`onProviderChunk` strings in `dist/assets/index.<hash>.js`, NOT via preload.cjs.
- [2026-08-05] RHEO automations: DSL for compositions is generated AI-side by `dslGenerator.ts` using symbols + word-operator grammar (`=`, `!=`, `>`, `>=`, `<`, `<=`, `contains`, `matches`, `exists`, `not_exists`) with action params in `action:key = value, ...` form — all 14 word-op tokens added to lexer/parser/engine (tokens placed BEFORE IDENTIFIER pattern; `exists`/`not_exists` are unary, no RHS consumed).
- [2026-08-05] The CompositionEngine's `compositions:validate` chain (lex→parse→scopeCheck) accepts the builder's generated DSL natively; scopeCheck only WARNS on unknown actions (`goal:create`/`schedule:add`/`email:send` warn, never block validity). `validateDsl` in useAutomationActions falls back `{valid:true}` when bridge missing. `nlParser.ts` pre-existed with `parseNlAutomation` (fenced ```automation JSON blocks) + `stripAutomationBlock` — must strip automation blocks from ALL DSL render paths in AiPage (canvas streaming, prose, deck).
- [2026-08-05] RHEO runtime verification: the ONLY debug port on the machine (9222) belongs to a Lenovo Vantage widget, NOT RHEO. RHEO runs without --remote-debugging-port; do NOT attach to 9222 expecting DeskFlow. Never kill user's electron processes; ask CZ to relaunch with the debug flag instead.
- [2026-08-04] An unrelated Electron app (YAP-A-TrON) runs from `C:\Users\cleme\PycharmProjects\YAP-A-TrON\node_modules\electron` and may be running concurrently while working in the App Tracker; its processes must never be killed or mistaken for the App Tracker's own app. If a running electron.exe appears under that path, RHEO itself is NOT running → runtime verification must be marked NOT LAUNCHED (never launch manually, per RHEO single-instance/db-lock rule).
- [2026-08-02] Browser-tracking "clear" contract: main emits `browser-tracking-event {type:'clear'}` via `notifyRendererClearBrowser()` whenever extension website data is rejected (currentApp null/non-browser mismatch, extension not focused, or fresh `active-win` check showing a non-browser foreground). Renderers (DashboardPage, App.tsx) must treat it as authoritative for exiting website mode. `freshForegroundIsBrowser(browsersList)` returns true/false/null (null = trust cache; DeskFlow/Electron windows count as non-browser). Guard lesson: any `currentApp === null` guard in `/browser-data` must defer to a fresh OS check, never blanket-accept or blanket-reject (blanket-reject drops legit startup browser data during the null-poll window).
- [2026-08-04] StateCoordinator fix (do NOT regress): `src/main/stateCoordinator.ts` MUST be a static `import { StateCoordinator } from "./main/stateCoordinator"` at the top of src/main.ts — the old `require('./stateCoordinator')` at app-ready threw MODULE_NOT_FOUND (no compiled stateCoordinator.js exists in dist-electron) and was silently swallowed, so agent/state.md was NEVER auto-generated (the file on disk was a hand-edited artifact). Static import makes Vite bundle it (main.cjs 59→60 modules). `agent/state.md` is READ-ONLY, auto-generated from `agent/state/*.md` spokes; agents write ONLY their own spoke (complete overwrite, 3 cycles max, `<!-- SESSION: -->` + `<!-- AGENT: ... | TERMINAL: ... -->` + `## CURRENT CYCLE (n)` markers must stay). Session ID: `DESKFLOW_SESSION_ID` env var → spoke filename match → copy of `agent/state/_template.md`.
- [2026-08-04] StateCoordinator runtime wiring: bundled into main.cjs now; the app regenerates the hub at app.whenReady() via StateCoordinator.initialize() (writes state.md.tmp then renames; leftover tmps auto-cleaned by cleanupTmpFiles). Verify hub regeneration by `rg "FIND YOUR SPOKE" dist-electron/main.cjs` = 1 after rebuild (marker of the PROTOCOL v2 text).
- [2026-06-28] Gap fill prediction: backend IPC handlers (predict-gap-fill, confirm-gap-fill, predict-day-gaps) need matching preload bindings + a GapPredictorPanel that replaces GapPanel in App.tsx wiring
- [2026-06-28] Typical Day chart tooltip: position right-side (translateY(-50%)) for tight grid layouts, not top-above; hour labels should show "12 AM", "6 AM", "12 PM", "6 PM" format; label visibility every 6th hour for readability
> Transient/current state does NOT go here — it goes in agent/state.md.

## PROMPT GENERATION (never fabricate quotes)
- [2026-06-28] The Raw Request block in prompt.md MUST contain the user's EXACT verbatim words. Never paraphrase, reconstruct, or fabricate quotes. If the session context doesn't contain the exact words, use an honest attribution header like "(Request compiled from cross-session context)" instead of faking a quote. Violating this violates the generate-prompt skill's #1 rule and wastes Architect AI cycles on wrong framing.
- [2026-06-28] REINFORCEMENT — "Compiled from cross-session context" is STILL not the user's exact words. If you don't have the verbatim source, ASK THE USER for the exact text instead of writing anything in the Raw Request block. Never write a paraphrase under any header. The Raw Request must be the user's unmodified utterance or nothing.

## PIPELINE ROLE (never forget)
- [2026-06-28] Lyceum Learn module Phase 1 complete — 10 block types rendered, Beta-Bernoulli mastery estimator, idempotent import, keyword-based grounding (no sqlite-vec yet), tutor with cache + scope gate. Full stack: schema → validator → DB → services → IPC → preload → React components → LearnPage.
- [2026-06-26] NetWorthLineChart must self-register Chart.js components (CategoryScale, LinearScale, PointElement, LineElement, Tooltip, Filler) instead of relying on a parent module's module-level registration — bundler tree-shaking can break the dependency.
- [2026-06-21] This is a two-AI relay pipeline. Architect (Notion AI) patches + ships ZIP + FIX PACKET; I (opencode) build, verify in the real app, and reply in CYCLE REPORT format. CZ relays. NEVER treat a session as standalone.
- [2026-06-21] On startup ALWAYS read agent/state.md to recover cycle # and open packet before doing anything. "What did we do so far?" is answered from state.md + this file, NOT by asking CZ.
- [2026-06-21] After ANY source change, RE-ZIP src and hand it back. Stale src.zip caused multiple false "your fix doesn't work" reports.

## RESPONSE FORMAT (corrected repeatedly — stop forgetting)
- [2026-06-21] Final responses MUST use the CYCLE REPORT format in AGENTS.md #3. Do not freeform. CZ corrected this 4+ times; the cost of forgetting is high.

## TESTING (avoid false PASS)
- [2026-06-21] IPC probe success ≠ feature works. Verdict PASS needs the actual layer: UI = real clicks (no programmatic React input — onChange won't fire), Terminal = read [TERMINAL_DEBUG] C2 data callback FIRED logs.

## BUILD / DEBUG GOTCHAS
- [2026-06-28] Build script (scripts/build.mjs) only compiles .ts files — non-TS runtime files like .sql migrations don't get copied to dist-electron. Added explicit `nonTsDirs` array after Step 3 to copy migration files. Any new .sql/JSON runtime files in services/ need an entry there.
- [2026-06-29] IDE Projects page: project list after adding projects requires `loadOverview` to use the same query path as mount — the setTimeout in the 'all' path was causing the function to return before `setOverview` was called. Removed it. Also removed `LIMIT 10` from the projects query so all projects always show.
- [2026-06-21] All-data-shows-0 → preload.cjs was not rebuilt. Rebuild preload separately with esbuild after build.mjs.
- [2026-06-29] `get-ide-projects-overview` query was missing `WHERE deleted_at IS NULL` — soft-deleted projects remained visible in the UI. Always check all SELECT queries for deleted_at filtering when adding soft-delete support.
- [2026-06-29] AI sync bug: `sync-ai-usage` IPC handler fire-and-forget via IIFE, returned fake `{ success: true, started: true }`. Renderer dismissed modal and loaded stale data before real sync finished. Fix: await `syncAllAIAgents` directly, throttle progress events at 100ms to prevent renderer freeze.
- [2026-06-29] After DB wipe, `syncAllAIAgents` skips files because mtime cache still matches. Fix: check if `ai_usage` table is empty before sync and invalidate cache.
- [2026-06-21] "Cannot access 'z' before initialization" = TDZ / circular import.
- [2026-06-21] "symbol X already declared" = duplicate const.
- [2026-06-21] Exit code -1073741510 = STATUS_CONTROL_C_EXIT (Ctrl-C / terminated), not a code bug.
- [2026-06-21] PTY event order is sacred: mark-spawned → spawn → created → initialize. Never reorder.

## CODEBASE FACTS
- [2026-06-21] Workspace persistence backend is COMPLETE (workspace:save/load/list/delete in main.ts; saveWorkspace/loadWorkspace/listWorkspaces/deleteWorkspace in preload). UI surfaces it via the Work group "Workspaces" subtab.
- [2026-06-21] Resume fix: verifyAgent is a false-negative for opencode (cmd.exe prompt matches SHELL_PROMPT_REGEXES) — warn-and-proceed, don't early-return. Launch uses terminalWriteRaw to bypass the queue.
- [2026-06-21] $[FIT-DBG] log (TerminalWindow.tsx:273) only fires on isActive transitions; in remount-on-group-switch architecture it never fires. Absence does NOT indicate fit failure - mount-time tryFit loop (lines 145-155) is the real mechanism.
- [2026-06-21] dnd-kit in TerminalMiniMap uses PointerSensor with 5px activation - native pointer events, NOT HTML drag-and-drop. Cannot be tested through accessibility API; structural verification (DndContext, GroupDropTarget, draggable attribute) is the correct approach.
- [2026-06-21] Clicking the New Terminal header button (title="New Terminal") creates a second group by splitting, causing BOTH TerminalPane components to remount (remount-on-group-switch architecture).
- [2026-06-22] DEFAULT_SYSTEM_PROMPT lives in src/lib/defaults.ts as a hardcoded template literal (84 lines post-v3 rewrite) AND is loaded into agent context via opencode.json instructions referencing agent/DEFAULT_SYSTEM_PROMPT.md. Both must be kept in sync; the opencode.json entry controls what the AI agent receives.
- [2026-06-22] opencode.json "instructions" paths MUST include exact file extensions (.md). Missing-extensions cause silent load failure — the entry is ignored with no error, and the file never enters agent context. Always verify that referenced files actually exist on disk with matching names and extensions.
- [2026-06-22] Finance modal context band pattern: parse `(wallet.metadata as any)` at the top of each modal component, then render a compact info bar after the opening `{() => (<>` and before the type toggle buttons. Accent colors match wallet type (Bank=#3B82F6, Debit=#8B5CF6, Credit=#F59E0B, Ewallet=#06B6D4, Crypto=#8B5CF6).
- [2026-06-22] TypewriterText flickering: inline arrow functions in `onDone` prop cause the animation effect to restart on every parent re-render because `onDone` is in the effect's dependency array. Always memoize callbacks passed to TypewriterText with `useCallback`.
- [2026-06-22] Provider name migration must use case-insensitive matching (toLowerCase) because saved `id`/`templateId` may have inconsistent capitalization (e.g. `CloudFlayer` vs `cloudflayer`).
- [2026-06-22] DevTools `before-input-event` should NOT check `mainWindow.isFocused()` — the webContents listener already scopes to the window, and `isFocused()` fails when any child element has focus.
- [2026-06-22] Cache invalidation for get-dashboard-aggregates: add a `markStatsDirty()` call after every `INSERT INTO stats_daily` write. Use module-level `dashboardCache` + `statsDirty` boolean + 60s TTL + `dashboardInFlight` promise map for dedupe.
- [2026-06-22] Phase 4.1/4.2 (useDisposable hook + Moon/Rings/Atmosphere/Trail) can be skipped in R3F projects — R3F handles JSX-created geometry lifecycle automatically. Only `new THREE.*` objects created outside JSX need manual disposal.
- [2026-06-22] Input/output token split: DB columns `input_tokens`/`output_tokens` exist in `ai_usage` but all queries used `SUM(input_tokens + output_tokens) as tokens`. To add human-vs-AI split: change all 5 queries in `get-ide-projects-overview` to select separate sums (`tokens_in`, `tokens_out`) at every level (top, daily, projects, modelBreakdown, modelDaily). Then propagate `tokensIn`/`tokensOut` through AIAgent interface → stacked bars in charts + ratio card + metrics grid.
- [2026-06-22] Wallet balance sync: Cash/Physical wallet denominations (metadata) are just metadata — they don't auto-update `wallet.balance` in DB. Must compute total from `localMetadata.denominations.reduce((sum, d) => sum + d.value * d.count, 0)` and call `onUpdateWallet` with `balance: totalFromDenoms` in `handleSave`. Only applies to cash/physical types. Other wallet types rely on explicit transaction creation for balance changes.
- [2026-06-23] AI tool registration pattern: Use `r(name, desc, params, securityLevel, category, handler)` helper in toolRegistry.ts. Read-level tools should gate with `checkAccess(key)` returning `{ _privacy: true, message }` when disabled. Confirm-level write tools call `api.methodName()` directly (preload binding). Long-term goals reuse the same `saveGoal` IPC handler with `period: 'longterm'`.
- [2026-06-23] Deep navigation: AI outputs `[type: navigation][page: /route][tab: key][section: id][label: text]` blocks. BlockRenderer passes section+tab to onNavigate. AiChat calls navigateTo() from deepNav.ts. Tab pages read location.state.tab on mount via useEffect with [] deps. Section IDs are `data-section="section.id"` attributes on section wrapper divs.
- [2026-06-23] parseBlocks.legacy.ts already handles `[key: value]` patterns generically (line 85-89). Any `[section: X]` or `[tab: Y]` in navigation blocks is automatically extracted into `block.fields.section` / `block.fields.tab` — no parser changes needed for new block fields.
- [2026-06-25] Goal decomposition uses `parent_id` on the goals table (not in `links` JSON) for efficient `SELECT WHERE parent_id = ?` queries. The `links` JSON field is used for goal<->problem/request cross-references.
- [2026-06-25] checklist AI tools require `checkAccess('checklist')` — add the `checklist` key to both the SettingsPage `dataAccess` array and the IPC `ai_dataAccess` preference storage for the privacy gate to work.
- [2026-06-25] "Cannot access 'X' before initialization" = TDZ. A React hook (useEffect) referencing a `const` variable before its declaration causes this. Fix: reorder so `useMemo`/`const` declarations precede hooks that depend on them.
- [2026-06-26] frontend-external-infra skill (`agent/skills/frontend-external-infra/SKILL.md`) is now the gateway to real component inventory. When building UI, load it BEFORE any design skill — it tells the agent which MCP server (shadcn/magicui/lucide/21st.dev/motion-dev/unsplash/reactbits/iconify) to call for real building blocks. Skills-only design (frontend-design, impeccable) teach taste but produce slop without inventory.
- [2026-06-26] MCP servers needing API keys (`@21st-dev/magic`, `unsplash`) use `scripts/mcp-launcher.mjs` which reads `.env` from project root and spawns npx with the right env vars. No-hardcoded-keys rule. Add new keyed servers by adding a config entry to the launcher's `cfg` object.
- [2026-06-26] Free-only MCP set: paid services (Motion+ AI Kit, Figma Dev Mode MCP, Magic Patterns) are excluded. Motion.dev community MCP (`github.com/Abhishekrajpurohit/motion-dev-mcp`) replaces the paid `motion-ai` — it requires manual clone+build, not npx. `MOTION_PLUS_KEY` env var is removed.
- [2026-06-26] CoinGecko API `vs_currencies` must match the user's `displayCurrency` — hardcoded `usd` causes wrong numeric values (e.g. $266 displayed as Rp264 instead of Rp4.2M). Always pass `displayCurrency` to `financeFetchCryptoPrices`/`financeGetCryptoHistory` IPC calls.
- [2026-06-26] `CryptoPrice` interface uses `coin_id` field, not `id`. All `prices.find(p => p.id === ...)` lookups silently fail → price = 0 → all values show as 0. Use `p.coin_id` consistently.
- [2026-06-26] Finance lock security bug: `checkSetup()` catch block called `setIsLocked(false)` on IPC failure — this opened the vault whenever the network/IPC failed. Fix: `setIsLocked(true)` in catch block. IPC failure = vault stays locked.
- [2026-06-26] Sticky-header collapse must listen to the ACTUAL scrollable ancestor, not `window`. In this app, pages render inside an `overflow-auto` div (App.tsx), so `window.scrollY` is always 0. Use a sentinel element + `getScrollParent()` helper, or IntersectionObserver against the real scroll container, to detect when the header becomes stuck.
- [2026-06-26] All unlock paths (`handleUnlock`, `handleBiometricUnlock`, `handleSetup`, rememberDevice) verified as correct — they all require successful IPC result before setting `isLocked(false)`. Only the catch block was wrong.
- [2026-06-26] AnimatePresence `mode="wait"` with two separate `{cond && <A>}` children causes React reconciliation to shift `false` values, preventing entering child from mounting. Use single ternary `{cond ? <A key="a"> : <B key="b">}`. If content still doesn't appear, remove AnimatePresence entirely — exit animation may not complete.
- [2026-06-27] Two-legged atomic transfers need a dedicated IPC handler with `db.transaction()` (BEGIN/COMMIT/ROLLBACK) — two separate `finance:create-transaction` calls can't rollback if the second leg fails. Store `transfer_id`, `from_wallet_id`, `to_wallet_id` on both legs. Delete handler must cascade to paired transactions via `transfer_id`.
- [2026-06-27] AICityscape Cycle 41: normalizedClone RefAxis type parameter for per-type scaling axis (height/length/footprint). IDM traffic with atan2 heading, intersection signals (14s cycle). DecorativeSkyline uses InstancedMesh with per-instance matrix scale from GLB bounding box — extract mesh via traverse(), compute `targetHeight / size.y` scale, setMatrixAt with rotation+position+scale combined. Quality gating: reduce car count 20→10, skip half furniture/trees, hide skyline in performance mode.
- [2026-06-27] Flex sizing chain for TerminalWindow: PaneRenderer split-child div must be `flex flex-col` + `min-w-0` (not just `min-h-0 overflow-hidden`) to propagate heights through nested flex. Leaf TerminalPane needs a `flex-1 min-w-0 min-h-0 overflow-hidden` wrapper. TerminalLayout outer div needs `flex flex-col` so root leaf wrapper works.
- [2026-06-27] userScrolledUp flag: `terminal.onScroll((newY) => { userScrolledUpRef.current = newY < buffer.baseY })` prevents unwanted scrollToBottom on tab/group switch. Check `!userScrolledUpRef.current` before calling `scrollToBottom()` in isActive effect.
- [2026-06-27] TransferWalletSelect must be a close-ended dropdown (not free-text input) for correct wallet linking. Group wallets by account with search, exclude the source wallet. The modal passes `to_wallet_id` in the payload; FinancePage routes `type === 'transfer'` to `financeCreateTransfer`.
- [2026-06-27] Transfer destination metadata merge: `handleAddTransaction` reads existing wallet metadata, merges incoming `dest_metadata.denominations` with existing counts, then calls `financeUpdateWalletMetadata` after a successful transfer. This updates physical/cash wallet onHand denominations when they receive a transfer from another wallet type.
- [2026-06-27] TransferDestinationPanel uses shadcn Alert-inspired left-accent `borderLeft` + `tint(accent, 0.04)` background pattern for info notices (crypto fiat label). Physical/cash destinations show a context header + `DenominationPicker`. Dest type detection is reactive via `destWallet?.id` changes.
- [2026-06-28] Prompt composition overhaul: Shared assembler (buildPromptLayers + renderSystemPrompt in promptAssembly.ts) routes both new-session and compose paths. Single-source Default via Vite ?raw import. Namespaced project keys (project:<id>) with idempotent migration. Four-scope Settings UI (Default/General/Agent-type/Project). GENERAL_ADDITIONS.md mirror via write-agent-file IPC.
- [2026-06-28] assemble-context IPC: Simple IPC handler in main.ts that queries workspace_problems, workspace_requests, terminal_sessions from DB and returns formatted markdown context. Preload binding `assembleContext`. Wired into handleInstructionPanelSend — prepends "## Assembled Dynamic Context" before sending.
- [2026-06-28] Duplicate `const { globalShortcut } = require('electron')` line in main.ts causes "symbol already declared" build error. Always grep for existing declarations before adding new ones.
- [2026-06-28] Critique vision sidecar build gotcha: `src/services/vision/index.ts` MUST inline type imports from `src/features/critique/types.ts` (copy types into the service file) because the build pipeline only pre-compiles `src/services/` — it does NOT bundle dependencies from outside that tree. Cross-directory `import { X } from '../../features/critique/types'` works at TS level but fails at runtime because `../../features/` resolves outside the compiled output.
- [2026-06-28] Learn Module resources (schema JSON, worked example .ldoc, author guide) live in `resources/learn/` and must be copied to `dist-electron/resources/learn/` during build. Added to `nonTsDirs` in build.mjs alongside the existing migrations copy. Resources are resolved at runtime via a `resourcePath()` helper that checks both dev path (`app.getAppPath()/resources/learn/`) and built path (`app.getAppPath()/dist-electron/resources/learn/`).
- [2026-06-29] Build fix: if main.ts requires `services/providers/providerLog.cjs`, build.mjs's `cjsShimTargets` array must include `'services/providers/providerLog.js'`. A missing .cjs shim causes "Cannot find module" at runtime even though the .js file compiled correctly.
- [2026-06-28] TWO completely separate HTTP client paths call AI providers — `callProvider.ts` (used by `get-topic-digest`/`test-ai-provider`) and `aiAgentService.callLLM()` (used by ALL chat interactions). They share NO templates, NO fallback chain, NO request/response logging. Fixes in one path NEVER propagate to the other. Any provider refactor must consolidate both paths.
- [2026-06-29] `better-sqlite3` v12.9.0 `db.backup()` does NOT reliably flush to disk on Windows — use WAL checkpoint + `fs.copyFileSync()` instead. The copy approach also avoids the `ERR_CRYPTO_INVALID_STATE` error from the native backup API.
- [2026-06-29] Backup verification DB must be opened WITHOUT `{ readonly: true }` on Windows — that flag produces `SQLITE_CANTOPEN` for gz-extracted backup files. Open writable for integrity check.
- [2026-06-29] ai_digest_fix_patch callProvider.ts and router.ts are 100% identical to ours — no changes needed. main.ts differed in only 2 spots: maxTokens 400→2000 (digest provider chain), anti-preamble system prompt wording.

## FINANCE PAGE LOCK SYSTEM
- [2026-06-29] Finance lock timer must run in the MAIN effect body (not cleanup), only when page is unlocked AND visible. Use `visibilitychange` listener to pause on hide and re-check backend lock state on show. Add interaction listeners (mousedown/keydown/touchstart/wheel) to reset timer on activity. The old bug: timer was in useEffect cleanup so it only fired on unmount, and navigating away then returning would auto-unlock because the backend's in-memory `financeLocked` was never re-checked on visibility return.
- [2026-06-30] Insights Typical Day: Original/Smooth modes were swapped. Original = backend 7×24 multi-day grid (patchedTypicalDay), Smooth = single-day raw log heatmap (originalDayData). Key lesson: when swapping large JSX blocks, use comment anchors (e.g., `{/* --- ORIGINAL MODE: ... --- */}`) in oldString to avoid ambiguity — multi-line edits can match wrong occurrences if the oldString is too generic.
- [2026-07-27] Preload build MUST use esbuild with `--format=cjs`, NOT Vite SSR. Vite `--ssr` outputs ESM (`import ... from "electron"`) even when renamed to `.cjs`. Electron's preload loader silently rejects ESM — window.deskflowAPI becomes undefined with no error. esbuild with `--bundle --format=cjs --external:electron` produces real CJS.
- [2026-07-27] `app.getPath('userData')` resolves to `%APPDATA%/<packageName>`. The `name` field in package.json MUST match the actual data folder. Mismatch = app reads wrong/empty database. On Windows NTFS, case is insensitive so `rheo` and `RHEO` resolve identically.
- [2026-07-27] Two separate DB files existed: `%APPDATA%/DeskFlow/deskflow-data.db` and `%APPDATA%/RHEO/deskflow-data.db`. The app was reading from DeskFlow (wrong), missing 11+ days of data that lived in the RHEO folder. Fix: change package.json name to match the real data folder.
- [2026-07-30] Category overrides MUST write to BOTH the JSON config file AND the DB table. `set-app-category` and `set-domain-category` IPC handlers only saved to `deskflow-categories.json` — `buildTierMap()` and `categorizeDomain()` read from DB tables (`category_overrides`, `domain_category_overrides`), so overrides were lost on restart. Fix: add `INSERT OR REPLACE INTO` in both handlers.
- [2026-07-30] `domain_category_overrides` table schema: `domain TEXT PRIMARY KEY, category TEXT NOT NULL`. Must be created at app startup alongside other tables. Separate table from `category_overrides` (which stores app overrides) — don't use a type discriminator column.

## electron-builder / packaging (2026-08-01)
- winCodeSign symlink failure: on this machine (Developer Mode OFF, non-admin shell) electron-builder aborts the NSIS/installer step with `Cannot create symbolic link : A required privilege is not held by the client` (darwin libcrypto.dylib/libssl.dylib). It re-downloads winCodeSign with a fresh cache hash EVERY run, so pre-seeding the cache does NOT help. Fix without admin: `\"win\": { \"signAndEditExecutable\": false }` in the build block (skips rcedit/signing; app exe then keeps the default Electron icon). To restore custom icon/metadata: enable Developer Mode once, remove that flag, rebuild.
- Build block `productName` was `DeskFlow` (produced DeskFlow.exe) while the app is RHEO - fixed to `RHEO`. The app icon file was `DeskFlow_AppIcon.png` at repo root and is referenced in THREE places (build.win/mac/linux icon + src/main.ts BrowserWindow + tray icon); renamed to `RHEO_AppIcon.png` and all live refs updated (do NOT touch agent/docs patch copies).
- electron-builder derives the installer name/version from the top-level `version` field in package.json (NOT from anywhere else). Bump it there + in package-lock.json root `version`; latest.yml/blockmap/installer filename follow automatically.
- One-command release build: `npm run dist` (= `node scripts/build.mjs` + `electron-builder --win`). Known harmless noise: npm ELSPROBLEMS 'invalid' warnings for react/react-dom/@react-three/fiber (npm ls --depth=0 is clean) - ignore them.
- Native modules (better-sqlite3, node-pty) verified loading under Electron 41.1.1 (modules_abi=145), so npmRebuild:false is safe with current node_modules.
- [2026-08-01] The finance "People" flow files (PeopleTab, PersonCard, PersonDetailModal, PaymentAllocationModal) are LF-only line endings (byte-verified: 0 CRLF) — NOT CRLF despite AGENTS.md's blanket claim. Use `\n` in multi-line edits there. They render money via raw `{displayCurrency}{x.toFixed(2)}` literals (no fmtCurrency import), so the number-mask wrapper is `const fmtMoney = (v:number) => showNumbers ? v.toFixed(2) : maskNumber(v.toFixed(2), maskMode, maskFixedValue);`.
- [2026-08-01] Finance Overview canonical layout: hero Net Flow GlassCard owns net flow (sparkline + MoM) — Quick Stats must NOT duplicate it. "Who owes me / repay" flows via groupByPerson(allTransactions) (src/lib/receivables; each person carries txIds) + RepaymentModal(onRecordFtRepayment iterating txIds) — NOT via SpendingSplitCard or getUnpaidByPerson (now unused in netWorth.ts).
- [2026-08-03] Finance follow-through (on-behalf-of) name resolution is FIXED at the data layer: main.ts finance:get-transactions (line ~25473) now LEFT JOINs finance_ft_persons fp ON t.ft_person_id = fp.id and selects COALESCE(t.on_behalf_of_label, fp.name) as on_behalf_of_label; the on-behalf-of summary handler (both encrypted + plain branches) and get-ft-persons stats also resolve via ft_person_id (stats match on_behalf_of_label = ? OR ft_person_id = ?). useTransactionForm.ts now initializes ftPersons from props.ftPersons so new modal-created transactions persist the real label. ALL renderer FT-name readers (getFtPerson in receivables.ts, groupByPerson 'Unknown', netWorth getUnpaidByPerson, TransactionDetailModal 'Someone') are fixed transitively since they consume get-transactions output. If a future read path queries finance_transactions directly (not via get-transactions), it MUST resolve ft_person_id too.
- [2026-08-04] AiPage CSS-on-first-load bug: the dk-* page shell styles (dk-root/dk-topbar/dk-chip) lived ONLY in deck.css, imported only by the lazy AiPageDeck chunk � first load in canvas/compositions mode rendered the shell unstyled until DECK mode happened to inject the CSS. Rule: any CSS used by an eager page component must be imported by that page (AiPage.tsx now imports ../components/ai/deck/deck.css); vite dedupes, so the lazy child's import is harmless.
- [2026-08-04] setAppCategory/setDomainCategory IPC handlers already existed in main.ts (line 5350) and preload.ts (line 171) � but pproveChange/pproveAllChanges in SettingsPage never called them. AI-approved category changes updated local React state only, never persisted to DB. Fix: both approval functions now call window.deskflowAPI.setAppCategory/setDomainCategory for each approved change, so the DB is updated immediately alongside the UI.
- [2026-08-05] External mosaic sizing (RHEO /external, RESULT.md v2 implemented): card weights are PROPORTIONAL = log1p(seconds)^gamma normalized (gamma presets subtle 0.65 / balanced 1.0 / dramatic 1.55), hierarchy control persisted in localStorage key 'external-mosaic-hierarchy' (try/catch), default balanced. sizeTier is areaFraction-driven (hero >0.20, secondary >0.12, medium >0.06) NOT index-based. K-cap: fractions < minCellAreaFraction (0.04) overflow out of the treemap into compactActivities (now ActivityWithSeconds[]; compact row labels sleep?'Sleep', seconds===0?'No time yet', else 'Overflow'). ActivityGridCell has cellHeight (rect.h layout px) gating card content: icon =100px, time chip (hero/secondary/medium) =80px, sparkline (hero/secondary) =160px. Verify grid changes with esbuild-bundle + node harness (TEMP/opencode/mosaic-harness.js, 25 checks). ExternalPage.tsx:796 gridLayout useMemo is DEAD (computed, never rendered) - safe to ignore. Never restore the old fixed ladder 0.55/0.27/0.18 - the user's 'the size is FIXED' complaint is why this exists.
- [2026-08-05] App Tracker (DeskFlow) totals invariant (user requirement): top-nav total AND Activity screen totals MUST match the chart — totals must count ALL tracked activity (apps + websites), never under-report by excluding website-tracking rows. `getLogs()` (main.ts:3776) returns both app + website rows; `getStats()` excludes websites. Website categories map to app categories via `WEBSITE_CATEGORY_MAP` (StatsPage + App.tsx). Total mode = sum of ALL period-filtered logs; Focus mode = productive tiers across apps+websites only. Default top-bar timeMode = 'total'. StatsPage Top Applications pct bars use apps-only denominator (appsOnlyTotalMs); known open gap: "App Time Distribution" pie is apps-only (no backend endpoint for website category distribution) — its header shows all-tracked total, pie stays apps-only.
- [2026-08-05] App Tracker runtime verification: the running DeskFlow/RHEO instance exposes NO --remote-debugging-port (port 9222 belongs to a Lenovo Vantage widget — do NOT attach to 9222). After ANY source change the user must fully close + relaunch the app to pick up the new bundle (process StartTime vs dist/assets/index.js LastWriteTime). Never kill the user's electron processes; runtime checks must be marked NOT LAUNCHED unless the app was started with the debug flag.
- [2026-08-05] Focus Groups implementation state: attribution singleton lives at src/hooks/useActiveFocusGroup.ts (module-level setActiveGroup/getActiveGroup/useActiveFocusGroup, listener-set pattern). useFocusGoals.ts rewritten to read ONLY real FocusPublicState fields (`active`) via useFocusSession + the singleton; exported contract `{ focusState, activeGoalIds, getAccumulatedSeconds, tick, matchedGoalIds }` is depended on by GoldPage.tsx + DailyPlannerCard.tsx � never change it. Empty group `allowed_categories` = ALL time goals qualify (matchGoalIds returns all).
- [2026-08-05] useFocusGroups() returns BOTH `selected` AND `selectedId` (line 130: `return { groups, loading, error, refresh, save, remove, startWith, selected, selectedId, setSelectedId }`) � `const { selected: selectedGroup } = useFocusGroups()` in DailyPlannerCard is valid, NOT a bug.
- [2026-08-05] Vite build does NOT type-check (esbuild strips types without checking) � a green build proves nothing about wrong destructures/API names; grep-verify names against the actual hook returns before shipping.
- [2026-08-05] better-sqlite3 native module ABI: compiled for Electron's Node (NODE_MODULE_VERSION 145) � system node (137) fails ERR_DLOPEN_FAILED. To query the DB from a script, run via `node_modules/electron/dist/electron.exe <script>` with explicit `process.exit(0)` at the end (else the process hangs); bare electron.exe piped console output can be swallowed � don't rely on stdout for verification.
- [2026-08-05] Focus Groups runtime verification: the running app instance had NO remote-debugging-port (prod server answers on the old 59841 port, serving dist/ from disk � hash matches new builds on reload). probe attach:true fails with "Unexpected token '<'" (HTML, not JSON) when the port is the prod server. Playwright MCP is loopback-blocked (ERR_CONNECTION_REFUSED on 127.0.0.1) while example.com works. Do not attempt launch mode a 2nd time (per prior lesson it never opens a debug endpoint and leaves orphans).
- [2026-08-06] NEVER write src.zip inside dist/ — vite's emptyOutDir does rmSync on dist and EPERM-fails when src.zip exists (my orphaned zip-src.mjs → Compress-Archive held the handle for minutes). Zip to src-export/src.zip (repo root) instead; tar excludes on Windows need FORWARD slashes (--exclude=agent/backups) or they silently fail and the zip balloons to 754MB. Identify own orphans via Win32_Process CommandLine ('node scripts/zip-src.mjs' + child 'Compress-Archive'), kill only those PIDs.
- [2026-08-06] beautiful-charts skill on Chart.js: striped-hatch bars = canvas pattern (8px tile: 40% solid base fill + 2px 45° diagonal stripes via createPattern, repeat) as dataset backgroundColor — keeps spec colors while adding texture; hover glow = solid hoverBackgroundColor; dashed crosshair = inline plugin reading chart.tooltip.getActiveElements(), passed to react-chartjs-2 via the plugins prop (NOT options.plugins); interaction mode:'index' + intersect:false needed to activate the crosshair.
- [2026-08-06] Focus Groups UI redesign: user was frustrated by (1) minimal/invisible UI, (2) transparent popup editor, (3) missing goal configuration, (4) no beautiful progress views. All addressed: opaque DialogContent (bg-zinc-950/95), wider timer layout (col-span-3 of 5), FocusGroupProgress component with per-group cards, dailyGoalSec + goalCategory fields added to FocusGroup type. Key lesson: dialogs in dark themes must be opaque, not transparent/glass.
- [2026-08-06] Vite build does NOT type-check (esbuild strips types) � a green build proves nothing about wrong destructures/API names; grep-verify names against the actual hook returns before shipping. This was already known but worth re-emphasizing after the DailyPlannerCard destructure false alarm.
- [2026-08-06] FocusGroup type fields: daily_goal_sec (number|null) and goal_category (string|null) are now part of the TypeScript types in useFocusGroups.ts. Backend DB columns need ALTER TABLE focus_groups ADD COLUMN daily_goal_sec INTEGER DEFAULT NULL, ADD COLUMN goal_category TEXT DEFAULT NULL � this is a follow-up task for the Architect.
- [2026-08-07] IPC response-shape hardening: a running app holds the OLD main.cjs in memory while a freshly built renderer bundle is served ? old handler returns legacy shapes (e.g. {success, updatedCount} with NO totalMismatch) and new UI code crashes (SettingsPage toLocaleString). Any renderer consuming a new IPC response MUST normalize numbers/arrays defensively (typeof n === 'number' ? n : 0, Array.isArray(x) ? x : []) � never trust the response shape across a hot-rebuild boundary.
- [2026-08-07] Phantom website tracking root cause FIXED: the `forceBrowserTracking` pref bypassed ALL focus gates in `/browser-data` + `handleBrowserData` (main.ts) â€” with it true, extension site deltas were accepted/logged even while VS Code was foreground (proven rows chat.z.ai/resmume/Kimi during Aug 6 11:05-12:55 with zero app logs). Gating is now UNCONDITIONAL: `is_browser_focused` + desktop's own foreground state are always enforced; the pref is dead code (Settings toggle removed; pref remains in prefs JSON harmlessly). Never re-introduce a bypass knob for browser-data ingestion.
- [2026-08-07] pollForeground checkpoint logging bug FIXED: the 2-min checkpoint called `categorizeApp(app, { isResolvedGame: true })` UNCONDITIONALLY â€” every checkpoint row for ANY app got category 'Gaming' (VS Code logged 44 Gaming rows â‰ˆ 0.62h beside its IDE rows). Contract: `resolveForegroundApp` (src/gameDetection.ts) returns `source === 'raw'` for non-games; map/index/scan/keepalive/title = resolved game. Fix: track `currentIsResolvedGame = !!resolved && resolved.source !== 'raw'` at poll time and pass it to the checkpoint only.
- [2026-08-07] browser-extension background.js `checkBrowserFocus` must match ONLY its own browser aliases (`BROWSER_NAME` + own getBrowserProcessNames) â€” the old union-of-all-browsers set (chrome,edge,comet,msedge,...) made the Comet extension report focused whenever ANY browser was foreground. Desktop/extension alias handling must stay mirrored per-browser, never unioned.
- [2026-08-07] Windows/PowerShell gotcha: `%APPDATA%` is NOT expanded inside double-quoted strings passed to native executables (sqlite3) â€” use `$env:APPDATA`. Also: PS 5.1 Compress-Archive can throw ConstructorInvokedThrowException on a ~755MB tree when invoked via execSync from node, yet succeeds when run directly in the shell; if zip-src.mjs fails, run the Compress-Archive command manually.

