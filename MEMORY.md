# MEMORY.md — Durable Cross-Session Lessons

- [2026-04-19] MEMORY.md added to project for durable cross-session learning.
- [2026-04-19] Project uses `scripts/build.mjs` — use this, not raw `vite build`.
- [2026-04-19] Preload must be built with esbuild (`--format=cjs --external:electron`), not Vite SSR, because Vite SSR outputs ESM `import` syntax in a `.cjs` file that Electron cannot load.
- [2026-07-07] When editing index.html to remove script blocks, NEVER delete surrounding tags (`</head>`, `<body>`, `<div id="root">`, `<script type="module" src="/src/main.tsx">`). Always verify the file still has `<div id="root">` and the Vite entry script after edits. A missing entry script = Vite builds 1 module instead of 5900+ = black screen.
- [2026-04-19] Build pipeline step 3 (pre-compile services via esbuild) can time out after 60s; step 4 (Vite library-mode build of main.cjs) never runs if step 3 hangs. Use `scripts/rebuild-main.mjs` to rebuild main.cjs independently.
- [2026-04-28] Deep Focus sessions use tables `deep_focus_sessions` and `deep_focus_events` with columns matching the schema in `src/domains/focus/focusSchema.ts`.
- [2026-04-28] The Learn module uses Lesson Markdown (.lmd) as the primary AI output format; JSON parsing (`parseLessonJson`) is a fallback. `BookCard.tsx` and `LessonLibrary.tsx` are the book-shelf UI components.
- [2026-04-28] Connector data stored in `connectors` table (configs) + `connector_items` table (synced emails/events) with full IPC CRUD + test + sync + items operations. Check preload.ts and main.ts before assuming new IPC channels are needed.
- [2026-05-03] Daily Digest must be surfaced as a first-class, prominent section in AiPage rather than nested inside Reflect.
- [2026-05-03] Focus HTTP endpoints are token-gated via X-DeskFlow-Token header on 127.0.0.1:54321.
- [2026-05-03] Rewind/Insight Engine uses template-only copy generation — no LLM calls, deterministic scoring only.
- [2026-07-07] CYCLE 139: Design Arena rrweb/analytics scripts in index.html (lines 26-34) caused `Uncaught (in promise) TypeError: Failed to fetch` console error from an uncaught fetch() POST to designarena.ai. Removed all three script blocks. No package.json dependencies existed — they were inline scripts. CONFIRMED: These were NOT the cause of the goal creation crash — that is the 10s AbortError in callProvider.ts.
- [2026-07-07] CYCLE 139: callProvider.ts line 47 has a hardcoded `new AbortController()` with `setTimeout(() => controller.abort(), 10000)` — no configurable timeout. This is the root cause of all goal-creation crashes. router.ts line 76 (`if (err.status !== 402) throw err`) propagates AbortError immediately because it has no `status` property.
- [2026-07-07] CYCLE 139: Bug-fix prompt at agent/docs/ai-page-fix/prompt.md + CONTEXT_BUNDLE.md ready for Architect AI.
- [2026-07-07] CYCLE 141: When writing a prompt about a page or feature set, the prompt and context bundle MUST cover ALL features of that page, not just the one broken feature that triggered the report. The Architect needs full context to avoid breaking unrelated subsystems.
- [2026-07-07] CYCLE 144: CONTEXT_BUNDLE.md written from task-agent-gathered source may not match the actual codebase. Always verify changes against the REAL source files before implementing. The actual AiPage codebase was more mature than the bundle suggested — most components already had StateShell, error/loading/empty states. Only 4 of 16 proposed files actually needed changes.
- [2026-07-08] CYCLE 146: AiPage hardening (RESULT.md) required 16 file changes. Key pattern: controllers (AiPage.tsx) hold error/toggleError/acceptError state, child components receive them as props and render inline error banners. The toast system (showToast) is the centralized feedback mechanism; per-item errors are granular state maps (Record&lt;id, string&gt;). AiPageDeck is a pure pass-through shell — no direct children, only slots.
- [2026-07-08] CYCLE 147: Black screen #1 cause = VITE_DEV_SERVER_URL in .env makes Electron load from Vite dev server (port 5173). Connection refused → did-fail-load fallback used `loadFile(file://)` which breaks `crossorigin` module scripts under `webSecurity: true`. Fix: `did-fail-load` must startProdServer() via HTTP, never loadFile(). Also clear VITE_DEV_SERVER_URL in start-dev.ps1.
- [2026-07-08] CYCLE 147: Black screen #2 cause = EPIPE uncaught exception from console.log in browser tracking HTTP server kills main process. Fix: `process.stdout.on('error', () => {})` + `process.on('uncaughtException', console.error)` at top of app.whenReady(). Never leave HTTP server console.log unprotected.
- [2026-07-08] TDZ bug: useCallback dep arrays are evaluated at declaration time — if they reference const functions declared later in the component, ReferenceError is thrown. Define ALL load functions BEFORE any function that references them.
- [2026-07-10] CYCLE 159d: Vite library-mode builds: output main.cjs requires `.cjs` extension explicitly (`require("foo.cjs")`), NOT bare `require("foo")`, because `module._extensions` in this Electron version only includes `.js`, `.json`, `.node` — no `.cjs` auto-resolve.
- [2026-07-10] CYCLE 160: Fundamentals #1-5 code was confirmed present in current src/. The only gap was `generateAgentConfigs` not wired into `runInitAll()` — now auto-generates project-scope configs on init. `getAllRunConfigs` IPC handler added. Work/run-configs subtab built in WorkspaceShell Work group.
- [2026-07-09] CYCLE 150: Sleep date assignment — `getSleepGroupDate` in `get-sleep-trends` MUST check both `started_at` AND `ended_at` dates. Only shift AM bedtime to previous day when they are on DIFFERENT local calendar dates (session crossed midnight from prior evening). If both are same date (e.g., fell asleep 1AM, woke 9AM same day), DO NOT shift. The old `h < 12` heuristic alone was wrong because it shifted same-day AM sessions to the wrong date. Also added `was_shifted` flag to daily trend data so chart labels use the correct date without re-deriving from `appExitMin < 720` heuristic.
